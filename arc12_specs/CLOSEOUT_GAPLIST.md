# Arc 12 — Running Closeout Gap List (for founder review)

Maintained live during the build. Folded into the final closeout report at WU8/arc close.

## Document gaps / contradictions found (founder decision needed)
1. **`lookup_property` data source has no owning arc.** Architecture §3.3.2 names the source as "MLS or admin-uploaded CSV" but no arc in §6 owns the property-source infrastructure (admin CSV upload UI / MLS connector). Tool ships with a contract-complete interim body labeled UNASSIGNED. **Founder: assign an arc.**
2. **`push_to_crm` native-CRM paths have no owning arc.** §3.3.2 names "HubSpot, Salesforce, custom webhook." The custom-webhook path can ride Arc 12 WU6's BYO outbound mechanism; the native HubSpot/Salesforce connector paths are unassigned in §6. **Founder: assign an arc (or confirm BYO-webhook is the only v1 CRM path).**

## Documented interim deviations from steady-state architecture (justified, tracked)
- **Cognition relocation (WU7).** escalate / save_memory / summarize relocated from the tool registry to a minimal always-on cognition module (Decision #20, §3.4). Marked interim; absorption into `LucielOrchestrator.run` is an Arc 14 exit criterion. Present in tree ahead of its permanent home — named here per founder ruling 6.
- **Interim tool bodies.** send_email/send_sms (Arc 13 adapters), call_sibling_luciel (WU5 body), bring_your_own_webhook (WU6 body), book_appointment/schedule_callback (dependency-gated). All contract-complete, no side effect, greppable TODO(<ARC>). Aligned per the documents' own arc schedule, not drift.

## Resolved-by-documents (recorded for closeout transparency; no founder action needed)
- **Sibling-grant cascade fires on DELETE, not PAUSE — correct per §3.6.1.** The Arc 11 lifecycle has pause (reversible, knowledge+sessions retained, instant resume) vs delete (soft-delete, 30-day grace, retention worker hard-delete). §3.6.1's "Deactivate Instance" cascade soft-deletes knowledge + frees the instance slot = destructive/delete semantics, so the grant-revoke cascade belongs on `delete_instance_with_grace`/`delete_by_pk`. Revoking grants on a reversible pause would orphan the composition graph and force re-authoring on every temporary pause, contradicting pause's contract. WU4 wired it to delete — aligned.
- **`reject` does NOT reuse `approved_by_user_id`.** It takes a distinct `rejected_by_user_id` param, writes `approval_state='revoked'` (§3.3.4 defines only live/pending_approval/revoked — no separate rejected state), and records the rejecter in the audit `after_json`. Aligned with §3.3.4's column set; rejecter identity preserved in the immutable audit trail.

## OPEN — must reconcile before close (not yet resolved)
- **No tool-authorization admin API exists (WU2b — in-scope Arc 12 gap).** WU2 built `instance_tool_authorizations` (table + repository + service) and the default-deny broker gate, but exposed NO Control-Plane API to LIST available tools for an instance or to SET/toggle per-instance tool authorization. The Tool UI's add-on band (checkboxes per §3.3.3) has nothing to call. Arc 12 delta row names "tool UI" + "tool authorization at runtime"; closeout rule requires frontend tool UI to match real backend endpoints (no ghost routes). MUST build a tool-authorization API (GET available tools w/ tier+channel+auth-state for an instance; POST/PUT/DELETE authorization row) before the UI is functional. Tracked as WU2b.
- **Sibling calls write the admin audit row but not yet the tool_execution_log.** §3.3.4 requires every sibling call to ALSO write the tool execution log with the full chain; §3.7.3 requires the admin-visible sibling-access audit row. WU5 (parallel) wrote the admin_audit_log row (ACTION_SIBLING_ACCESS) but `tool_execution_log` only came into existence in WU6 (parallel sibling). WU8 must wire the sibling dispatch to ALSO emit a tool_execution_log row so both §3.3.4 and §3.7.3 are satisfied. Also confirm BYO invocations and (future) all tool dispatches emit tool_execution_log per Decision #10.
- **Test counts are inconsistent across subagent runs and UNVERIFIED by me (hardening required at WU8).** Reported totals drifted: 9→11 failures (WU2→WU4), then during EX3 a run reported 1464 passed / 5 failed with the 6 rls_c4_3 failures ABSENT and ~370 fewer passing tests than the prior ~1831 — strongly suggesting different test-collection scopes ran across subagents (subset runs, or rls_c4_3 erroring at COLLECTION on some sandboxes vs FAILING on others). I cannot run pytest here (no deps). WU8 MUST: (1) do ONE authoritative `pytest` full-collection run on a properly-provisioned sandbox; (2) bisect EVERY failure against Arc 11 baseline `d4baf14` proving pre-existing vs introduced; (3) explain the collection-count variance; (4) fix the 6 rls_c4_3 stale-path tests (founder no-deferral rule — a hardcoded `/home/user/workspace/luciel/` path is itself drift) and the 3 audit_script env failures so the suite is GENUINELY green, not green-by-omission. No subagent's inline pytest count is trusted until this WU8 run.

## OPEN — website/backend reconciliation (WU8)
- **Website still carries domain_id in embed-key/invite/team-member TS contracts — stale vs v2 backend.** Backend EX1c/EX3 fully removed domain_id from those public schemas + dropped the columns; the Tool UI agent judged them "still valid" (couldn't see the backend sweep). Frontend↔backend drift — closeout rule forbids it. WU8 (website side) must remove the dead domain_id fields from the website's embed-key/invite/team-member TS types + any code passing them. Small/surgical.
- **Vestigial backend `onboarding.default_domain_id`** (+ default_domain_display_name) is a legacy seed field; onboarding_service only uses it to seed an `allowed_domains` egress list (different concept). Low priority. WU8 decision: rename/retire to a non-"domain" concept or leave with a clear note (it does not bear a DB column and is not the legacy Domain layer).

## EXCISION COMPLETE (founder-directed, verified)
- agent_id/domain_id fully excised: code (EX1a-d), RLS (EX2), all 9 non-audit-chain columns (EX3 one-table-per-run: trace, memory, api_key, session, conversation, knowledge, identity_claim, user_invite, scope_assignment), and the admin_audit_logs hash-chain reseal (EX4, founder-locked). Verified: ZERO agent_id/domain_id ORM columns remain; `_CHAIN_FIELDS` clean; single migration head `arc12_ex4_reseal_audit_chain_drop_agent_domain`. Uniqueness guarantees preserved on v2 column sets (identity_claim, scope_assignment). EX4 emitted an ACTION_AUDIT_CHAIN_RESEALED traceability record; downgrade is schema-only (v1 hashes unrecoverable by design — documented).
- **WU8 cleanup item:** stale module DOCSTRINGS still describe the dead three-layer (tenant_id, domain_id, agent_id) scope model in prose (auth.py, conversation.py, identity_claim.py, scope_assignment.py headers). Text-only, no live code. Refresh in the WU8 alignment pass so documentation matches the v2 Admin→Instance reality.

## Pre-existing defects (NOT introduced by Arc 12; flagged for cleanup decision)
- **6 × `test_rls_c4_3*` failures** hardcode a stale absolute alembic.ini path (`/home/user/workspace/luciel/`). Machine-specific path in a test = drift. Predates Arc 12. **Decision: fix in Arc 12 sweep or schedule?** (Founder rule says no deferrals — leaning toward fixing at WU8 since the suite must be green to close.)
- **3 × `test_arc11_audit_script` failures** env-dependent. Same question.

## Larger-than-delta-row diffs (disclosed per founder ruling 5)
- **chat_service.py sweep (WU7):** removing superseded Domain/Agent three-layer scaffold + substring tool-detection. The Arc 12 delta row (§6) does not name chat_service; this is in scope under the non-deferrable file-wide alignment mandate. Diff will be larger than the delta row implies.
- **admin_tier_overrides.max_composition_depth column drop (WU2):** schema cleanup of a Decision-#19-violating column; not named in the delta row but required for schema-vs-documents alignment.

## Entitlements vs Vision §7 reconciliation
- TODO at WU8: verify app/policy/entitlements.py tier capability table matches Vision §7 exactly; flag any discrepancy before close (per arc completion rule).
