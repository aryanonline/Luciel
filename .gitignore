# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# Virtual environment
.venv/

# Environment secrets
.env

# IDE
.vscode/
.idea/

# Database files
*.db
*.sqlite3

# OS
.DS_Store
Thumbs.db

# Step 26 report artifacts (local-only)
step26_report.json
step26_report_*.json

# Step 26b runbook artifacts (local only)
current-taskdef.json
app-task-def.json
migrate-task-def.json
overrides-*.json

# Step 27c-final + Step 27 deploy: runbook-artifact patterns. Script
# bodies are versioned; only ephemeral per-session intermediates and
# evidence files are excluded.
ep_sg.txt
*-task-def-v*.json
app-task-def-v*.json
migrate-task-def-v*.json
worker-task-def-v*.json
mint-overrides.json
sync-evidence-overrides.json
probe-full.txt
worker-log-dump*.txt
step*-snapshot-id.txt
step*localfull*.json
step*prodfull*.json
step*prodgateevidence*.txt
step*_local_FULL_GREEN_*.json
step*msg.txt
step*tagmsg.txt
step27cfinal_msg.txt

# Step 28 Phase 1: runbook-artifact patterns. Script bodies preserved
# in commit messages + admin_audit_logs rows; not committed to git
# because they mutate live data on a specific DB.
d11_sweep.py
step28_*_sweep.py
step28_*_msg.txt

# Step 28 Phase 1 - prereq-data: Pattern O image digest workspace files.
# Canonical digest provenance is in ECR via `aws ecr describe-images`.
step28-prereq-data-recon-image-digest.txt

# Step 28 — session-authoring & operator-side debug scratchpads.
# These are operator scratch files; never appropriate to commit.
RESUMESAT.md
RESUMEMONDAY.md
_RESUME_MONDAY.md
inspect_audit.py
inspect_db.py
inject_orphan.py
verify_dryrun.py
msg.txt

# Step 28/29 local diagnostic artifacts (audit residue, not part of source)
_step28_artifacts/
_step29_artifacts/
