# Luciel Drift Register

This file is the canonical, in-repo register of all named drift tokens. Per the Luciel working doctrine, every fix lands as one named drift token + one named commit + a verify gate. Tokens are immutable once recorded; supersession is captured via a new entry that references the prior token, never by editing.

Source-of-truth precedence: code > commit > recap > prior recaps > chat. This register is a **commit-derived index**, not a substitute for `git log`. The commit hash column is authoritative; the prose is a convenience pointer.

## Schema

| Column | Meaning |
|---|---|
| Token | Drift token, format `D-<slug>-YYYY-MM-DD` |
| Commit | Hash on the branch where the fix landed |
| Branch | Branch the commit is on at time of entry |
| Status | `closed` (fix landed + verified), `open` (acknowledged, not yet fixed), `deferred` (carry-forward to a future step), `superseded-by-<token>` |
| Pillar / Cluster | Verification pillar or 29.y cluster the token belongs to, if any |
| Summary | One-line description; full rationale lives in the commit body |

## Active entries

### Step 29.y gap-fix series (2026-05-07)

Branch: `step-29y-gapfix` (forked from `step-29y-impl`).

| Token | Commit | Status | Pillar / Cluster | Summary |
|---|---|---|---|---|
| `D-actor-permissions-comma-fragility-2026-05-07` | `1099f4f` | closed | P23 (audit chain) | JSON-serialize new `actor_permissions` writes; dual-format read; historical rows untouched to preserve hash chain |
| `D-audit-note-length-unbounded-2026-05-07` | `30878b0` | closed | P23 (audit chain) | Cap audit `note` at 256 chars at `AdminAuditRepository.record()` boundary with truncation marker |
| `D-scope-policy-action-class-gap-2026-05-07` | `5d99db8` | closed | P14 (scope policy) | Add `ScopePolicy.enforce_action(...)` primitive; no callers wired in this commit |
| `D-worker-audit-write-failure-not-alerted-2026-05-07` | `491d427` | closed | P23 (audit chain) | `WORKER_AUDIT_WRITE_FAILED` structured log marker + thread-safe process-local failure counter |
| `D-findings-docs-not-in-repo-2026-05-07` | `d2572db` | closed | meta | Reconstruct `docs/findings/` index for phases 1b/1d/1e/1f/1g from code + commit history |
| `D-cluster-4b-deferral-undocumented-2026-05-07` | `4c8522a` | closed | Cluster 4b | `docs/STEP_29Y_DEFERRED.md` records E-6 / E-12 / E-13 carry-forward to Step 30b |
| `D-cluster-7-unaccounted-2026-05-07` | _this commit_ | closed | Cluster 7 | Cluster 7 has no commits, tests, or code citations on `step-29y-impl`; investigated, no evidence on branch; logged here as the canonical disposition |
| `D-step29y-impl-no-close-tag-2026-05-07` | _pending C8_ | open | meta | No `step-29y-complete` tag exists; `docs/STEP_29Y_CLOSE.md` will define the verify-then-tag checklist |
| `D-historical-rate-limit-typo-disclosure-2026-05-07` | C19 _this commit_ | closed | P11 (rate-limit fail-mode) | Disclosure of historical rate-limit fail-mode typo. Pre-29.y the SlowAPI middleware read `os.getenv("REDISURL")` (no underscore) at module load. All prod environments export `REDIS_URL` with the underscore, so the lookup returned `None` and SlowAPI fell through to `memory://` storage. Effect: per-route rate limits were per-process across N backend containers instead of cluster-shared, so a tenant on a stated `100/min` limit could effectively achieve up to `100*N/min` by being load-balanced across replicas. The typo was already corrected in `app/middleware/rate_limit.py:20` during Step 29.y Cluster 5 (B-1) before this disclosure landed; this register entry documents the historical exposure window for compliance. No customer-facing rate-limit incidents were reported during the typo lifetime. Carried into the canonical recap v3.5 §11.2a. Closed alongside `D-redis-url-centralize-via-settings-2026-05-08` which makes future regressions of this class compile-time-detectable by routing all `REDIS_URL` reads through `app.core.config.settings`. |
| `D-canonical-recap-v3.4-omits-step-29x-29y-2026-05-07` | _pending C10_ | open | meta | Canonical recap v3.4 omits Step 29.x and Step 29.y; v3.5 will document 44 commits / ~5300 lines and reference all gap-fix tokens |
| `D-audit-verification-harness-retry-duplicates-2026-05-07` | C11 (`3798b32`) + C12 _this commit_ | closed | Cluster 4 (E-2) / DISC-2026-003 | 223 verification-harness duplicate `worker_*` audit rows blocked migration `d8e2c4b1a0f3`; first attempt deleted 166 rows but broke 60 hash-chain links (P23 FAIL); reverted via CSV restore and migration redesigned forward-only; full disclosure in `docs/DISCLOSURES.md` |
| `D-cluster4-e2-rework-as-forward-only-2026-05-07` | _this commit_ | closed | Cluster 4 (E-2) / DISC-2026-003 | Migration `d8e2c4b1a0f3` redesigned to add `AND created_at >= TIMESTAMPTZ '2026-05-08 04:00:00+00'` to its partial UNIQUE index, preserving Pattern E without exception (no historical row mutated, chain unbroken, control enforced from cutoff forward) |
| `D-celery-app-not-imported-on-uvicorn-boot-2026-05-07` | C13 (`0922cdf`) | closed | P25 (worker pipeline liveness) / latent prod | `app/main.py` did not import `app.worker.celery_app`, so `@shared_task` on `extract_memory_from_turn` (and any other task) bound to Celery's default `current_app` whose default broker is `amqp://guest@localhost//`. Pillar 25 probe and any chat-turn async enqueue on a fresh uvicorn process would raise `kombu.exceptions.OperationalError` (port 5672 refused) instead of publishing to the configured Redis/SQS broker. Fix: import `celery_app` in `app/main.py` (canonical) and `app/memory/service.py` (defense in depth). Latent prod risk because verification harness imports `_infra_probes.py` which loads `celery_app` and masked the bug in dev — first real production chat turn after a clean uvicorn boot would have hit this. (Pre-C15 note: at C13 commit time this entry incorrectly listed P13 as also fixed by C13; P13's failure was actually a test-side poll-timeout, see `D-pillar13-spoof-audit-poll-too-short-2026-05-07` / C15.) |
| `D-pillar14-consent-grant-uses-wrong-tenant-key-2026-05-07` | C14 (`0b6f913`) | closed | P14 (departure semantics) | `pillar_14_departure_semantics.py` issued the T2 consent grant with `k1_raw` (T1's chat key) but specified `tenant_id=t2_id` in the body. `consent.py:_resolve_tenant_id` correctly rejected this as cross-tenant (`403 cross_tenant_denied`), surfacing as a P14 FAIL. Test bug, not a route bug — the production contract is correct and stays untouched. Fix: swap `k1_raw` -> `k2_raw` so the T2 consent is issued with T2's own key, matching the symmetric K1/T1 consent grant 27 lines earlier |
| `D-pillar13-action-constant-case-divergence-2026-05-08` | C18 _this commit_ | closed | P13 (cross-tenant identity) / supersedes `D-pillar13-spoof-audit-poll-too-short-2026-05-07` (C15, C16) | `app/verification/tests/pillar_13_cross_tenant_identity.py:104` re-declared the audit-action constant locally as `"WORKER_IDENTITY_SPOOF_REJECT"` (uppercase). The model at `app/models/admin_audit_log.py:69` defines the canonical value as `"worker_identity_spoof_reject"` (lowercase), and that is the literal value the worker writes into `admin_audit_logs.action` when Gate 6 fires. The forensic endpoint `/admin_audit_logs_step29c` filters with `AdminAuditLog.action == :action` against PostgreSQL, which is case-sensitive on `text`, so the test polled `action="WORKER_IDENTITY_SPOOF_REJECT"` and never found a row that was always present in the DB under the lowercase action name. C15 (120s) and C16 (180s) widened the poll budget under the false hypothesis that latency, not filter mismatch, was the cause; both were symptom-level and could not have helped. **Direct DB evidence:** row id 6888, action `worker_identity_spoof_reject`, tenant `step24-5b-p13-t1-59201c30`, actor_key_prefix `luc_sk_hnXOx`, created_at 12:38:46.825 UTC -- present and correct on the 2026-05-08 verification run while P13 polled to 195.47s and FAILed. Manual `curl` to the same endpoint with the lowercase action returned the row in <1s. Fix: import `ACTION_WORKER_IDENTITY_SPOOF_REJECT` from `app.models.admin_audit_log` so divergence becomes a compile-time error rather than a silent 180s hang. Also revert poll budget to 6 x 5s = 30s (total 90s post-enqueue) -- tight enough to surface a real future Gate 6 latency regression instead of dead-waiting through 180s of false patience. Production unaffected. Worker write-side, model constant, and audit chain are all correct and untouched. | 
| `D-redis-url-centralize-via-settings-2026-05-08` | C19 _this commit_ | closed | meta / architecture | `REDIS_URL` was historically read in 4 separate locations (`app/middleware/rate_limit.py`, `app/worker/celery_app.py`, `app/verification/_infra_probes.py`, plus its definition in `app/core/config.py`). Three of those sites used `os.environ.get("REDIS_URL", "redis://localhost:6379/0")` directly, bypassing the `Settings` object that exists for exactly this purpose. This is the same class of drift as the `REDISURL` (no-underscore) typo (`D-historical-rate-limit-typo-disclosure-2026-05-07`) — a future engineer fixing one site would silently miss the others. Fix: route all three reader sites through `app.core.config.settings.redis_url`. CELERY_BROKER_URL stays a direct env read because it is broker-selection state (sqs:// vs redis://), not a Redis instance URL. Architecture rationale committed as `docs/architecture/broker-and-limiter.md`, which documents (a) why prod runs both SQS-as-broker and Redis-as-rate-limit-store, (b) the precedence rules for both URLs, and (c) the failure modes of each. Production runtime behaviour unchanged — same env vars, same SSM sources, same effective values; only the Python read path is centralized. |
| `D-celery-app-set-default-or-import-order-2026-05-08` | C17 (`2f73121`) | closed | P25 (worker pipeline liveness) / latent prod | After branch consolidation onto `step-29y-gapfix`, Pillar 25 probe regressed to FAIL (HTTP 500 in ~4s) with `kombu.exceptions.OperationalError [WinError 10061]` to `pyamqp/5672` — even though `app/main.py:27` correctly imports `app.worker.celery_app` (C13). Diagnostic: `from app.worker.celery_app import celery_app; from celery import current_app; current_app is celery_app` returned `True` in a fresh `python -c` shell, but failed under uvicorn. Root cause: `Celery()`'s constructor only registers itself as `default_app` if no default exists yet; under uvicorn another import path can touch `celery.current_app` before our module loads, leaving the default at Celery's stock instance whose broker is `amqp://guest@localhost//`. C13's import-on-boot is necessary but not sufficient. Fix: add unconditional `celery_app.set_default()` after the `Celery(...)` constructor in `app/worker/celery_app.py`. Idempotent and import-order-independent. Production-relevant: any uvicorn boot ordering shift in prod (different middleware load, plugin update) could have surfaced the same regression silently — first real chat turn would have raised the same kombu error. | 
| `D-pillar13-spoof-audit-poll-too-short-2026-05-07` | C15 (`cf69393`) + C16 (`128ce83`) | superseded-by-`D-pillar13-action-constant-case-divergence-2026-05-08` | P13 (cross-tenant identity) | P13 polled for the `ACTION_WORKER_IDENTITY_SPOOF_REJECT` audit row with a 60s sleep + single 5s retry (65s total). Under `--pool=solo` worker contention (Windows dev constraint; prefork not supported on Win32), tasks serialize behind P12's 42s legit extraction + P13's setup-turn legit extraction + any leftover tasks from prior runs. Gate 6 audit row latency observed: 70-150s end-to-end. Verified directly: `admin_audit_logs` rows 6224 (run 1, `created_at` 04:06:21) and 6388 (run 2, `created_at` 04:14:48) both contain the correct `worker_identity_spoof_reject` action with correct `actor_key_prefix` and mismatch note. Route + worker + Gate 6 all correct. C15 widened to 120s, still flaked at 133s. C16 widens to 180s (60s sleep + 24 x 5s poll), covers worst observed run with ~30% margin. A real Gate 6 regression would still surface as a different action or no row at all, and the AssertionError now includes a copy-paste-ready forensic SQL query to disambiguate. Production unchanged. (Carry-forward: document `--pool=solo` dev-vs-prod gap in `docs/STEP_29Y_DEFERRED.md` so threadpool migration on the dev worker is tracked; prod uses SQS with multiple workers, so head-of-line blocking is dev-only.) |

### Step 29.y close-out series (2026-05-08)

Branch: `step-29y-gapfix` (continuing).

| Token | Commit | Status | Pillar / Cluster | Summary |
|---|---|---|---|---|
| `D-prod-ops-role-missing-ssm-tags-2026-05-08` | C23 (`bc8b269`) | closed | meta / IAM | `luciel-ecs-prod-ops-role` inline policy `WriteProdPlatformAdminKey` granted `ssm:PutParameter` but not `ssm:AddTagsToResource`. boto3's `put_parameter(Tags=[...])` issues both API calls under the hood, so any prod-rotation attempt that requested tags would 403 on the tag write while the value-write succeeded — leaving the SSM parameter rotated but untagged with `luciel:key_id`. Discovered during Step 29.y Phase E key consolidation when three sequential mints (ids 593/594/595) rolled back at the tag-write step. Fix: add `ssm:AddTagsToResource` to the same Sid, scoped to the same single-resource ARN. The boto3 SDK quirk that issues two API calls for one logical operation is documented inline in the policy file. |
| `D-ssm-write-overwrite-false-blocks-rotation-2026-05-08` | C24 (`bc8b269`) | closed | meta / key rotation | `app/services/api_key_service.py:_write_key_to_ssm` issued `put_parameter(Overwrite=False, Tags=[...])`, the bootstrap-only contract. The production rotation path (which receives an explicit `ssm_path` argument from the caller) needs `Overwrite=True` because the parameter already exists from a prior rotation. With `Overwrite=False` the second-and-later rotation would 400 with `ParameterAlreadyExists` and silently fail. AWS API also forbids passing `Tags` alongside `Overwrite=True`, so the rotation path is split: `put_parameter(..., Overwrite=True)` first, then a separate `add_tags_to_resource` call. Bootstrap path (no `ssm_path` arg) keeps the original single-call contract with `Overwrite=False`. Tag-write failures are logged-but-non-fatal because the DB audit row is the source of truth for which key id belongs to which SSM path; tags are convenience metadata for human operators. |
| `D-audit-chain-listener-only-in-app-main-2026-05-08` | C25 _this commit_ | closed | P23 (audit chain) | `install_audit_chain_event()` (the SQLAlchemy `before_flush` listener that populates `row_hash` / `prev_row_hash` on every `AdminAuditLog` row) was registered only by `app/main.py` and `app/worker/celery_app.py` at process boot. Any code path that imports `SessionLocal` directly without also importing `app.main` (operator heredocs in the prod-ops container, one-off scripts, REPL sessions) constructed sessions whose flushes did NOT trigger the chain handler — silently writing audit rows with NULL `row_hash` and NULL `prev_row_hash`, creating forensic holes. Pillar 23 hard-fails on a NULL gap with non-NULL after, so this latent gap would surface as a verification failure on the next post-incident audit row. Fix: install the listener in `app/db/session.py` at module-import time. Every ORM code path imports `SessionLocal` (or `get_db`), so this is the structural choke point — "forgot to install the listener" becomes impossible. The legacy installer calls in `app/main.py` and `app/worker/celery_app.py` remain as defense-in-depth (idempotent: gated by `event.contains(...)`). Pillar 23's error message updated to point at `app.db.session` as the canonical install location. Discovered via the `D-audit-row-3445-hash-backfilled-2026-05-08` incident below. |
| `D-audit-row-3445-hash-backfilled-2026-05-08` | C25 _this commit_ | closed | P23 (audit chain) / DISC-2026-002 (key consolidation) | During Step 29.y Phase E platform-admin key consolidation on 2026-05-08, an ad-hoc Python heredoc executed inside the prod-ops container minted `api_keys` row id=597 (prefix `luc_sk_qmMVl`) and wrote the corresponding `admin_audit_logs` row id=3445 (action=create, resource_type=api_key, resource_pk=597, tenant_id=platform). The heredoc imported `SessionLocal` directly without importing `app.main`, so the `before_flush` listener was never registered for that session and row 3445 was inserted with `row_hash=NULL` and `prev_row_hash=NULL`. Detected on the immediate post-mint consistency probe (Section E showed `row_hash=NULL prev=NULL` while rows 3441–3444 had a continuous chain). Recovery: row 3445 was backfilled by hand by computing `canonical_row_hash(_row_to_dict(row), prev_hash=row_3444.row_hash)` exactly as the listener would have done, then issuing a single raw-SQL UPDATE setting both columns. The backfill is a deterministic recomputation of metadata — no business field of row 3445 was changed; the row_hash recomputed from the row's persisted column values is, by definition, the same hash the listener would have produced if it had been registered at flush time. A meta-audit row 3446 was written via the proper ORM path (with C25's session.py installer ensuring the listener fired) documenting the backfill, action=update, resource_type=api_key, resource_pk=597, note pointing at this drift token and the postmortem. Full incident timeline, IAM gap context, three-failed-mints history, and code/process changes are recorded in `docs/postmortems/2026-05-08-platform-admin-consolidation.md`. Audit chain is fully intact end-to-end (Pillar 23-equivalent walk over rows 1..3446 passes). |
| `D-prod-rds-two-migrations-behind-deployed-code-2026-05-08` | C28 _this commit_ | closed | meta / deploy process / P23 (latent) / Cluster 4 (E-2 latent) | Two alembic migrations sat in-repo on `step-29y-gapfix` for ~14h45m after authoring without being applied to prod RDS, while application code that depends on the schema they introduce was deployed to backend and worker services. Specifically: `c5d8a1e7b3f9` (Cluster 3 / D-8: `admin_audit_logs.row_hash` and `prev_row_hash` NOT NULL — authored 2026-05-07 23:48 UTC at `d413a0b`) and `d8e2c4b1a0f3` (Cluster 4 / E-2: `ux_admin_audit_logs_worker_reject_idem` partial unique index — authored 2026-05-07 23:58 UTC at `ed0e6eb`, redesigned forward-only 2026-05-08 03:34 UTC at `e650e66`/C12). Both sat unapplied while rev29 (deployed earlier) and rev30 (deployed today at 13:55 EDT backend / 14:04 EDT worker) ran code that calls `repo.record(skip_on_conflict=True)` (line 246 of `app/repositories/admin_audit_repository.py`) — the `IntegrityError`-swallowing branch is meaningful only when the partial unique index exists; without the index, a worker-reject duplicate would have raised IntegrityError and bubbled past the swallow, breaking idempotency semantics. Discovered during Step 29.y close-out Phase D when `alembic upgrade head` was run as a sanity check (the working session-summary recorded `head=a1f29c7e4b08` and the operation was expected to be a no-op). The upgrade applied both migrations successfully at 2026-05-08 14:19 EDT. **Why the latent risk did not materialize:** (a) `admin_audit_logs.row_hash` was nullable in the prod schema during the gap window, so the C25-discovered NULL-hash incident on row 3445 (2026-05-08 12:14 EDT) inserted successfully and was hand-backfilled before the NOT NULL constraint applied. If the constraint had been applied first, the heredoc INSERT would have failed with `not-null violation` and the audit row would have been lost entirely (worse forensic outcome than the NULL we recovered). (b) No worker-reject collision occurred during the gap window; the missing partial unique index was never exercised against a colliding pair. **Verification artifacts** (captured 2026-05-08 14:20 EDT): (1) `information_schema.columns` shows `row_hash` and `prev_row_hash` both `is_nullable=NO`. (2) `pg_indexes` shows `ux_admin_audit_logs_worker_reject_idem` as `CREATE UNIQUE INDEX ... ON admin_audit_logs USING btree (action, tenant_id, resource_natural_id) WHERE action LIKE 'worker_%' AND resource_natural_id IS NOT NULL AND created_at >= '2026-05-08 04:00:00+00'`. (3) `alembic_version` = `d8e2c4b1a0f3` confirmed via both `alembic current` and direct DB query. (4) Hash chain rows 3444→3448 walks correctly post-NOT-NULL: row 3445's backfilled hash (`80732546309cd1c4...`) survived the constraint application and chains correctly to row 3446 (`3b61a1650c983208...`) → 3447 (`5f07816f2eefc518...`) → 3448 (`3346e9c96e5f4ded...`). **Process fix landing in C28:** the close-out runbook `docs/runbooks/PROD_ACCESS.md` (to be authored in F.2) will codify "alembic upgrade head against prod RDS is mandatory after every backend image deploy, immediately after rolling deploy completes, before declaring deploy successful — even when the change set appears code-only." The check is cheap (idempotent no-op when at head, sub-second) and structurally prevents this drift class. **Latent prod-risk window:** 2026-05-08 03:34 UTC (when `d8e2c4b1a0f3` redesign C12 landed, last code-deploy-relevant migration commit) to 2026-05-08 18:19 UTC (when migrations applied) = ~14h45m. During this window prod application code called code paths that assumed schema state the prod schema did not have. **Customer impact:** no observed customer-facing impact; no audit failures, no dropped writes, no IntegrityError bubbles observed; chain integrity preserved end-to-end. Recorded for compliance. Full timeline and three-RC analysis (RC-1 deploy process did not gate on alembic head; RC-2 working session-summary asserted head without verification; RC-3 no automated startup-time check compares image alembic heads to RDS `alembic_version`) in `docs/incidents/2026-05-08-prod-rds-migration-gap.md`. |
| `D-no-alembic-head-vs-rds-startup-check-2026-05-08` | _deferred to Step 30_ | open | meta / deploy process | Add a backend startup-time health-check that compares the image's `alembic heads` (computable from the in-image `alembic/versions/` tree) against `SELECT version_num FROM alembic_version` from the configured RDS, and either refuses to serve traffic or emits a CloudWatch alarm on mismatch. This is the structural fix for RC-3 of `D-prod-rds-two-migrations-behind-deployed-code-2026-05-08`. Deferred to Step 30 because it requires implementation work and alarm wiring; the runbook fix in C28 closes the immediate hazard. When closed, the runbook step "alembic upgrade head against prod RDS is mandatory" can be relaxed from a manual checklist item to a verified invariant. |
| `D-prod-ops-role-cannot-list-or-remove-ssm-tags-2026-05-08` | _deferred to Step 30_ | open | meta / IAM | Cosmetic: `luciel-ecs-prod-ops-role` lacks `ssm:ListTagsForResource` and `ssm:RemoveTagsFromResource` on the platform-admin-key parameter. Effect: a leftover `luciel:iam-probe=ok` tag from the 12:27 EDT IAM-propagation probe (during the 2026-05-08 consolidation) cannot be removed by the ops container, and the consistency probe cannot enumerate the parameter's tag set. The tag is harmless (does not affect auth, parameter value, or any reader). Deferred to Step 30 along with the broader prod-ops role review (`D-execution-role-overscoped-ssm-readonly` and the network-architecture cleanup tokens). When closed, the same probe pattern from the postmortem can also be retired. |
| `D-task-def-registry-bloat-2026-05-08` | C29 _this commit_ | closed | meta / operational hygiene | Pre-cleanup the `luciel-cluster` ECS account held 84 ACTIVE task definition revisions across 9 families (luciel-backend rev 1–34, luciel-worker rev 2–14, luciel-prod-ops rev 1–3, luciel-grant-check rev 1–4, luciel-migrate rev 1–14, luciel-mint rev 1–4, luciel-recon rev 1, luciel-smoke rev 1–2, luciel-verify rev 1–12 + 14–17). Only the latest revision of each family is referenced by a running service or by any `aws ecs run-task` invoker in the codebase; the other 75 revisions were inert audit history. Effect: `aws ecs list-task-definitions` was 84 ARNs deep, drowning operators in noise during incident response (surfaced today during Step 29.y close-out when enumerating cluster state for Tier 2 cleanup planning). Each obsolete revision also carried an entry in CloudWatch's task-def cache and a permanent IAM PassRole audit trail entry, neither of which was being read. **Code-reality audit before deregistration** (recorded for future operators applying the same pattern): every family's latest revision was either (a) referenced by a running service (backend\:34, worker\:14), (b) the default in an in-repo run-task invoker like `mint-via-fargate-task.ps1` or `verify-run-task-input.json` (mint\:4, verify\:17, both family-only references that auto-resolve to LATEST), or (c) pinned exactly by a `run-task-*.json` skeleton (smoke\:2 in `run-task-smoke.json`). No code path referenced a non-latest revision; the `luciel-verify:13` mention in `app/verification/__main__.py:67` is a historical comment about Step 29 D→E behavior parity and rev 13 had already been deregistered in a prior cleanup. **Cleanup applied 2026-05-08 14:39–14:48 EDT:** 82 deregister-task-definition calls (47 in Phase 1: backend rev 1–33 + worker rev 2–13 + prod-ops rev 1–2; 35 in Phase 2: grant-check rev 1–3 + migrate rev 1–13 + mint rev 1–3 + smoke rev 1 + verify rev 1–12 + 14–16). All returned `INACTIVE` cleanly. Pattern E preserved: `deregister-task-definition` flips status to INACTIVE; AWS retains the full task-def JSON for 90 days and any rev can be re-registered from history if a rollback is needed. Post-state: exactly 9 ACTIVE task definitions, one per family, all latest, all referenced by running services or run-task invokers in the codebase. Both backend and worker services remained `runningCount=1` throughout; no service disturbance. **Re-registration cost** (for any future emergency rollback to a deregistered rev): one `aws ecs register-task-definition --cli-input-json file://<archived-json>` call against either the in-repo `td-*.json` snapshots (where present) or the AWS-retained inactive registry (queried via `describe-task-definition --task-definition family:rev`). Sub-minute operation; no data path. |
| `D-orphaned-apprunner-ecr-role-2026-05-08` | C29 _this commit_ | closed | meta / IAM | `luciel-apprunner-ecr-role` (created 2026-04-12 18:21 UTC, trust policy `build.apprunner.amazonaws.com`, only attached AWS-managed policy `AWSAppRunnerServicePolicyForECRAccess`, no inline policies, no instance profiles, null description) was a remnant of an abandoned App Runner deployment exploration from before the ECS-only consolidation that landed in Step 27. No active App Runner service exists in account 729005488042 region ca-central-1; no IaC references the role; no `iam:PassRole` action ever fires against it from any current task def or scheduled task. Pre-flight verification confirmed (a) zero inline policies, (b) exactly one attached managed policy (the AWS-managed one), (c) zero instance profiles for the role. Cleanup applied 2026-05-08 14:48 EDT: `iam:DetachRolePolicy` to remove `AWSAppRunnerServicePolicyForECRAccess`, then `iam:DeleteRole`. Post-verify: `aws iam get-role --role-name luciel-apprunner-ecr-role` returns `NoSuchEntity`. Final luciel-* IAM role inventory = 8 (down from 9), all with active production responsibilities: `luciel-ecs-execution-role`, `luciel-ecs-migrate-role`, `luciel-ecs-mint-role`, `luciel-ecs-prod-ops-role`, `luciel-ecs-verify-role`, `luciel-ecs-web-role`, `luciel-ecs-worker-role`, `luciel-mint-operator-role`. Pattern E note: IAM role deletion is NOT reversible by design (delete is permanent, no 90-day retention on the role identity itself). Recreation cost if needed: re-run the equivalent of an `aws iam create-role --role-name luciel-apprunner-ecr-role --assume-role-policy-document ...` call — but the App Runner exploration is not coming back, so this cost is academic. The two App Runner CFN/IaC artifacts that historically referenced this role were deleted in Step 27 (commit history search confirms no current `cfn/*.yaml` or `infrastructure/**/*.tf` references). |
| `D-session-summary-asserted-no-autoscaling-cfn-shows-live-2026-05-08` | C29 _this commit_ | closed | meta / process | The Track 3 close-out session-summary (used to plan Tier 2 cleanup at the start of 2026-05-08 14:00 EDT block) carried a stale assumption that no production autoscaling existed for either the backend or worker service, on the basis of which Step 30 was supposed to receive a forward-fix item "add autoscaling." Tier 2 enumeration via `aws cloudformation list-stacks --region ca-central-1 --query "StackSummaries[?starts_with(StackName, 'luciel-') && StackStatus != 'DELETE_COMPLETE'].[StackName,StackStatus,LastUpdatedTime]"` revealed three live luciel-* CFN stacks, all `CREATE_COMPLETE`: `luciel-prod-alarms` (2026-05-05 20:43 UTC), `luciel-prod-worker-autoscaling` (2026-05-05 20:54 UTC), `luciel-prod-verify-role` (2026-05-05 22:07 UTC). The worker-autoscaling stack has been live since Step 28 Phase 2 Commit 6 (shipped 2026-05-05 ~17:35 EDT per `docs/runbooks/step-28-phase-2-deploy.md` line 890 and `docs/CANONICAL_RECAP.md` v2.0 line 36) with `ScalableTarget` on `service/luciel-cluster/luciel-worker-service`, capacity 1–4 on `ecs:service:DesiredCount`, single CPU `TargetTracking` policy `luciel-worker-cpu-target-tracking` at 60% with cooldowns 60s/300s, AWS service-linked role `AWSServiceRoleForApplicationAutoScaling_ECSService` attached. Direct probe at 2026-05-08 14:42 EDT confirmed all SuspendedState flags are false (DynamicScalingInSuspended=false, DynamicScalingOutSuspended=false, ScheduledScalingSuspended=false). Root cause of the false assumption: the session-summary aggregated cleanup notes from multiple sources without grounding the autoscaling claim against `aws cloudformation list-stacks`; the `D-celery-broker-not-verified-deferring-backlog-autoscaling-2026-05-05` token (which is real and still deferred) was conflated into a broader "no autoscaling exists" claim that was never true for CPU-based worker scaling. **Process fix:** before any Tier 2 / Tier 3 cleanup wave, the close-out runbook (`docs/runbooks/PROD_ACCESS.md` to be authored in F.2) will require an `aws cloudformation list-stacks` + `aws application-autoscaling describe-scalable-targets` + `aws iam list-roles --query "Roles[?starts_with(RoleName, 'luciel-')]"` ground-truth pass to verify state assumptions before any destructive call. **Removed from Step 30 backlog:** the made-up `D-no-autoscaling` Step 30 entry that appeared in earlier in-session planning notes is hereby removed; worker autoscaling is and was correctly shipped. The genuine deferred autoscaling work (`D-celery-broker-not-verified-deferring-backlog-autoscaling-2026-05-05` for worker backlog policy and `D-backend-service-no-autoscaling-2026-05-08` for backend) carry forward as separately-tracked tokens. No production impact. |
| `D-backend-service-no-autoscaling-2026-05-08` | _deferred to Step 30_ | open | meta / capacity planning | `aws application-autoscaling describe-scalable-targets --service-namespace ecs --resource-ids "service/luciel-cluster/luciel-backend-service" --region ca-central-1` returns `{"ScalableTargets": []}` as of 2026-05-08 14:42 EDT — the backend service has no scalable target registered. This was deliberate per the original Step 28 Phase 2 design (per `docs/runbooks/step-28-phase-2-deploy.md` line 1155: "Backend service autoscaling — not in original Commit 6 scope per ALB-front design"); worker autoscaling shipped because async memory-extraction throughput is the variable load, while ALB-fronted backend was assumed to be steady-state. With Step 29.y close-out showing prod traffic is still pre-customer (ops-only), this assumption is fine for now. Defer to Step 30 to author a backend CPU TargetTracking policy (or request-count-per-target via ALB metrics, broker-agnostic) modeled exactly after `cfn/luciel-prod-worker-autoscaling.yaml` once we have customer traffic patterns to size against. The capacity ceiling MaxCapacity for backend should be sized from the same RDS connection-pool ceiling that gates worker scaling (`luciel-rds-connection-count` alarm at 90 connections). No production impact today; recorded so it does not surface as another "why does this not exist" question in a future incident. |
| `D-cluster-task-def-cleanup-runbook-stale-2026-05-08` | C29 _this commit_ | closed | meta / docs | `docs/runbooks/step-29y-prod-cleanup-2026-05-08.md` (the original cleanup runbook authored at the start of the close-out window) included a deregistration directive for `luciel-grant-check:4` — but rev 4 is the latest, currently-runnable revision of that family. Following the runbook verbatim would have wrongly deregistered the active grant-check task def, leaving no runnable rev (and thus blocking the next time grant-check is invoked manually for an SSM permissions probe). Caught during Tier 2 code-reality audit (the family's latest is the only rev referenced anywhere; the runbook was reading the cluster's full revision list, not the current-rev list). The actual T2.2 Phase 2 execution corrected this and kept rev 4 active, deregistering revs 1–3 only. **Process fix:** the runbook is superseded by this commit's drift-token-driven cleanup record — future re-runs of the same Tier 2 pattern should compute "keep latest, deregister all others" programmatically from `aws ecs list-task-definitions --family-prefix <family> --status ACTIVE --sort DESC --max-results 100 | jq -r '.taskDefinitionArns[1:][]'` rather than reading static revision lists out of a runbook that ages between reading and execution. The stale runbook stays in-repo as historical context (and as a worked example of why dynamic computation beats static lists for cleanup operations) but is annotated at the top with a pointer to this token. |

### Carry-forward (deferred, not in scope this step)

| Token | Carry-forward to | Reason |
|---|---|---|
| `D-actor-permissions-storage-format-migration-step-30b-2026-05-07` | Step 30b | Full column rewrite + chain recompute requires prod migration + Pattern N alignment; outside code-only window |
| Cluster 4b items (E-6 / E-12 / E-13) | Step 30b | Prod IaC, cross-region replication, DR runbook; outside code-only window. See `docs/STEP_29Y_DEFERRED.md` |

## Maintenance contract

1. New drift tokens must be appended here in the same commit that introduces them.
2. Status changes (open → closed, etc.) are recorded by editing the row in a new commit; the commit message must reference the token.
3. Tokens are never deleted. Supersession is recorded by setting `Status = superseded-by-<new-token>` and adding a new row for the replacement.
4. Hashes in this file are pointers, not the source of truth. If a hash and `git log` disagree, `git log` wins and this file must be corrected.
