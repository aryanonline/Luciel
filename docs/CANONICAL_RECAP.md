# Luciel — Canonical Recap (Business)

**What this document is:** The single source of truth for what Luciel is, who it serves, what it will and won't do, and how it gets to market. Written so a co-founder, an investor, a senior hire, or a thoughtful customer can read it end-to-end and understand the product without translation.

**What this document is not:** Code architecture, AWS topology, drift register, or commit history. Those live in `ARCHITECTURE.md` and `DRIFTS.md`.

**Maintenance protocol:** Surgical edits only. When a strategic question is answered or a roadmap step lands, update in place. When a decision changes, update in place and log the prior decision in `DRIFTS.md`. No version-history sediment in this doc — the doc reflects current state; history lives in git.

**Status markers used in this document:**
- ✅ Resolved / Closed — the answer is settled and the build matches.
- 🔧 Build-in-progress — designed and described as the target; current implementation is partial, with the gap captured in `DRIFTS.md` and a roadmap step that closes it.
- 📋 Planned — on the roadmap with a defined success criterion; not yet built.
- 🔬 Decision-gate — evidence is being gathered before the answer is committed.

---

## Section 1 — What Luciel is

Luciel is a domain-adaptive, model-agnostic judgment layer.

It exists as a single core intelligence that an Admin can instantiate as one or more Instances to fit their own work. The same Luciel that helps a brokerage owner think about portfolio strategy helps an individual agent think about a single client's needs — same character, same reasoning style, different knowledge and different tools.

Luciel exists to be perceptive, calm, incisive, trustworthy, and unusually good at discovering what someone truly wants beneath what they first say.

---

## Section 2 — The two layers

Luciel has two layers. One is fixed. One is configurable. This is the central design idea of the product.

| Layer | What stays fixed | What changes |
|---|---|---|
| **Soul** | Persona, tone, reasoning philosophy, trust boundaries, conversational style | Very little; only carefully versioned upgrades |
| **Profession** | Nothing universal beyond interface contracts | Domain knowledge, APIs, tool access, workflows, client goals, market rules |

The Soul is what makes a Luciel feel like Luciel regardless of where it's deployed. The Profession is what makes it useful for a specific person, team, or company.

A real-estate Luciel and a legal Luciel are the same Luciel with different Profession layers. A Luciel for an individual agent and a Luciel for a brokerage are the same Luciel with different scope and different tools.

---

## Section 3 — Voice

Luciel's voice is consistent everywhere it shows up:

- Perceptive, not intrusive.
- Confident, not arrogant.
- Elegant, not verbose.
- Curious, not interrogative.
- Strategic, not robotic.

---

## Section 4 — What Luciel will never do

These are non-negotiable. They define the product as much as the features do.

- Pretend certainty Luciel does not have.
- Hide tradeoffs from the user.
- Invent facts from missing data.
- Push someone toward an action that serves the hiring business but not the end user.
- Pressure users emotionally.
- Use perceptiveness as coercion.
- Take consequential action without permission.
- Retain sensitive information without explicit policy approval.
- Reach beyond the scope and tools the deployment was given.

**On "consequential."** A consequential action is one that is irreversible (signing a contract, charging a card, deleting data), high-blast-radius (mass communications, broad data exports), or off-pattern for the user (an unusual category, an unusual amount, an unusual recipient, an unusual time). Routine work — reading a calendar, recording call notes, saving a memory — is not consequential and Luciel does not interrupt the user to confirm it. ✅ The shape of this gate is now enforced server-side: every tool invocation passes through the action-classification gate in `app/policy/action_classification.py` before the tool's `execute()` method runs (Step 30c). Tools declare their tier on the class itself (`declared_tier = ActionTier.ROUTINE | NOTIFY_AND_PROCEED | APPROVAL_REQUIRED`); the fail-closed wrapper routes anything that has not declared a tier to APPROVAL_REQUIRED so a forgotten declaration cannot silently escalate privilege. The off-pattern dimension is named in the design but its detector is deferred until the four-kinds memory architecture is queryable (`DRIFTS.md` `D-context-assembler-thin-2026-05-09`).

---

## Section 5 — How Luciel thinks

Luciel runs the same internal loop every time, no matter the domain:

1. **Perceive** — capture the explicit request, and read the emotional tone underneath it.
2. **Infer** — identify hidden priorities, constraints, and uncertainties.
3. **Verify** — ask one targeted question, or pull one targeted data point, to resolve the most consequential unknown.
4. **Act** — recommend, explain, retrieve, route, or carry out work, depending on what's appropriate. 🔧 Acting beyond conversation (booking, sending, writing to external systems) lands in Step 34; ✅ the action-classification gate that governs it landed in Step 30c (every tool invocation is tiered ROUTINE / NOTIFY_AND_PROCEED / APPROVAL_REQUIRED in `app/tools/broker.py` before the tool runs).
5. **Reflect** — assess confidence and outcome quality, and decide whether the situation has crossed a threshold that requires escalation.

This loop is what makes Luciel feel like judgment instead of search.

---

## Section 6 — Luciel's six components

Every Luciel deployment, regardless of vertical or scope, is built from the same six components. Together they form the operating system the product runs on.

**Persona.** The fixed identity, tone, and behavior doctrine. Versioned rarely. This is the Soul layer in concrete form.

**Runtime.** The execution engine that receives a request, assembles context, runs the reasoning loop, calls tools, and returns a response. Compiles context, enforces policy, controls tool use, logs decisions for later inspection.

**Memory.** Layered, deliberate, modest. Enough to feel continuity, never so much that persistence becomes unbounded or unsafe. Memory comes in four kinds:

- **Session memory** — short-lived working context for the active conversation.
- **User preference memory** — persistent facts about a person's priorities and tastes, when they've allowed it.
- **Domain memory** — structured knowledge patterns for the vertical (real estate, legal, mortgage, etc.).
- **Client operational memory** — business-specific rules, workflows, and exceptions for the deploying organization.

**Tool.** The registry and invocation interface for everything outside Luciel's head — search, calculation, retrieval, external APIs, other Luciels. Loosely coupled by design, so the implementations can change without changing Luciel itself. 🔧 Today the tool surface is LLM-only at the runtime; calendar, CRM, email, and database actions are designed and reserved but land in Step 34.

**Policy.** What Luciel is allowed to do. Governs scope of access, escalation rules, action confirmation, and any client- or domain-specific restrictions.

**Observability.** Logs, decision traces, tool calls, and evaluation metadata. Without this, Luciel cannot be improved systematically — there is no way to know why a response succeeded or failed.

---

## Section 7 — What Luciel is good at

Luciel is built around six explicit cognitive abilities. These are the basis for how the product is prompted, evaluated, marketed, and improved.

**Desire inference.** Identifying what the user actually values beneath surface phrasing. Distinguishing stated wants from real priorities — status versus comfort, budget versus fear, speed versus certainty.

**Context synthesis.** Combining the live conversation, structured records, prior memory, and domain data into one coherent model of the situation. Useful intelligence comes from synthesis, not isolated retrieval.

**Recommendation judgment.** Not listing options — ranking and framing them based on the user's values and tradeoffs. This is the part of the product that encodes domain meaning and client-specific judgment, and it is the part competitors cannot copy quickly.

**Conversational guidance.** Walking a user through uncertainty without making the experience feel like a form. Asking only the questions that meaningfully improve the next decision.

**Trust boundaries.** Stating when an answer is inferred, asking before consequential action, staying within granted tools and permissions, and never claiming knowledge that isn't grounded.

**Escalation.** Knowing when not to finish alone. Handing off when authority, confidence, compliance, or emotional stakes cross a defined threshold.

---

## Section 8 — Recommendation format

Every Luciel recommendation, in any domain, follows the same shape. This is a product contract, not a stylistic preference.

- **What I think suits you best.**
- **Why it fits you.**
- **What tradeoff comes with it.**
- **What I still need to confirm.**

Recommendations should feel like judgment, not search results.

---

## Section 9 — When Luciel escalates

Luciel does not try to finish every task alone. It hands off to a human when:

- Confidence is low and the downside is meaningful.
- The conversation crosses a legal, financial, or medical liability boundary.
- There are strong signs of emotional distress or conflict.
- A high-value moment arrives where a human relationship matters more than a fast answer.

---

## Section 10 — What stays fixed, what changes

| System part | Fixed | Configurable |
|---|---|---|
| Identity and tone | Yes — Luciel stays Luciel | Minor client guardrails only |
| Ethical boundaries | Yes | Scope-specific compliance additions |
| Reasoning philosophy | Yes | Scope-specific heuristics layered on top |
| Tools and APIs | No | Yes — per scope and vertical |
| Knowledge and ontology | No | Yes — per vertical and scope |
| Workflow logic | Partly | Yes — based on the deploying organization's operations |

The fixed parts are what make every Luciel feel like the same intelligence. The configurable parts are what make each one useful for a specific person, team, or company.

---

## Section 11 — Strategic questions

These are the seven questions that shape Luciel as a product. Each one is a real product decision, captured in the language of the customer scenario it solves for. The answers are settled. The success criteria are how we'll know — from a customer's experience, not from a test suite — that the answer actually works.

*Note on the renumbering (2026-05-22, Arc 9):* The prior `Q6 — What happens when scope-level personnel get promoted, demoted, or leave entirely?` row collapsed into **Q1** (the data-belongs-to-the-Admin and rotation-on-role-change shape) and **§14** (the role-binding-by-tier entitlement row). Channels was previously Q7 and is now **Q6**; cross-channel was previously Q8 and is now **Q7**. The collapse is recorded in DRIFTS §3 `D-q6-personnel-rotation-collapsed-into-q1-and-section-14-2026-05-22` and in Arc 9 work-unit WU-9.1.

| Question | Answer | How we'll know we're successful | Status |
|---|---|---|---|
| **Q1** — If Luciel is truly domain-agnostic, then once an Admin has provisioned their account — by Free self-signup, Pro self-serve Stripe checkout, or Enterprise sales-mediated onboarding — they should be able to choose for themselves how to branch their Luciel deployment: one Instance for personal use, or several Instances for different lines of work, or — at the Enterprise tier — composed and knowledge-shared Instances across an organisation. The Admin is the single billing entity and the single permissions root; below them sit one or more Instances, and below each Instance sit zero or more Leads (the human end-customers each Instance serves). Each level can only manage what's at or below its own scope. The Admin should also be able to upgrade or downgrade between tiers without having to restart their deployment or lose the work they've already done. Because data lives with the Admin scope (not with any individual user), promotion, demotion, or departure of scope-level personnel is an operational property of this same shape: access changes cleanly, mandatory key rotation runs on role change, and the audit log shows who did what when — the Instances and their knowledge are owned by the Admin, not by the user who configured them. | Single Admin scope at the top; the caller's scope dictates what they can create. The Admin account is the onboarding atom (one account per Admin, provisioned through the tier-appropriate path — Free self-signup, Pro Stripe Checkout, or Enterprise sales-mediated), branched downward by Admin choice into Instances (and, at Enterprise, into composed Instance groups). Tier moves are first-class: an Admin can upgrade or downgrade between Free / Pro / Enterprise without recreating Instances, re-ingesting knowledge, or losing memories — the operational record is in §12 Step 35 (re-parenting + tier moves) and the entitlement deltas are authored in §14. Operational keys (rotated under the role-binding-by-tier row in §14, runbook in §12 Step 32a) are issued **against** an existing Admin account; key issuance is not itself the onboarding event. | A new Admin completes signup (Free), checkout (Pro), or sales-mediated provisioning (Enterprise), and within an hour has chosen for themselves whether to run a single Instance, several Instances under their own Admin scope, or — at Enterprise — a composed and knowledge-shared deployment with optional delegated sub-admins (SSO-mapped, gated by `admin_tier_overrides.delegated_admin_enabled`). No founder is on the call. An Instance Lead can manage only the Instance they own. No one can manage anything above their own level. | ✅ Resolved. Architecture: ARCHITECTURE §4.1 (Admin scope as billing boundary, with Free exception), §4.7 (three-layer scope enforcement), §3.3 step 3–4 (auth + scope policy check). Operational record: §12 baseline Steps 1–10. The flat `Admin → Instance(s) → Lead(s)` model is the canonical shape at every tier; the legacy `Tenant → Domain → Agent → LucielInstance` four-level hierarchy is retired and lives only in the audit chain (see DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22`). The current tier shape is **Free / Pro / Enterprise** (see §11.7 for public-positioning copy and §14 for the entitlement matrix); the legacy four-tier shape (Solo / Team / Company / Enterprise) is retired and preserved in the audit chain at DRIFTS §3 same drift. |
| **Q2** — An Admin should be able to see the Luciels deployed under their scope, what each one is doing, and how much business value each is generating. A Lead should see the same view scoped to their own Instance. The dashboard is the single surface where this rollup is visible. | Tier-adaptive dashboard rendering over a fixed three-view backend (Admin rollup / Instance-group / Single-instance), driven by usage data, configurable value metrics, and workflow outcomes. | An Enterprise Admin opens the dashboard once a week and can answer in under a minute: which Instance is getting the most value out of Luciel, which Instances are underused, and where to invest more attention; metering and overage are surfaced inline. A Pro Admin sees the Admin rollup + Instance-group view (composition is visible). A Free Admin sees only the Single-instance view. An Instance Lead sees only their own Instance at every tier. Each answer arrives in under a minute. | ✅ Implemented at the backend (Step 31, 2026-05-12). Three scope-bound dashboard views live under three-layer scope enforcement; underlying routes `/api/v1/dashboard/{admin,instance_group,instance}` (renamed from `{tenant,domain,instance}` at Arc 5) carry the rollups. The tier→view mapping renders Free=Single-instance only, Pro=Admin rollup + Instance-group, Enterprise=all three plus metering / overage. Architecture: ARCHITECTURE §3.2.12. Operational record: §12 Step 31 row below. Closing tag: `step-31-dashboards-validation-gate-complete`. **⚠ Prod-deploy gap** open — code on `main`, not yet on prod RDS/ECS; see DRIFTS §3 `D-step-24-5c-and-31-schema-and-code-undeployed-to-prod-2026-05-12`. **Pillar-4d / 4e verify deferred** to Step 31.1 follow-up (post Step 32 rotation); see DRIFTS §3 `D-pillar-4d-audit-row-field-verify-deferred-2026-05-13` and `D-pillar-4e-cross-table-row-verify-deferred-2026-05-13`. Closing tag stands. The Step 32 wave-2 tier-adaptive `/app` shell selects which combination renders. The Enterprise metering / overage surface is open as DRIFTS §3 `D-enterprise-metering-not-implemented-2026-05-22`. |
| **Q3** — Luciel is being designed to reason well and reduce hallucination. Today we use only a vector database, but a combination of vector and graph databases could improve grounding. The open question is how to decide what kind of information belongs in each. | Yes, hybrid retrieval — relational graph queries first, opt-in per domain via configuration; graduate to a dedicated graph database once scale demands. | When a Luciel answers a question that depends on relationships ("which of my agents have worked with clients in this neighborhood and price band?"), the answer is correct, complete, and arrives in the same conversation — not pieced together by hand from three different searches. Hallucination rate on relationship questions is measurably lower than vector-only retrieval. | 🔬 Decision-gate (Step 37). Architecture: ARCHITECTURE §3.2.6 (memory tier). Operational record: §12 Step 37 row below. |
| **Q4** — A Pro or Enterprise Admin has deployed multiple Instances under their own Admin scope. Because all of them are theirs, they should be able to communicate, know about each other, and work together — so the customer gets one coordinated outcome instead of several disconnected ones. | Inter-instance composition via explicit grants per direction (`instance_composition_grants`), audited per call (hash-chained `AdminAuditLog` row), depth-bounded per tier. Composition is the organizing primitive within an Admin scope; cross-Admin composition is permanently forbidden at every tier. | A Pro Admin with three Instances (a listings Instance, a marketing Instance, a client-followup Instance) asks one question and gets one answer that draws on all three — without the customer having to know which Instance does what, and without any Instance reaching outside its lane. An Enterprise Admin extends the same shape with knowledge-share grants and deeper composition. A Free Admin cannot compose at all. | 📋 Planned (Step 36, after evaluation framework). Architecture: ARCHITECTURE §3.2.4 (background worker tier — coordinator surface), §3.3 step 7–8 (scoped tool invocation + action classification). Operational record: §12 Step 36 row below. Composition depth by tier: **Free: 0** (no composition — enforces the Free→Pro upgrade wall); **Pro: 2** (Instances compose within Admin scope up to two hops, the load-bearing affordance that distinguishes Pro from Free); **Enterprise: unlimited within reason** (subject to `admin_tier_overrides` for negotiated bounds). See §14 entitlement matrix and DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22`. |
| **Q5** — If a Pro Admin (Sarah) has been using Luciel as a solo customer, and her organisation later signs up for Enterprise, her existing Instances and the knowledge inside them should carry forward into the organisation's Enterprise Admin rather than being abandoned or recreated by hand. And, separately, when Sarah herself decides to move up or down between tiers on her own Admin, she should be able to do so easily — without recreating Instances, re-ingesting knowledge, or losing memories. | Email-stable user identity; an **Admin→Admin re-parenting** flow that moves Instances, knowledge, memories, and sessions from Sarah's Pro (or Free) Admin into the organisation's Enterprise Admin in a single transaction. Pro-rated credit on the Pro subscription; the Enterprise platform fee absorbs the migrated capacity. **Same-Admin tier moves** (Free↔Pro↔Enterprise on the same Admin row) use the same primitive in degenerate form: the Admin row stays put, the tier column flips, entitlement deltas (§14) apply on the next read, and Stripe prorates the subscription — no data motion, no re-ingestion, no founder involvement. | Sarah has been on Pro for six months. Her organisation signs Enterprise. With one click on Sarah's end and one approval on the organisation Admin's end, Sarah's Instances, their conversation history, ingested knowledge, and configured Luciels all carry forward into the Enterprise Admin scope. Sarah doesn't lose anything. The organisation gets the benefit of her six months. **Zero founder involvement** on the re-parent. Separately: when Sarah upgrades from Free to Pro (or downgrades from Pro to Free) on her own Admin, she clicks one button in the billing portal, Stripe charges or credits her on the next invoice, and within seconds her dashboard reflects the new entitlement set — her Instances, knowledge, memories, and sessions are untouched. | 📋 Planned (Step 38 for cross-Admin re-parenting; Step 35 for same-Admin tier moves). Architecture: ARCHITECTURE §4.5 (cascade-correct departure — re-parenting is the inverse shape), §4.1 (Admin scope as billing boundary), §4.7 (three-layer scope enforcement — unchanged across tier moves). Operational record: §12 Step 35 (re-parenting + same-Admin tier moves) + Step 38 (cross-scope federation) rows below. Re-parenting is one-directional under the three-tier shape: Free→Pro and Pro→Enterprise. Same-Admin tier moves are bidirectional (Free↔Pro↔Enterprise). The intermediate Team-tier landing (which existed in the legacy four-tier shape) is no longer a way-station; Pro→Enterprise is the standard small-team adoption shape. See §14 entitlement matrix and DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22`, `D-same-admin-tier-transition-doctrine-hole-2026-05-22`. |
| **Q6** — Luciel is domain-agnostic, and any Admin can create their own Instances and ingest their own knowledge. Depending on the deployment, an Instance might need many forms of communication — SMS, voice, email, chat widget, and others. An Instance may have access to many tools, including other Instances. The Instance needs to know how to deliver business outcomes across all of those channels. | A channel adapter framework. Inbound webhooks and outbound tool registrations, all bounded by the same scope policy. Channels emerge from configuration, not from a separate product per channel. | An Instance running on a Pro Admin can answer a Lead over the widget today, the same Instance answers the same Lead over voice tomorrow (post Step 34a), and the conversation continues — without the customer having to re-introduce themselves. | 📋 Candidate (Step 34a). Architecture: ARCHITECTURE §3.2.1 (public endpoint), §3.2.9 (integrations today), §3.3 step 1 (channel ingest). Today's reachable channels: widget (Step 30b, Step 30d) + programmatic API. Operational record: §12 Step 30b / Step 30d / Step 34a rows below. Voice / SMS / email channels are committed as roadmap across **all three tiers (Free / Pro / Enterprise)** — the channel framework is a tier-orthogonal capability gated only on Step 34a's adapter framework being reachable end-to-end. The corresponding marketing-site promise on Pricing.tsx is therefore truthful as a roadmap commitment but not yet a live-today entitlement; the live-today vs roadmap split is authored in §14's entitlement matrix. See DRIFTS §3 `D-channels-promised-not-built-multi-tier-2026-05-20`. |
| **Q7** — If a Luciel has access to multiple channels — phone, chat widget, email — how does it manage cross-channel conversations? What happens when someone is chatting on the widget and suddenly switches to phone? | Conversation grouping linked across sessions: a `conversation_id` on every session points at a `conversations` row that holds the durable thread; sessions remain the atomic auditable unit, never merged. A cross-session retriever surfaces recent messages from sibling sessions under the same `conversation_id`, scoped to the same Admin (and Instance, where the policy demands instance-isolation) so an Instance can never read across a scope boundary. Phone numbers and emails are recorded as rows in a separate `identity_claims` table (orthogonal to scope, the same way `users` is), each linked to a `users.id`; in v1 a claim is asserted by the ingress adapter that consumes the channel (the phone gateway swears the call came from a particular number; the widget swears it came from a particular logged-in user) and trusted within the issuing Admin scope. End-user-driven verification (email-confirm link, SMS code, SSO subject match) lands with Step 34a, when the adapters exist to consume it. | A prospect chats with a Luciel on a company's website on Monday, calls the company's Luciel-answered phone line on Wednesday, and the Luciel picks up where the conversation left off — without the prospect having to re-introduce themselves or repeat their context. The handoff feels human. | ✅ Implemented (Step 24.5c, 2026-05-11). Widget + programmatic API legs proven against live code; voice/SMS/email legs inherit the same primitives and reach end-to-end with Step 34a's adapter framework. Architecture: ARCHITECTURE §3.2.11. Operational record: §12 Step 24.5c row below. Closing tag: `step-24-5c-cross-channel-identity-complete`. **⚠ Prod-deploy gap** open — migration `3dbbc70d0105` and dependent code on `main`, not yet on prod RDS/ECS; see DRIFTS §3 `D-step-24-5c-and-31-schema-and-code-undeployed-to-prod-2026-05-12`. |

### §11.7 — Public tier positioning (Arc 4 tier-shape revision → Arc 5 Commit 2 Option A revision, 2026-05-23)

This subsection holds the customer-facing copy that the Pricing surface (`Luciel-Website/src/pages/Pricing.tsx`) renders for each tier. The copy is the buyer's first contact with the live billing claims; it must stay in lockstep with the entitlement matrix in §14, the locked numeric source `arc5-out/A-arc5-arc4-plan-defects.md` §6.5, and the policy module at `app/policy/entitlements.py`. Drift between this subsection and any of those is a P0 truth defect.

**Free** — *For evaluators and curious individuals*

> Try the same Luciel that powers paid Pro and Enterprise deployments. One Instance, **100 leads per month**, base model, 30-day lead retention, **30 rpm API access with one embed key**. No credit card required. Upgrade to Pro any time without losing your work.

**Pro** — *For solo operators and small teams*

> Everything in Free, plus:
> - **Ten Instances** — run a listings Luciel, a marketing Luciel, a client-followup Luciel, and seven more in parallel
> - **5,000 leads / month included** — headroom for a working real-estate practice with margin to grow
> - **Mid-tier model + occasional top-tier bursts** — the model your daily work needs
> - **Composition within your Admin** — your Instances talk to each other, coordinate, hand off context
> - **300 rpm API access with up to 10 embed keys** — integrate Luciel with your existing tools
> - **25 admin-team seats** — invite your full ops team to the dashboard
> - **Custom domain (1 widget CNAME), email + chat widget channels**
> - **48-hour email support**
>
> **Pricing:** Flat monthly via Stripe. (Exact dollar amount carried in Stripe SKU metadata; CANONICAL doctrine does not pin the price-list value because Stripe is the source of truth for live amounts. Stripe SKU plan documented at `arc4-out/A-tenancy-collapse-arc-record.md` §6.)

**Enterprise** — *For teams running real estate operations at scale*

> Everything in Pro, plus:
> - **Unlimited Instances** — spin up dedicated Luciels per property, per market, per team
> - **Unlimited leads** — included volume scales with your contract; usage-based pricing beyond
> - **Premium models** — access to frontier LLMs and custom fine-tunes
> - **Composition + knowledge-share across your Admin**
> - **3,000 rpm API default (contract may negotiate higher), unlimited embed keys, unlimited widget CNAMEs**
> - **Unlimited admin-team seats**
> - **SSO, custom SLA (24h email + dedicated CSM), 7-year audit retention (or contract), delegated-admin role for sub-admins**
>
> **Pricing:** Custom annual contract starting at `$24,000/year` (Arc 6 locked value — the published floor; actual contract sized by sales on first deal-shape conversation and pinned to the live Stripe contract terms per `arc4-out/A-tenancy-collapse-arc-record.md` §6 and reasoning at `arc6-out/B-arc6-numeric-lock.md` §2). Hybrid billing: platform fee + included usage + overage; committed-use discount available. [Talk to sales](mailto:enterprise@vantagemind.ai)

**The "starting at" floor is load-bearing.** A pricing page that says "Contact us" without an anchor reads as a black box to procurement and slows enterprise deals. The floor is a real number that tells finance teams the order-of-magnitude commitment before they invest in a sales conversation. The committed-use-discount lever and overage rate stay in the sales conversation, not on the page.

**What is deliberately not on the Pricing surface today:**

- A live-today contract size for Enterprise. The published `$24,000/year` floor is the order-of-magnitude anchor (Arc 6 locked per `arc6-out/B-arc6-numeric-lock.md` §2); the actual contract size is set by the first enterprise deal-shape conversation, not by the doc layer.
- A Team tier. Eliminated in this revision; if Team-shaped customer profiles emerge later, the Pricing surface adds a fourth card between Pro and Enterprise. The legacy Team tier survives in the audit chain at DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22` and in git at the Arc 4 v1 commits (`66f6528` / `8c3e0b7`).
- A free-trial banner. Free is the trial — no time limit, no expiry, no payment-method-on-file requirement. The standard SaaS pattern of "time-limited Pro trial" is rejected for the same reason "14-day free trial" was killed at Step 30a.6: a customer who can't convert in 14 days is a customer we're chasing out of the funnel.

**Cross-references:**
- Entitlement matrix v2 (engineering-facing): §14 below.
- Engineering spec: `arc4-out/A-tier-matrix-detail.md` (revised in this Arc 4 tier-shape revision pass).
- Stripe SKU plan: `arc4-out/A-tenancy-collapse-arc-record.md` §6.
- Free-tier abuse controls: DRIFTS §3 `D-free-tier-captcha-missing-2026-05-22`.
- Enterprise metering gap: DRIFTS §3 `D-enterprise-metering-not-implemented-2026-05-22`.

---

## Section 12 — Roadmap

The path from where Luciel is today to a fully realized version of the product. Every step is described in plain language, with success measured by what the customer or the founder can observe — not by what's in the test suite.

**Row ordering note:** Rows are sorted by step number (24.5c first as the lowest minor-step value, then 28 → 38 with sub-step suffixes ordered naturally inside each major step). This is presentation order for read-cold-ness, not execution sequence — for the planned-next sequence (which step lands before which), see §13.1 T-scenarios and the upstream-dependency framing inside each step's Status cell.

| Category | Step | Description | How we'll know we're successful | Status |
|---|---|---|---|---|
| Identity | **24.5c** | Cross-channel identity and conversation continuity. | A user moving between channels (widget, phone, email) is recognized as the same person, and the conversation continues without reset. The v1 build proof uses the two reachable channels today — the embeddable chat widget and the programmatic API — exchanging messages on two different sessions under the same `conversation_id`, joined by one `identity_claims` row, with the cross-session retriever surfacing the sibling session's recent turns. The voice/SMS/email legs of the literal T8 demonstration (widget Monday → phone Wednesday) inherit the same primitives but require Step 34a's channel adapter framework to be reachable end-to-end, and stay scoped there per the T8 footnote in §13.1. | ✅ Implemented (2026-05-11) across five sub-branches (PRs #24–#28). Three primitives — `conversations` table, `conversation_id` FK on `sessions` (session-linking, not session-merging), `identity_claims` table — plus a `CrossSessionRetriever` sibling to the per-session retriever, all under the same three-layer scope enforcement. Architecture: ARCHITECTURE §3.2.11, §3.3 step 5, §4.9. Closing tag: `step-24-5c-cross-channel-identity-complete` (on doc-truthing commit, per the Step 30c `99c6eb5` precedent). Live e2e: `tests/e2e/step_24_5c_live_e2e.py`. **⚠ Prod-deploy gap** open — migration `3dbbc70d0105` and dependent code on `main`, not yet on prod RDS/ECS; see DRIFTS §3 `D-step-24-5c-and-31-schema-and-code-undeployed-to-prod-2026-05-12`. Deliberate v1 non-goals: end-user claim verification (Step 34a), cross-tenant identity federation (Step 38), per-user cross-channel inbox view (Step 31 dashboards). Answers Q7 — the cross-channel continuity question (renumbered from Q8 under WU-9.1). |
| Hardening | **28** | Operational maturity sprint — security, compliance, observability, and cleanliness, in four phases. | Luciel can stand up to a real brokerage's due-diligence questions about how their data is handled, who has access, and what happens if something goes wrong. Every answer comes with evidence, not assertion. | 🔧 Phase 1–3 complete; Phase 4 in progress under **Arc 8** (`arc8-out/A-arc8-security-hardening-arc-record.md`) with five work-units LANDED (WU-1 misfiled gating drift closure; WU-2 worker-not-root + BuildKit hardening; WU-3 build-SHA observability; WU-4 drift-stanza closures; WU-5 free-tier captcha infra prep) and three work-units PENDING (WU-6 SES feedback/suppression/IAM/sandbox-exit cohort; WU-7 free-tier captcha end-to-end closure; WU-8 Enterprise metering hook per ARCHITECTURE §3.2.14). Closing tag `arc-8-security-hardening-complete` cuts at WU-8 close. Hardens Q1's rotation-and-departure leg (rotation foundation — Pattern E for secrets; the role-binding-by-tier row in §14 carries the entitlement shape) and the operational backbone for Q2 / Q7. Architecture: ARCHITECTURE §3.2.7, §3.2.8, §3.2.10, §3.2.14, §4.6, §4.7. |
| Testing | **29** | Automated verification suite that re-runs against every change and proves the platform is still healthy. | Before any change ships to a real customer, an automated check confirms that all 25 platform guarantees still hold. We never ship a regression by accident. | ✅ Closed (25/25 verification passing). Validates the scope-discipline pillar that Q1 / Q2 / Q7 all rest on (including Q1's rotation-and-departure leg). Architecture: ARCHITECTURE §4.7. |
| Billing | **30a** | Subscription billing — sign-up, payment, plan changes, cancellation — integrated with our company website. | A new individual customer can find Luciel on our website, sign up, pay, start using their Luciel, change plans, and cancel — all without anyone from our team being involved. | ✅ Closed (2026-05-13). v1 Individual self-serve flow: Pricing CTA → `/signup` → `POST /api/v1/billing/checkout` → Stripe-hosted Checkout (CAD-only, Stripe Tax) → `checkout.session.completed` webhook → `OnboardingService.onboard_tenant` + `subscriptions` row + audit-row (Invariant 4) → magic-link email → cookied `/account/billing` (Stripe Customer Portal). Cancellation flows `customer.subscription.deleted` → `AdminService.deactivate_tenant_with_cascade` (Pattern E). Webhook fail-closed signature-verified, idempotent via `subscriptions.last_event_id`. Closing tag: `step-30a-subscription-billing-complete`. Architecture: ARCHITECTURE §3.2.9, §3.2.13. **Stripe credentials wiring + live $30 CAD activation completed at Step 30a.2-pilot GATE 3** (2026-05-15) — see DRIFTS §3 `~~D-stripe-live-account-not-yet-activated-2026-05-13~~` and `~~D-stripe-credentials-never-wired-to-prod-backend-2026-05-14~~` for the closure stanzas. Two v1 carve-outs picked up downstream: `~~D-billing-team-company-not-self-serve-2026-05-13~~` (closed at Step 30a.1 / superseded by Arc 4 tier-shape revision), `D-magic-link-auth-cookie-session-2026-05-13` (closed at Step 30a.3 password auth). Operationalises Q5 self-serve leg + the §4.1 billing-boundary primitive. |
| Billing | **30a.1** | _Current-truth (2026-05-22-late tier-shape revision): six-SKU Individual/Team/Company surface **fully retired** by Free/Pro/Enterprise (Enterprise sales-ops provisioned). Pre-mint primitive (`TierProvisioningService`), entitlement-cap primitive (`DOMAIN_COUNT_CAP_BY_TIER`, `AdminService._enforce_tier_scope`), tier↔scope mapping, and invite primitive survive intact and re-target Free/Pro/Enterprise at Arc 5/6. Audit chain: `D-tenancy-collapse-admin-instance-lead-2026-05-22`._ ~~Tiered self-serve — extend Step 30a to Team/Company, add annual cadence, pre-mint scope-bearing rows at signup.~~ | A new Team customer chooses monthly or annual, pays, sees Team tab with invite-teammate + per-tier instance cap. (Historical 4-tier acceptance; current truth: Free/Pro/Enterprise.) | ✅ Closed (2026-05-13) under 4-tier shape; **retired by Arc 4 tier-shape revision**, primitives carry to Free/Pro/Enterprise at Arc 5/6. Six-SKU shipped: Individual $30/$300, Team $300/$3,000, Company $2,000/$20,000; annual = 10× monthly. Pre-mint via `TierProvisioningService.pre_mint_for_tier`; scope enforcement via `AdminService._enforce_tier_scope`. Alembic `c2a1b9f30e15` (down_revision `b8e74a3c1d52`) added `subscriptions.billing_cadence` + `subscriptions.instance_count_cap`. Backend PR [#49](https://github.com/aryanonline/Luciel/pull/49); website PR [#3](https://github.com/aryanonline/Luciel-Website/pull/3). Closing tag: `step-30a-1-tiered-self-serve-complete`. ARCH §3.2.13. Closes `~~D-billing-team-company-not-self-serve-2026-05-13~~`. Derivative: `D-tier-scope-mapping-service-layer-only-2026-05-13`, `~~D-vantagemind-dns-cloudfront-mismatch-2026-05-13~~`, `~~D-stripe-live-account-not-yet-activated-2026-05-13~~`. Operationalises Q5. |
| Billing | **30a.2** | Paid-intro trial + cancellation-cascade completion + retention purge worker. Locks the uniform-across-tier-and-cadence trial policy, closes the cascade gap Step 30a's cancel webhook needed, lands the Celery beat retention worker. | First-time customer pays $100 CAD intro at signup → 90d before recurring price kicks in (uniform every tier/cadence); returning customer (case-insensitive email) pays plan rate with no second intro. Cancelling customer: every scope-bearing row flipped soft-deleted in one transaction; 90d later tenant hard-deleted by Celery beat at 08:00 UTC nightly, AdminAuditLog preserved. | ✅ Code-complete on prod (2026-05-14, `luciel-backend:45` + `luciel-worker:25`, Alembic head `dfea1a04e037`). Stripe-side surface (7 live Prices + 7 SSM puts) closed at Step 30a.2-pilot GATE 3. ARCH §3.2.13 (intro_fee + 90d trial mechanics), §3.2.4 (retention purge worker), §4.4 (soft-delete cascade). Closed drifts: DRIFTS §5 `~~D-trial-policy-mixed-per-tier-2026-05-14~~`, `~~D-cancellation-cascade-incomplete-conversations-claims-2026-05-14~~`, `~~D-retention-purge-worker-missing-2026-05-09~~`, `~~D-stripe-credentials-never-wired-to-prod-backend-2026-05-14~~`, `~~D-stripe-live-account-not-yet-activated-2026-05-13~~`. Future-debt: `D-celery-beat-single-replica-coupling-2026-05-14`. Closing tag: `step-30a-2-trial-and-purge-complete`. |
| Billing | **30a.2-pilot** | Step 30a.2 closing sub-step — `POST /pilot-refund` route, website intro-offer surface, Stripe-live activation (GATE 3), live $100 CAD purchase-and-refund smoke. Commits 3a–3j. | Real human lands on `vantagemind.ai/pricing` → Start 90-day pilot → Stripe Checkout $100 CAD live → SES magic-link → `/account/billing` → Refund → $100 CAD returned within 5–7 days → sub canceled + tenant deactivated + courtesy email. Five-leg verify: Stripe Dashboard; DB audit-chain row_hash continuity; inbox; CloudWatch `[pilot-refund-email]`; sub+tenant row state. | ✅ Closed (2026-05-16). Prod `luciel-backend:59`, image `step30a2-pilot-43b2614-r1` (Commit 3j `43b2614`). GATE 3 Stripe live 2026-05-15: intro Price in `acct_1TX2BmRytQVRVXw7`, 9 SSM puts under `/luciel/production/`, `STRIPE_*` wired into task-def `:46+`. SES out of sandbox 2026-05-15, `vantagemind.ai` verified, `LucielSESSendEmail` on `luciel-ecs-web-role`. **Live evidence:** first $100 CAD `pyr_1TXXwHRytQVRVXw7BGiHdxiJ` / charge `py_3TXUMwRytQVRVXw70A1iOPs5`, sub `sub_1TXUN1RytQVRVXw7wmsfATXJ` refunded end-to-end — DB audit rows 4234–4238 row_hash continuity intact, tenant_config 313 deactivated 02:26:52, sub canceled 02:26:56. Commit chain 3a–3j ends `43b2614`. Closing tag: `step-30a-2-pilot-complete`. ARCH §3.2.13. Closes `~~D-pilot-refund-endpoint-and-website-surface-2026-05-15~~`, `~~D-pilot-refund-customer-email-missing-2026-05-15~~`, `~~D-stripe-event-dict-conversion-python314-2026-05-15~~`, `~~D-stripe-sdk-major-pin-2026-05-15~~`, `~~D-stripe-webhook-checkout-vs-subscription-field-source-2026-05-15~~`, `~~D-pilot-is-pilot-metadata-driven-2026-05-15~~`, `~~D-pilot-refund-write-path-trial-end-asymmetry-2026-05-15~~`, `~~D-stripe-basil-2025-invoice-charge-traversal-2026-05-15~~`, `~~D-stripe-credentials-never-wired-to-prod-backend-2026-05-14~~`, `~~D-stripe-live-account-not-yet-activated-2026-05-13~~`. Opens: `D-pilot-live-evidence-step-21-redo-and-step-23-deferred-2026-05-16`, `D-pilot-account-billing-no-subscription-on-file-2026-05-16`, `D-ecs-exec-powershell-python-c-quoting-fragile-2026-05-16`. |
| Billing | **30a.3** | Password authentication mandatory at every signup across every tier; magic-link demoted to recovery + invite-acceptance only. Lands `users.password_hash`, `/api/v1/auth/login` (mints same `luciel_session` cookie), `/api/v1/auth/set-password` (first invite-acceptance + self-serve reset), `/forgot-password` (re-uses magic-link mint). **Stripe Checkout → first-login rewritten under Free/Pro/Enterprise:** Pro at $100 (Free $0 no Stripe row, Enterprise sales-ops provisioned) sets password at registration; success page lands `/auth/set-password?token=<bootstrap_magic_token>`, POSTs to `/api/v1/auth/set-password`, then cookie mints + `/app` redirect. Magic-link is bootstrap + recovery only, **not** daily-login. Same JWT cookie shape + Step 31.2 middleware. | Returning customer opens `/login`, types email+password, lands `/app` cookied — no inbox round-trip. New customer post-Checkout lands `/auth/set-password?token=…`, sets password, lands `/app` cookied — one inbox round-trip ever. Forgot-password: type email, receive magic-link, click, set new password, land `/app`. Invitee clicks invite link, sets password, lands `/app`. **Zero founder involvement.** | ✅ Closed (2026-05-16). Password auth is the daily-login primitive across all tiers via the Option B welcome-email mechanic; magic-link survives only as bootstrap + recovery. Live $100 paid signup completed end-to-end in incognito on `vantagemind.ai` (2026-05-16 ≈21:06 EDT). Closing tag: `step-30a-3-password-auth-magic-link-fallback-complete`. ARCH §3.2.13 (Option B welcome-email mechanic). Closes `~~D-magic-link-only-auth-no-password-fallback-2026-05-16~~` (full closure stanza in DRIFTS §5). Unblocks Step 30a.4 / 30a.5. Operationalises Q1 self-serve daily-login leg. |
| Billing | **30a.4** | _Current-truth (2026-05-22-late tier-shape revision): teammate-invite surface is now the Pro-tier seat-invite path (3 seats per Pro Admin); Team-tier framing retired. Invite primitive survives intact, re-targets Pro Admin seats at Arc 5 / Arc 6._ ~~Team self-serve completion — invite-teammate UI, `POST /api/v1/admin/invites`, `/app/team`, SES single-use HS256 7-day token landing `/app/invite/<token>`.~~ | A Team customer paid $300 intro → opens `/app` → Invite teammate → types 3 emails → 3 invites via SES. Each teammate clicks, sets password, lands `/app` with own agent. **Zero founder involvement.** (Historical 4-tier acceptance; current truth: Pro-Admin seat surface.) | ✅ Closed (2026-05-17) under 4-tier shape; **retired by Arc 4 tier-shape revision**, invite primitive carries to Pro-Admin seat at Arc 5/6. 28 contract tests + dev-Postgres e2e + 33/33 website green. 4 cookie-gated routes (`POST/GET /admin/invites`, `POST .../resend`, `.../revoke`). `UserInvite` + Alembic `e7b2c9d4a18f` (descends `a3c1f08b9d42`; `token_jti` UNIQUE, `purpose`/`status` enums, 7d expires, 24h JWT via `mint_set_password_token`). `InviteService` + `UserInviteRepository`; `USER_INVITED`/`INVITE_REDEEMED`/`INVITE_RESENT`/`INVITE_REVOKED` audit. `/auth/set-password` invite branch provisions Agent + ScopeAssignment. Closing tag: `step-30a-4-team-invite-ui-complete` (commit C7); **corrected** `step-30a-4-team-invite-ui-corrected` re-cut 2026-05-17 with D1 `8019866` + D2 `6cb8d4a` (Alembic `b4d8a2e7c1f3`) + E1 `4a650c5`/`0818b9e` (CORS widen `app/main.py:133`). Closes `~~D-team-self-serve-incomplete-invite-ui-missing-2026-05-16~~`, `~~D-step-30a-owner-scopeassignment-missing-self-serve-checkout-2026-05-17~~`, `~~D-cors-delete-method-blocked-2026-05-17~~`. Opens: `D-set-password-token-logged-plaintext-2026-05-17`, `D-welcome-email-subject-mojibake-2026-05-17`, `D-step-30a-4-live-300-paid-evidence-pending-intro-fee-scaling-2026-05-17`. ARCH §3.2.13, §4.1, §4.7. |
| Billing | **30a.6** | _Current-truth (2026-05-22-late tier-shape revision): the Solo/Team/Company/Enterprise shape this step locked is **fully retired** — replaced by Free / Pro / Enterprise as one product, capacity-gated, Enterprise on hybrid billing. Entitlement-matrix-v1 (18-dim, 3-tier) preserved in git at `66f6528`/`8c3e0b7`; superseded by v2 matrix at §14 (Axis 1–7) + `arc4-out/A-tier-matrix-detail.md` v2 + `app/policy/entitlements.py` v2 (Arc 5). The 4-tier rename surface (`admin_id`/`instance_id`) survives intact. Audit chain: `D-tenancy-collapse-admin-instance-lead-2026-05-22` + `~~D-tier-semantics-realignment-2026-05-20~~`._ ~~Tier-hierarchy semantic realignment — Team flattened from `agent + domain` to `agent`-only provisioning; Company preserved as `agent + domain + tenant`; entitlement matrix v1 landed in CANONICAL_RECAP §14 + `app/policy/entitlements.py`.~~ | A returning visitor to vantagemind.ai/pricing sees three tier cards with truthful intro-fee + 90-day-trial copy; per-tier features derived from the same entitlements module backend enforces. (Historical 4-tier success criterion preserved for audit chain; current truth is Free/Pro/Enterprise capacity-gated shape per Arc 4 revision.) | 🔧 In progress 2026-05-20 across four passes when superseded by Arc 4 tier-shape revision (2026-05-22). Pass 1–2 ✅ (six OPEN drifts authored under `D-tier-semantics-realignment-2026-05-20` umbrella; §14 entitlement-matrix-v1 sub-section authored; ARCH §3.2.13/§4.1/§4.7 annotated; carve-out drift `D-entitlement-matrix-v1-roadmap-rows-deferred-2026-05-20` carrying 8 Roadmap rows). Pass 3–4 superseded: code-side work re-targeted to Arc 5 callsite-rename pass; `Pricing.tsx` copy + `app/policy/entitlements.py` v2 land under the Free/Pro/Enterprise shape. Closing tag (planned): superseded — row closes at Arc 4 close per the new tier-shape doctrine. Operationalises `~~D-tier-semantics-realignment-2026-05-20~~`. |
| Hardening | **30a.7** | Cascade integrity + privilege-revocation hardening — post-Step-30a.5 correction when `/pilot-refund` against `co-354c5056` left orphan `scope_assignments`. Extends `AdminService.deactivate_tenant_with_cascade` from 9 to **13 in-function layers + 1 upstream subscription**, adds tenant-deactivated gate in `session_cookie_auth.py`, ships `scripts/backfill_cascade_orphans.py` (`--dry-run` default, per-tenant tx, `--tenant <id>` scope), pins shape with static-AST contract test `tests/services/test_cascade_includes_all_privilege_layers.py` (18 tests green). **13-layer ordering (leaf-first):** L1 conversations → L2 identity_claims → L3 memory_items → L4 api_keys → L5 luciel_instances → L6 agents → L7 agent_configs → L8 domain_configs → **L9 scope_assignments** → **L10 user_invites** (`ACTION_INVITE_REVOKED`) → **L11 sessions** → **L12 synthetic_orphan_users** (`synthetic=True` AND zero remaining active scope_assignments cluster-wide) → L13 tenant_config. Subscription upstream via `billing_webhook_service.py`; total per teardown = 14 audit rows. | Deactivating a tenant flips every privilege-bearing row in one transaction — no orphan scope_assignment, no redeemable user_invite, no active session, no synthetic-only orphan user. `SessionCookieAuthMiddleware` returns `401 {code: "TENANT_DEACTIVATED"}` on any cookied request whose `tenant_id` resolves to `TenantConfig.active=False`. **Zero customer impact** — `co-354c5056` was the only orphan-carrier. | 🔧 In progress 2026-05-20 across 7 passes; Pass 0–4 ✅ (7 OPEN drifts under `D-cascade-comment-drift-9-layer-claim-vs-13-layer-reality-2026-05-20` umbrella; admin_service.py extended L9–L12; `session_cookie_auth.py` gate; backfill CLI + contract test). Pass 5 🔧. Pass 6–7 📋 (commit/tag/push). **PROD-TIME gated:** build → ECR → ECS rolling → dry-run → `--apply` backfill on `co-354c5056` → final tag. Closing tag (planned): `step-30a-7-cascade-integrity-hardening-complete`. ARCH §4.4 ¶607 (9 → 13 + 1 upstream), §4.5 ¶615. |
| Billing | **30a.5** | _Current-truth (2026-05-22-late tier-shape revision): Company-tier self-serve **retired**. Enterprise is sales-ops provisioned; multi-Domain org-building replaced by `admin_tier_overrides`-driven per-Admin ceilings on unified Admin → Instance → Lead hierarchy. Self-serve route family + CompanyTab UI are dead code at Arc 5. Invite primitive survives at Pro-tier seat path._ ~~Company self-serve completion — org-building UI for paid Company-tier multi-department deployment.~~ | A Company customer paid $1,000 intro → `/app/company` → creates 2 Domains → invites 1 lead per Domain. Each lead sets password → `/app/team` scoped to Domain → invites 2 agents. (Historical T1 §13.1; current truth: unified hierarchy.) | ✅ Closed (2026-05-18) under 4-tier shape; **retired by Arc 4 tier-shape revision**. PR #50 backend + website #4 + post-smoke PR #51 + website #5. Image digest `sha256:8b5020d7745c3ae681e1083f2cc2407793e69b9f8250bfba08fef327dfeb4692`; task-defs `luciel-backend:72` / `luciel-worker:27` / `luciel-migrate:16`. Routes: `POST/GET /api/v1/admin/domains/self-serve`, `DOMAIN_COUNT_CAP_BY_TIER = {individual:1, team:1, company:50}`, Alembic `step30a_5_user_invite_role_and_audit_actions.py` (chained on `b4d8a2e7c1f3`), `UserInvite.role` + `DOMAIN_CREATED`/`INVITE_ROLE_ASSIGNED` audit. Closing tag `step-30a-5-company-self-serve-complete` at deploy SHAs `dba6a755` / `6ac47c0e`. Closes `~~D-company-self-serve-incomplete-org-building-ui-missing-2026-05-16~~`, `~~D-invite-redeemed-user-sees-no-subscription-on-file-2026-05-18~~`, `~~D-company-tab-domain-rollup-fields-missing-2026-05-18~~`, `~~D-team-tab-invite-row-missing-role-and-domain-2026-05-18~~`, `~~D-luciel-migrate-pins-raw-digest-fragile-2026-05-18~~`. Opens: `D-luciel-ecs-web-role-missing-ses-send-permission-2026-05-18`, `D-billing-webhook-service-stripe-attribute-error-2026-05-18`, `D-stripe-checkout-no-email-validation-2026-05-18`. ARCH §3.2.13, §4.1, §4.7. |
| Frontend | **30b** | Embeddable chat widget that any company can drop into their existing website. | A company adds a few lines of code to their site, and within an hour their visitors are having real conversations with the company's Luciel. This is the unblock for the first paying customer. | 🔧 Build complete + CDN live (2026-05-10, merge `5ffd42d`). Bundle reachable at `https://d1t84i96t71fsi.cloudfront.net/widget.js`. CDN-infrastructure closing tag: `step-30b-widget-cdn-complete`. Stage-1 staging E2E observed clean 2026-05-10 (four-turn multi-prompt run, all green). Architecture: ARCHITECTURE §3.2.2 (Issuance). Two-stage validation gate to flip ✅: stage-1 done; **stage-2 = REMAX Crossroads first paying-customer drop, observed clean 24–48h** — row stays 🔧 until then. Drifts opened by the staging run: `D-route-shipped-without-end-to-end-coverage-2026-05-10` (widget-surface slice CLOSED at Step 30d, broader-routes slice OPEN — Pattern E follow-up adds `pull_request` trigger), `D-widget-no-content-safety-or-scope-guardrail-2026-05-10` (CLOSED at Step 30d), `D-widget-chat-no-application-level-audit-log-2026-05-10` (CLOSED at Step 31 sub-branch 1). Operationalises Q6 — the widget channel leg (renumbered from Q7 under WU-9.1). |
| Hardening | **30c** | Action classification — tool invocations tiered as routine, notify-and-proceed, or approval-required, so Luciel asks first only when an action is genuinely consequential. | Customers feel Luciel acts decisively on routine work and pauses only when stakes warrant it. Audit log proves every approval-required action had a confirmation row preceding it. | ✅ Closed (2026-05-11). Fail-closed three-tier gate (ROUTINE/NOTIFY_AND_PROCEED/APPROVAL_REQUIRED) as pluggable provider; `LucielTool.declared_tier` mandatory, base default `None` so forgotten tier inherits APPROVAL_REQUIRED. ARCH §3.3 step 8, §4.9. Closing tag `step-30c-action-classification-complete` on `99c6eb5` (re-cut from code-complete `b216300`). Prod enforcement clean 2026-05-11: `luciel-backend:39` + `luciel-worker:19`, ECR `sha256:f0bf303272fb0801eefc4cf0d20d2ddb624f2a5f60c8e845cbe422869739f863` from HEAD `84339a3`. Runbook: `docs/runbooks/step-30c-action-classification-deploy.md`. Closes `D-confirmation-gate-not-enforced-2026-05-09`. Side-observation: `D-celery-worker-runs-as-root-2026-05-11` (pre-existing). Enforces Section 4 behaviour contract. |
| Hardening | **30d** | Widget content safety and scope guardrails — every chat turn passes moderation before the LLM is called, every embed key requires a non-empty per-domain scoping prompt before mint, automated test proves both on every widget-surface change. | A visitor can ask Luciel anything (off-topic, prompt-injection, harmful) and the widget responds in a way the customer's brand is comfortable defending. Operator cannot ship a widget without scope or safety — issuance refuses, CI refuses. | ✅ Closed (2026-05-11): (A) issuance scope-prompt preflight PR #14 merge `3cbd489`, (B) provider-agnostic fail-closed moderation gate + sanitized SSE refusal PR #15 merge `c7d958e`, (C) widget-surface E2E CI harness PR #16 merge `146f133`. ARCH §3.2.2, §3.3 step 6.5, §4.9. Closing tag `step-30d-content-safety-complete`. First clean run: widget-e2e dispatch `25695322187`. Closes `D-widget-no-content-safety-or-scope-guardrail-2026-05-10` + widget-surface slice of `D-route-shipped-without-end-to-end-coverage-2026-05-10` (broader-routes OPEN). Pattern E follow-ups: PR #18 (pgvector pin), PR #19 (`/health` readiness), PR #20 (hermetic stub LLM). Hardens Q6 widget channel's content-safety leg. |
| Frontend | **31** | Hierarchical dashboards (company / department / individual) and a five-part pre-launch validation gate before any new customer goes live. | Each level of the organization sees exactly what's happening at and below them. No customer goes live until five categories of readiness — isolation, customer journey, memory quality, operations, compliance — are all green. | ✅ Implemented (2026-05-11 → 2026-05-12) across PRs #29–#34. Three scope-bound dashboard views + widget-chat audit log + five-pillar pre-launch validation gate. ARCH §3.2.12, §3.2.7. Closing tag: `step-31-dashboards-validation-gate-complete`. Live harness `tests/e2e/step_31_validation_gate.py` exits 0 against dev Postgres 2026-05-12 (run `20260512-144847-068362`, 40/40 claims green: isolation 8/8, customer_journey 11/11, memory_quality 6/6, operations 9/9, compliance 6/6). Closes `D-step-31-impl-backlog-2026-05-11`, `D-widget-chat-no-application-level-audit-log-2026-05-10`. **⚠ Prod-deploy gap** open: `D-step-24-5c-and-31-schema-and-code-undeployed-to-prod-2026-05-12`. Closure caveats: Pillar 4 dual-path audit signal (CloudWatch markers + `admin_audit_log` rows via `before_flush`) tracked at `D-pillar-4c-evidence-location-recap-ambiguous-2026-05-13`; sub-pillars 4d/4e deferred to Step 31.1 (`D-pillar-4d-...`/`D-pillar-4e-...-deferred-2026-05-13`). Answers Q2 three-tier dashboards; completes Q1/Q7 pre-launch posture. |
| Hardening | **31.2** | Backend unlocks for the self-service customer journey — bridge the magic-link session cookie minted at Step 30a into admin-API context, lift the v1 carve-out so embed keys can pin to a specific `LucielInstance.id`. | A cookie-bearing customer who landed on `/dashboard` after magic-link can call `POST /api/v1/admin/luciels`, `POST /api/v1/admin/embed-keys`, `GET /api/v1/dashboard/*` from the marketing site without ever holding a long-lived admin key. Operator minting an embed key can pin to a specific Luciel instance; the key carries instance id end-to-end through widget chat. | ✅ Implemented (2026-05-13) on backend branch `step-31-2-cookie-bridge-and-instance-embed-keys`: (A) `f90b9a2` SessionCookieAuthMiddleware (`COOKIE_AUTH_PATHS=("/api/v1/admin","/api/v1/dashboard")`, resolves cookie → User → active subscription → `tenant_id`; mounted after `ApiKeyAuth`; `auth.py` short-circuits when `auth_method=="cookie"`). (B) `0322ade` lifted v1 `luciel_instance_id` carve-out on `POST /admin/embed-keys` (Pydantic validates instance exists 404, tenant-scope 403, domain-scope 403, active 422; no schema change — `ApiKey.luciel_instance_id` already nullable FK). (C) `c88dfec` e2e harness `tests/e2e/step_31_2_live_e2e.py` (6 scenarios) + 19 cookie-bridge + 15 instance-embed-key contract tests. ARCH §3.2.2, §3.2.13. Closing tag: `step-31-2-cookie-bridge-and-instance-embed-keys-complete`. Partially addresses `D-magic-link-auth-cookie-session-2026-05-13` (full close at Step 30a.3). Opens `D-admin-audit-logs-actor-user-id-fk-missing-2026-05-13`, `D-step-30a-billing-shape-test-moderation-config-failure-2026-05-13`. **⚠ Prod-deploy gap** — `D-step-24-5c-and-31-schema-and-code-undeployed-to-prod-2026-05-12` extends to 31.2 (code-only; Step 30a `b8e74a3c1d52` still needs prod RDS). Operationalises Q1 backend leg. |
| Frontend | **32** | Admin dashboard UI for the self-service customer journey — post-login surface where a paying customer sees tenant rollups, creates/configures Luciel instances, tests live, copies the deploy snippet. **Two waves:** wave 1 (✅ `/dashboard`, tenant-rollup-for-everyone); wave 2 (📋 `/app/*`, tier-adaptive under Free/Pro/Enterprise) re-renders unified Admin→Instance→Lead per cookied user's tier. Backend `/api/v1/dashboard/{tenant,domain,instance}` powers both. | **Wave 1:** post-login lands `/dashboard`, sees Overview rollups, opens Luciels tab, creates instance (`scope_level="tenant"` v1), Test sub-tab (throwaway `[test]` key SSE), Deploy sub-tab mints key + copies `<script src=…>` snippet — all without operator help. **Wave 2:** Free user sees one Admin/one Lead; Pro user sees Admin pool with seat-invites; Enterprise user sees overrides-driven shape per `admin_tier_overrides`. Each tier's workflow uses exactly the concepts that tier exists for. | **Wave 1 — ✅ Implemented** (2026-05-13) on website branch `step-32-admin-dashboard-ui` HEAD `4f2d64c` (8 files, 1765 ins, 11 tests). NEW on `aryanonline/Luciel-Website`: `src/lib/admin.ts` (typed gateway, `credentials:"include"`, exports `getTenantDashboard`/`listLucielInstances`/`createLucielInstance`/`updateLucielInstance`/`deactivateLucielInstance`/`createEmbedKey`/`listEmbedKeys`); `src/pages/Dashboard.tsx` (auth-gated on `GET /api/v1/billing/me`); `src/pages/LucielInstanceDetail.tsx` (Test SSE chat via `[test]` key, Deploy mints `POST /api/v1/admin/embed-keys` generating `<script src="https://d1t84i96t71fsi.cloudfront.net/widget.js" data-luciel-key=…>`); `src/components/admin/CreateLucielForm.tsx`; `src/App.tsx` adds `/dashboard` routes; `Login.tsx` redirect flips to `/dashboard`. Tests: 8 + 3. Build green. ARCH §3.2.13. Closing tag: `step-32-admin-dashboard-ui-complete`. Wave-1 carve-outs: Configure read-only v1; no file ingestion UI (Step 32a); password/SSO at Step 30a.3. **Wave 2 — 📋 Planned** under Free/Pro/Enterprise per Arc 4 revision. Single `/app/*` family on `vantagemind.ai`, gated by Step 31.2 middleware; renders unified Admin→Instance→Lead with `admin_tier_overrides`-driven shape. Wave-2 closing tag (planned): `step-32-tier-adaptive-app-shell-complete`. Operationalises Q1 frontend leg. |
| Frontend | **32a** | File input — every Luciel can ingest documents the customer provides. | A customer drops in their listing book, their playbook, or their internal handbook, and the Luciel starts using that knowledge in conversations the same day. | 📋 Planned. Operationalises Q3 ingestion leg — the document side of the memory tier. Architecture: ARCHITECTURE §3.2.6. |
| Intelligence | **33** | Evaluation framework — relevance, persona consistency, escalation precision, all measured automatically. | We can answer, with numbers, "is this Luciel getting better or worse over time?" — and we can tell which direction a recent change moved each metric. | 📋 Planned. Operationalises the measurement substrate Q4 (coordinator) and Q3 (hybrid retrieval) both need before they can be safely decided. |
| Enterprise | **33b** | Dedicated infrastructure tier for customers who require their own isolated environment. | A large customer who requires their own dedicated stack can be served on the same product, on their own infrastructure, without us forking the codebase. | 📋 Planned (no current ETA). Hardens Q1 / Q2 / Q7 deployment topology for enterprise customers (cross-channel renumbered from Q8 to Q7 under WU-9.1). Architectural home for the Q1 / Q2 / Q7 enterprise-deployment cross-refs in §11; remains here as the owning step for that branch of the product surface even though execution waits on first enterprise demand. |
| Intelligence | **34** | Workflow actions — Luciel can book appointments, send emails, create leads, and query business systems on behalf of the user. | Luciel stops being only an advisor and starts doing real work in the customer's existing tools — calendar, CRM, email, internal databases — with proper permission and audit. | 📋 Planned. Operationalises Q6 outbound tool leg (channels renumbered from Q7 to Q6 under WU-9.1); honours Section 4 via the action-classification gate landed at Step 30c. Architecture: ARCHITECTURE §3.3 step 7–8. |
| Intelligence | **34a** | Channel adapter framework — SMS, voice, email, all governed by the same scope policy as the chat widget. | A customer adds a phone line or an SMS number to their Luciel, and within a day it's handling inbound calls and texts with the same character and the same memory as the chat widget. | 📋 Planned (no current ETA). Operationalises Q6 (channels) and the voice/SMS/email legs of Q7 (cross-channel continuity) — inherits the three primitives shipped at Step 24.5c. (Q-tokens renumbered from old Q7/Q8 under WU-9.1.) Owning step for DRIFTS §3 `D-channels-only-chat-implemented-2026-05-09`. Architecture: ARCHITECTURE §3.2.1, §3.3 step 1. |
| Intelligence | **35** | Multi-vertical expansion playbook — a repeatable framework for adding the next vertical (legal, mortgage, engineering, etc.). | Onboarding a new vertical takes weeks, not months. The next vertical reuses the Soul layer entirely and only configures the Profession layer. | 📋 Planned. Operationalises the re-parenting half of Q5 (move scope upward across vertical boundaries). Architecture: ARCHITECTURE §4.2 (two layers), §4.5 (cascade-correct departure). |
| Advanced | **36** | Luciel Council — multiple Luciels in the same scope coordinating to deliver one outcome. | A user with three specialized Luciels asks one question and gets one coordinated answer, with each Luciel contributing what it knows best. | 📋 Planned (after 33). Operationalises Q4 — the coordinator Luciel and scoped Luciel-to-Luciel tool calls. Architecture: ARCHITECTURE §3.2.4, §3.3 step 7–8. |
| Advanced | **37** | Hybrid retrieval — graph and vector together, decided per domain, scaled up to a dedicated graph database when the customer base demands it. | Relationship-heavy questions get answered correctly without the user having to assemble the answer themselves. Hallucination on those questions drops measurably. | 📋 Planned. Decides Q3 (hybrid retrieval go/no-go). Architecture: ARCHITECTURE §3.2.6 (memory tier). |
| Advanced | **38** | Bottom-up expansion — when an individual customer's department or company comes on board, their work carries forward without loss. | Sarah's six months of accumulated context move with her into the department's deployment, and again into the company's. No one starts from zero just because the buyer changed. | 📋 Planned. Operationalises Q5 (Sarah-to-department-to-company) and the cross-scope identity federation leg of Q7 deferred at Step 24.5c (renumbered from Q8 under WU-9.1). Architecture: ARCHITECTURE §4.5, §3.2.11 v1 non-goal. |

---

## Section 13 — End-to-end product acceptance

Once the roadmap is complete, these are the scenarios we will run end-to-end to prove Luciel works the way it was designed. Each one is a real customer arc, written as a story. The right column is what we, watching it happen, would see as proof the product is working — not what's in a test suite, but what the customer actually experiences.

The first eight scenarios map directly to the strategic questions in Section 11 — they are the practical demonstration that each strategic answer holds in real use. The next group covers customer journey arcs that span multiple questions. The final group proves the behavior contracts from Sections 4 and 9 — that Luciel behaves the way Luciel is supposed to behave, not just that the features work.

### 13.1 Scenarios proving the strategic answers (Q1–Q7)

T-scenario numbering (T1–T8) is preserved across the Arc 9 Q-renumber so historical references in DRIFTS / arc-records / git remain anchored; the Q-token tag in each row points at the renumbered §11. Audit chain for the prior wording (four-tier branching, T6/T7/T8 mappings before the Q-collapse) lives in git at `8dd88fe` and earlier, and in DRIFTS §3 / §5 mirrors.

| # | Scenario | What we expect to see |
|---|---|---|
| **T1** (proves Q1) | A brand-new Enterprise customer completes a talk-to-sales call, is provisioned by sales-ops via `admin_tier_overrides`, signs in, lands on `/app`, and invites their teammates by email through the Pro/Enterprise seat-invite flow under the single Admin → Instance → Lead hierarchy. Each invitee clicks the magic link, sets a password, and lands on `/app` with the correct Instance-Lead scope visible. | The branching is done end-to-end by the customer and sales-ops working together, with **zero founder involvement on the runtime flow** — no support ticket, no manual key issuance, no Stripe-Dashboard intervention beyond the sales-ops provisioning step the Enterprise tier is designed to require. The Enterprise Admin sees the Admin-level rollup (every Instance, every Lead, audit trail). Each Instance Lead sees only their own Instances. No Lead can touch another Instance's Leads. The Admin can audit every action through `admin_audit_log`. Scope enforcement is the same Admin → Instance → Lead gate Step 24.5c and Step 31 exercise; the new surfaces are the **invite-acceptance flow** (Step 30a.5) and the **tier-adaptive `/app` shell** that renders the full Admin-level view for Enterprise callers (Step 32 wave 2). |
| **T2** (proves Q2) | The Enterprise Admin from T1 opens `/app`, a seat-holding Instance Lead opens `/app`, and a Free single-Instance user opens `/app`. Each one sees a single landing surface scoped to their level, with no "you don't have access" empty states and no concepts above their pay grade visible. | The Enterprise Admin sees the full Admin rollup (Instance breakdown, Lead activity, audit trail). The Pro/Enterprise Instance Lead sees a Lead-rollup landing for their own Instances only. The Free user sees a flat "My Instance" landing with no Admin chrome. Each answer arrives in under a minute. The dashboard data is the same Step 31 backend (`/api/v1/dashboard/{admin,instance}`); the change at Step 32 wave 2 is that the UI **hides hierarchy above the caller's scope rather than greying it out**, so a Free user literally never sees the words "Admin" or "seat" in their normal workflow. The schema is the canonical Admin → Instance → Lead — the collapse is UX-only. |
| **T3** (proves Q3) | A real-estate agent asks their Luciel: "Which of my buyers from last quarter were looking in neighborhoods where I now have new listings under their budget?" — a question that requires walking relationships, not just searching text. | Luciel answers correctly and completely in one response. The answer names the buyers, the matching listings, the price fit, and the timing. The agent doesn't have to piece it together from three separate searches. On a held-out set of relationship questions like this one, hallucinations are measurably lower than what the same Luciel produces from vector search alone. |
| **T4** (proves Q4) | An agent has deployed three Luciels for their own work — a listings Luciel, a marketing Luciel, and a client-followup Luciel. They ask one question: "Draft a follow-up to the buyers who toured 142 Maple last weekend, mention the two new listings I just got that fit their budget, and use whatever marketing language sounds most like me." | One coherent draft comes back. The listings Luciel surfaced the new properties. The client-followup Luciel knew who toured 142 Maple. The marketing Luciel shaped the voice. The user did not have to pick which Luciel to ask. None of the three Luciels reached outside its lane. |
| **T5** (proves Q5) | Sarah has been using Luciel on the **Free** tier as a single-Instance individual for several weeks. She upgrades to **Pro** via self-serve Stripe Checkout — same email, same data, no second account. Three months later her team signs up for **Enterprise** via talk-to-sales; sales-ops provisions the Enterprise Admin and invites Sarah by email. Sarah accepts the invite; her Pro Admin re-parents under the Enterprise Admin in the same transaction. | Sarah's Lead history, her conversation memory, her ingested knowledge, and her configured Instances all carry forward across Free → Pro and Pro → Enterprise without loss. The Enterprise Admin starts on day one with the benefit of Sarah's accumulated context. **Zero founder involvement on the Free → Pro upgrade** (self-serve through the same Stripe Checkout surface Pro signups use); **sales-ops involvement on the Pro → Enterprise upgrade by design** (talk-to-sales CTA — there is no self-serve Enterprise Checkout path). The invite-acceptance surface is Step 30a.5; the cross-Admin re-parenting transaction is Step 38. |
| **T6** (proves Q1's rotation-and-departure leg) | An Instance Lead at an Enterprise Admin is promoted to `delegated_admin`. A different Instance Lead leaves the organisation entirely. | The promoted Lead's access expands cleanly. The Luciels they built as a Lead are still theirs and still working, and they now have delegated-admin authority on top. The departing Lead's access ends within the same hour they leave. Every key they had touched is rotated. The Luciels they built for the Admin are still working — because the data was never theirs. The audit log shows exactly what happened, when, and by whom. |
| **T7** (proves Q6 — channels) | A brokerage configures a Luciel that takes inbound phone calls, replies to text messages, answers chat-widget conversations on the company's public site, and sends follow-up emails. | A prospect interacting through any of the four channels experiences the same Luciel — same character, same memory of their prior interactions, same recommendations. From the inside, adding a fifth channel later (say, WhatsApp) is a configuration change, not a separate product build. |
| **T8** (proves Q7 — cross-channel continuity) | A prospect chats with a brokerage's Luciel on the website Monday morning. Wednesday afternoon they call the brokerage's phone line, which is also Luciel-answered. | The Luciel on the phone greets them by name, references what they were looking for on Monday, and continues the conversation as if no time had passed. The prospect does not re-introduce themselves or repeat their context. The handoff between channels feels human. The voice and SMS legs land with Step 34a (channel adapter framework); until then the cross-channel demonstration runs over chat plus programmatic ingress only. |

### 13.2 Cross-cutting customer journey scenarios

| # | Scenario | What we expect to see |
|---|---|---|
| **T9 — Individual signup, daily use, memory** | An individual agent finds Luciel on our website, signs up, pays, configures their first Luciel, has three multi-turn conversations over a week about specific clients, and comes back the following Monday. | Sign-up to first useful conversation takes under thirty minutes. A week later, Luciel remembers each of the three clients by name, knows their priorities, knows what was sent to them, and picks up cleanly when the agent asks "any thoughts on Jordan since we last talked?" Memory is precise — Luciel doesn't blur details across clients. |
| **T10 — Brokerage onboarding to live with prospects** | A brokerage owner books an Enterprise talk-to-sales call, is provisioned by sales-ops via `admin_tier_overrides`, completes onboarding with our team's help, distributes Pro/Enterprise seat invites to their teammates by email, and embeds the chat widget on their public website. Within two weeks, a real prospect has their first conversation with the brokerage's Luciel. | Five-stage pre-launch validation passes before the brokerage goes live: isolation, customer journey, memory quality, operations readiness, and compliance. The first prospect conversation produces a usable Lead — captured in the brokerage's CRM, with Luciel's recommendation explained, including what tradeoff Luciel made and what it still needs to confirm. The brokerage owner can see the conversation, the recommendation, and the audit trail in their Admin dashboard the same day. |
| **T11 — Customer leaves the platform** | A brokerage cancels their Enterprise subscription. | Within one atomic operation, every Instance under the brokerage's Admin stops responding, every key they had is revoked, every Instance Lead under that Admin loses access, and a full audit record is generated. The data is retained for the contracted retention period and then purged. No orphaned access. No half-states. The brokerage receives a clean exit summary they can hand to their compliance team. |
| **T12 — Workflow action with audit** | An agent's Luciel is asked to book a property showing for a buyer. | Luciel proposes the action with what it's about to do ("Book Wednesday at 4pm with the listing agent at 142 Maple, send a confirmation to the buyer, add to your calendar"), waits for the agent's approval, executes only after approval, and records the action in the audit trail with who approved it, when, and what changed in each external system (calendar, CRM, email). |
| **T13 — New vertical onboarded from the playbook** | We onboard the second vertical — say, mortgage brokers. The Soul layer is unchanged. The Profession layer is configured fresh: domain knowledge, tools, workflows, compliance rules. | The first mortgage broker is live within weeks, not months. The Luciel feels like Luciel — same character, same recommendation format, same trust boundaries — but it knows mortgages, talks like someone who knows mortgages, and uses mortgage tools. None of the real-estate-specific configuration leaked across. |

### 13.3 Behavior-contract scenarios (proving Sections 4 and 9)

These prove Luciel behaves the way Luciel is supposed to behave. They are as important as the feature scenarios — possibly more important, because they are what earn customer trust at scale.

| # | Scenario | What we expect to see |
|---|---|---|
| **T14 — Honest about what it doesn't know** | An agent asks Luciel: "What's the closing price going to be on this listing?" — a question Luciel can't actually answer with certainty. | Luciel does not invent a number. It says clearly what it can offer (comparable recent closes, current market signals, the seller's stated floor) and what it cannot (a guaranteed closing price). It distinguishes inference from fact. The agent leaves the exchange with more useful information than they started with, and zero false confidence. |
| **T15 — Refuses to push against the end user's interest** | A brokerage has configured their Luciel with a sales-pressure prompt that nudges every prospect toward the most expensive listing. A prospect tells Luciel they're financially anxious and looking for the safest option in their budget. | Luciel does not push the expensive listing. It surfaces options that match the prospect's stated priority. If the brokerage's configuration tries to override this, Luciel's Soul layer holds — the brokerage cannot configure Luciel to coerce. The brokerage can see, in their dashboard, what Luciel did and why. |
| **T16 — Stays in its lane** | An individual agent asks their own Luciel for another agent's client list. | Luciel declines cleanly, with a reason the agent understands ("that's outside what I have access to from your scope"). It does not invent the answer. It does not leak partial information. It does not pretend the request was unclear. |
| **T17 — Asks before consequential action** | An agent's Luciel is asked something that, to fulfill, would require sending an external email to a client. | Luciel does not send the email. It drafts the email, surfaces what it's about to do, and waits for the agent's confirmation. Only after explicit approval does the email go out. The action is recorded with who approved it. |
| **T18 — Escalates when the situation crosses a threshold** | A prospect, mid-conversation with a brokerage's Luciel, expresses meaningful emotional distress about a housing situation. | Luciel does not try to resolve the situation alone. It responds with calm and with care, surfaces the human contact at the brokerage, and hands off the conversation cleanly. The brokerage's dashboard shows the escalation, the trigger, and the handoff — so the human picking up the conversation has full context. |
| **T19 — Recommendation in canonical format** | Any agent, asking any recommendation question across any vertical, in any channel. | The response follows the four-part recommendation format every time: what Luciel thinks suits them best, why it fits them, what tradeoff comes with it, and what Luciel still needs to confirm. The format does not drift across domains. The format does not drift across channels. |

---

## Section 14 — Monetization

Luciel is sold at three tiers (**Free**, **Pro**, **Enterprise**). Each tier is the **same product**, gated along six independent axes: instance count, per-instance capacity, model tier per instance, composition, configurability, and operational features. This matches our reference class — OpenAI, Perplexity, Linear, Vercel — where tier-up unlocks more capacity and richer configuration of the same product, not a different product. Tier label is shorthand for a bundle of gate settings; the same VantageMind binary serves every tier. Free ships at $0 with no Stripe subscription row at all (provisioning is gated by CAPTCHA and one-per-email/IP); Pro ships as a flat-monthly Stripe subscription (with an optional annual cadence priced approximately 28% below the monthly-equivalent — Arc 6 deviates from the earlier "10× monthly" guidance after competitor-anchored re-grading; see `arc6-out/B-arc6-numeric-lock.md` §3); Enterprise ships on a **hybrid billing model** (recurring platform-fee Price plus a metered usage Price on the same subscription, with a committed-use discount on the floor) per ARCHITECTURE §3.2.13 / §3.2.14.

The legacy four-tier framing (Solo / Team / Company / Enterprise locked at Step 30a.6 on 2026-05-20) and the genuinely-different-products framing (§275) are both **retired**; the live shape is the three-tier framing below. The retirement path is recorded at DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22` and at `~~D-tier-semantics-realignment-2026-05-20~~`; the historical four-tier price-point table lives in git at the Arc 4 v1 commit (`66f6528` / `8c3e0b7`) and in the audit-chain entries in §17.

| Tier | Monthly | Annual | Instance cap | Audience | What it covers |
|---|---|---|---|---|---|
| **Free** | $0 | $0 | 1 | An evaluator or curious individual | One Instance, base model only, up to 100 Leads / month, no composition, **API enabled at 30 rpm with 1 embed key**, 1 admin-team seat. CAPTCHA + one-per-email-and-IP at signup. |
| **Pro** | $349 CAD | $2,990 CAD (annual ≈ 28% off monthly-equivalent; see `arc6-out/B-arc6-numeric-lock.md` §3 for the reasoning behind the deviation from earlier "10× monthly" guidance) | 10 | A solo operator or small team | Up to 10 Instances on the mid model with top-model bursts, up to 5,000 Leads / month, composition within Admin at depth≤2, API at 300 rpm with 10 embed keys, 25 admin-team seats, 1 widget custom-domain CNAME, flat-monthly Stripe billing. |
| **Enterprise** | Starting at $24,000 / year | Starting at $24,000 / year | Unlimited (via `admin_tier_overrides`) | A team running real estate operations at scale | Unlimited Instances (subject to per-Admin overrides), all model tiers including custom fine-tunes, unlimited Leads (with included floor + overage), composition + knowledge-share, API at 3,000 rpm default with unlimited embed keys + unlimited widget CNAMEs, unlimited admin-team seats, **hybrid billing** (platform fee + included usage + overage; committed-use discount available). Talk-to-sales CTA, no self-serve checkout. |

The Pro and Enterprise live values ($349 / $2,990 / $24,000 CAD) were locked at Arc 6 Commit 1 per `arc6-out/B-arc6-numeric-lock.md` and are pinned to live Stripe SKU metadata at the Arc 6 mint commit — not to this canonical doctrine table. The same values land in Pricing.tsx at the same Arc 6 commit and are read from the Stripe Price `metadata` on every checkout-session mint, so this doctrine cannot silently fall out of sync with the runtime.

**Why the price difference is what it is.** Free, Pro, and Enterprise are the same product at different capacity gate settings. A Pro Admin gets more Instances, deeper composition, higher per-Instance capacity, and the API affordance compared with a Free Admin — more of the same product, not a different product. An Enterprise Admin extends that with `admin_tier_overrides`-driven per-customer ceilings, the hybrid billing model that lets large customers pay for actual usage rather than a worst-case static cap, and the full operational feature set (audit CSV export, SSO, priority support, dedicated success manager). The same VantageMind binary runs at every tier; only the entitlement gates and the billing-model wiring differ. This is the reference-class pattern (OpenAI, Perplexity, Linear, Vercel): tier-up is capacity-up and capability-up, never topology-up.

What each tier actually buys (over the Admin → Instance(s) → Lead(s) hierarchy):

- **Free** buys a single Instance under a single-admin Admin, base-model only, up to **100 Leads per month**, no composition, **API enabled at 30 rpm with 1 embed key**, 1 admin-team dashboard seat. The abuse-control posture is `CAPTCHA + one-per-email-and-IP` at signup, no payment-method gate (`admins.stripe_customer_id` is NULL while `tier='free'`, lazy-created at the moment of upgrade per the Gap 1 resolution at `arc5-out/A-arc5-arc4-plan-defects.md` §6.4); the runtime entitlement check reads `admins.tier='free'` directly with no `subscriptions` join. Free is an evaluation surface, not a permanent deployment: an evaluator who exceeds 100 Leads/month or wants composition is on the upgrade path to Pro. **The Free-tier API enablement raises `D-free-tier-captcha-missing-2026-05-22` to P1** because programmatic load (not just token budget) is now part of the abuse surface; the captcha + soft-gate must land before Free launches.
- **Pro** buys an Admin with up to **10 Instances**, the mid model with top-model bursts on configurable surfaces, up to **5,000 Leads / month**, composition within the Admin scope at depth≤2 (the load-bearing affordance that distinguishes Pro from Free — a Pro Admin's Instances can compose their knowledge within the Admin scope, a Free Admin's cannot), **API at 300 rpm with up to 10 embed keys**, **25 admin-team dashboard seats** (admin-account-scoped, not per-Instance per the seat-row semantic locked at `arc5-out/A-arc5-arc4-plan-defects.md` §6.5), and **1 widget custom-domain CNAME** (the new Axis 5 sub-row landing at Arc 6 via a new `admin_widget_domains` table, NOT in the legacy `domains` table dropped at Arc 5). The billing model is **flat monthly Stripe** (with optional annual priced ~28% below the monthly-equivalent — Arc 6 deviates from the earlier "10× monthly" guidance after competitor-anchored re-grading; see `arc6-out/B-arc6-numeric-lock.md` §3); `subscriptions.billing_model = 'flat'`. **The Pro multiplicative composition (300 rpm × 25 seats × 10 instances × 10 keys) opens `D-pro-tier-rate-limit-abuse-surface-2026-05-23`** — per-key + per-instance rate buckets land in Arc 8 post-Arc-5 before Pro is at scale.
- **Enterprise** buys an Admin with unlimited Instances (subject to `admin_tier_overrides` for negotiated bounds), all model tiers including custom fine-tunes, unlimited Leads (with an included floor and metered overage), composition with `knowledge_share_grants` enabled by default, **API at 3,000 rpm default** (contract may negotiate higher), unlimited embed keys, unlimited widget CNAMEs, unlimited admin-team seats, optional SSO-mapped `delegated_admin` sub-admins, and the full operational feature set (audit CSV export, SSO, dedicated CSM, 24h email SLA, 7-year audit retention or contract). The billing model is **hybrid**; `subscriptions.billing_model = 'hybrid'`. Enterprise is sold via a talk-to-sales CTA — there is no self-serve Stripe Checkout path; the platform fee + metered Price is provisioned by sales-ops via the Customer Portal or direct Stripe API per the runbook at arc4-out/A-tenancy-collapse-arc-record.md §6.

The instance cap on each tier is a per-tier ceiling at Free and Pro, and a per-Admin override surface at Enterprise; the **entitlement matrix** sub-section below is the first-class artifact that authors the operational differences (rate limits, conversation caps, channel availability, branding, SSO, SLA, success-manager) the price difference is also paying for, beyond the per-Instance capacity difference.

**Annual cadence shape (Pro only).** Pro is the only paid tier with a monthly/annual cadence split; Free has no billing cadence (no Stripe row) and Enterprise is annually-committed by default with the hybrid Price set on the Enterprise subscription. Pro-annual is priced at **ten times the Pro-monthly rate**, billed once per year (one invoice, not twelve) — the headline copy on Pricing.tsx reads "One bill a year, billed once × 10. Same product, same scope." rather than "2 months free", because the customer-facing story is the cadence shape (one bill instead of twelve), not a discount framing the price table does not support. The Pro-annual flow is otherwise identical to Pro-monthly: same paid intro fee (one-time, flat $100 CAD — see the Intro offer paragraph below) on first-time customers, same 90-day trial window on the recurring price, same Step 30a.3 password-set-at-registration surface, same Stripe Customer Portal for self-serve plan changes and cancellation. Pro-annual carries no quota uplift over Pro-monthly — the entitlement matrix below is keyed on tier only, not on (tier, cadence); a customer who picks Pro-annual instead of Pro-monthly does so for cash-flow reasons (one PO per year rather than twelve), not for capacity. There is no separate free-trial window on the Pro SKUs (the paid intro shape closed at Step 30a.2 replaced all per-tier trial-day policies). Cancellation mid-cycle through the Stripe Customer Portal stops future recurring billing through the same `customer.subscription.deleted` → Pattern E cascade; the intro fee is refundable through the dedicated `/pilot-refund` route during the 90-day window and non-refundable thereafter (per "Intro refund policy" below). The legacy "2 months free" framing, the per-tier monthly-trial windows (14 days for Individual, 7 days for Team and Company), and the per-tier intro-fee scaling design ($100 / $300 / $1,000) are all preserved in the audit chain via DRIFTS §5 `~~D-annual-cadence-two-months-free-framing-2026-05-20~~`, `~~D-trial-policy-mixed-per-tier-2026-05-14~~`, and `~~D-intro-fee-scaling-to-per-tier-2026-05-16~~`; the live shape is the one in this paragraph.

**Intro offer (Pro only).** First-time Pro customers — identified by case-insensitive email match against the historical customers table — pay a **one-time intro fee of $100 CAD at Pro signup**, receive a 90-day trial on the recurring Pro price, and convert to the standard Pro recurring rate on day 91. The intro fee is **flat $100 CAD** (the per-tier scaling design that briefly lived in this paragraph between 2026-05-16 and 2026-05-20 is retired and preserved in the audit chain at DRIFTS §5 `~~D-intro-fee-scaling-to-per-tier-2026-05-16~~`). The 90-day window is uniform across Pro-monthly and Pro-annual; the once-per-email-ever rule still holds — a Pro customer who cancels and rejoins later pays the standard Pro recurring rate at the second signup with no second intro. **Free has no intro fee** (signup is $0 and gated by CAPTCHA + one-per-email/IP rather than by payment). **Enterprise has no intro fee** in this shape — sales-ops sets a per-customer pilot or committed-use discount on the platform-fee Price directly via the Customer Portal or Stripe API, and the implementation home for that is the `admin_tier_overrides.committed_use_discount` column (see ARCHITECTURE §3.2.14). See ARCHITECTURE §3.2.13 (Billing surface — the implementation home for the Stripe Option A line-item assembly, the `is_first_time_customer` gate, and the single `stripe_price_intro_fee` SSM key).

**Intro refund policy (Step 30a.2, locked 2026-05-15; per-tier scaling proposed 2026-05-16 and REJECTED 2026-05-20 at Step 30a.7 diligence pass; courtesy-email addendum landed at Step 30a.2-pilot Commit 3j 2026-05-16).** A first-time Pro customer in the active 90-day intro trial can self-serve a full refund of the $100 CAD intro fee they paid at Pro signup (Free customers have no intro fee, and Enterprise pilot discounts are administered via `admin_tier_overrides.committed_use_discount_bps` rather than refunded as a one-time charge) at any time before the trial converts on day 91. The refund cancels the underlying subscription in the same operation — the customer cannot keep the trial running after the refund clears. After day 91 the intro fee is non-refundable; cancellation through the Customer Portal stops future recurring billing (the §3.2.13 cancel cascade) but does not retroactively refund the intro charge or any recurring charges already invoiced. Refunds during the trial run through a dedicated self-serve endpoint rather than the Customer Portal because Stripe's Portal does not natively expose one-time-charge refunds; the architectural detail (the eighth `/pilot-refund` route, the `SUBSCRIPTION_PILOT_REFUNDED` audit action, and the eligibility predicate that re-uses `is_first_time_customer` plus a trial-window check) lives in ARCHITECTURE §3.2.13. **Customer-communication leg (Commit 3j addendum 2026-05-16):** alongside the on-page refund-success surface and Stripe's optional account-level refund receipt, the platform sends the buyer a best-effort SES courtesy email (subject “Your VantageMind pilot has been refunded”) summarising the refunded amount, the Stripe refund id, the 5–7 business day card-credit window, the canceled-and-closed account state, and a feedback CTA — fired post-commit so a SES failure can never roll back the refund cascade, and a failure path that writes a `pilot_refund_email_send_failed` audit row instead of throwing. The email is a third confirmation leg, not a contract: the audit row remains the single source of truth for the refund itself. ARCHITECTURE §3.2.13 names this as the fifth atomic side effect of `BillingService.process_pilot_refund`.

### Entitlement matrix (Free / Pro / Enterprise — abstract shape)

The operational differences between Free, Pro, and Enterprise live along **six gating axes plus a billing-model axis**. The table below is the canonical buyer-facing shape; the per-dimension detail (live-today vs roadmap status, enforcement call-sites, drift carriers, Alembic columns) lives in ARCHITECTURE §3.2.13 (entitlements) and §3.2.14 (metering + hybrid billing). The runtime source of truth is `app/policy/entitlements.py` (landing at Arc 5); the engineering-facing detail spec is `arc4-out/A-tier-matrix-detail.md`; the **single source of truth for the locked numeric values is `arc5-out/A-arc5-arc4-plan-defects.md` §6.5** (2026-05-23 Option A founder-locked entitlement table). When canonical / architecture / policy module disagree, the defects-doc §6.5 table wins, the policy module is corrected to match it, and the disagreement is recorded as a drift.

| Axis | Free | Pro | Enterprise |
|---|---|---|---|
| **Instance count** (how many Instances the Admin may run) | 1 | 10 | Unlimited (sales-negotiated, via `admin_tier_overrides`) |
| **Per-instance capacity** (Leads/month, audit retention) | 100 Leads/mo, 30d audit | 5,000 Leads/mo, 1y audit | Unlimited included floor + metered overage, 7y audit (or contract) |
| **Model tier per instance** (which LLM backs each Instance) | Base model only | Mid model with top bursts | All tiers + custom fine-tunes |
| **Composition** (whether Instances can call each other; knowledge-share) | Disabled | Within Admin, depth≤2 | Unlimited, with explicit `knowledge_share_grants` |
| **Configurability** (operator branding, channel adapters, widget custom-domain CNAME) | Defaults only, no widget CNAME | Defaults only + 1 widget CNAME (`chat.theircompany.com`); channels on Step 34a roadmap for all tiers | Custom branding + unlimited widget CNAMEs + roadmap channels |
| **Operational features** (seats, API, SSO, support, CSV export, success manager) | 1 admin-team seat, **30 rpm API enabled** (1 embed key), community support | 25 admin-team seats, 300 rpm API (10 embed keys), 48h email support | Unlimited admin-team seats, 3000 rpm API default, SSO, CSV export, custom SLA (24h email + dedicated CSM) |
| **Billing model** (Axis 7 — the hybrid axis) | No `subscriptions` row (`$0` Free; `admins.stripe_customer_id` NULL, lazy-created on upgrade) | `subscriptions.billing_model='flat'` | `subscriptions.billing_model='hybrid'` (platform fee + included usage + metered overage; committed-use discount available) |

Live-Today axes (enforced at runtime now): Instance count, Per-instance Leads/month cap, Seats, Billing model. All other axes are on the Roadmap and tracked under the carve-out drift `D-entitlement-matrix-v1-roadmap-rows-deferred-2026-05-20`; per-row drifts open lazily at the corresponding-Step touch. The previous 18-dimension × 4-tier matrix (Solo / Team / Company / Enterprise) is retired and preserved in git at Arc 4 v1 commits (`66f6528` / `8c3e0b7`). The intermediate Arc 4 v2 3-tier numeric draft (Free 10 leads / Pro 3 instances / Pro 60rpm / Pro 5 seats / Pro 2000 leads / Pro 3 embed keys / Enterprise 1000rpm) is **superseded 2026-05-23** by the Option A numeric table above; the v2 draft is preserved in git at `66f6528` / `8c3e0b7` (Arc 4 commits) for audit. **Seat row semantic (locked 2026-05-23):** "seats" means admin-team dashboard logins at admin-account scope (Meaning 1); it is NOT a per-instance count. **Widget custom-domain CNAME row semantic (NEW axis, locked 2026-05-23):** the widget-served subdomain (e.g. `chat.theircompany.com`) lives in a new `admin_widget_domains` table landing at Arc 6, NOT in the legacy `domains` table dropped at Arc 5. **Abuse-control prerequisites for launch:** Free-tier API enablement makes `D-free-tier-captcha-missing-2026-05-22` (escalated P2→P1 2026-05-23) a hard prerequisite to Free launch; Pro multiplicative composition (300rpm × 25 seats × 10 instances × 10 keys) opens `D-pro-tier-rate-limit-abuse-surface-2026-05-23` (P1) which lands per-key + per-instance rate buckets in Arc 8 post-Arc-5.

**A note on dedicated infrastructure.** A future enterprise tier will offer a fully dedicated environment (own database, own compute, own audit boundary) for customers whose compliance posture requires it. It will be built when the first customer actually demands it — not speculatively.

---

## Section 15 — What Luciel deliberately is not

These are not gaps. They are decisions. Adding any of them requires a roadmap-level conversation, not a feature request.

- **No mobile app.** The chat widget covers the customer surface today. A native app costs more than it adds.
- **No marketplace of user-generated Luciels.** Verticals are operator-defined and operator-curated. Quality is the moat.
- **No model training or fine-tuning.** Luciel uses the best available foundation models through their APIs. The differentiation is judgment, configuration depth, and integration — not a custom model.
- **No internationalization yet.** English-language and North America–focused until customer demand surfaces.
- **No on-premise deployment.** Dedicated cloud infrastructure (Section 13) is the highest level of isolation we offer, unless and until a paying customer requires more.
- **No chasing competitor features.** If a feature isn't on this roadmap, it's deliberately out of scope. We will say no.

---

## Section 16 — Source-of-truth rule

If a chat summary, a session recap, a slide, or a pitch contradicts this document, **this document wins**. Update the document; do not produce contradicting versions in flight.

---

## Section 17 — Maintenance

- This document is business and product only. Code and infrastructure detail belong in `ARCHITECTURE.md`. Open and resolved deviations belong in `DRIFTS.md`.
- Surgical edits only. When a strategic question moves status, update Section 11. When a roadmap step lands, update Section 12. When an end-to-end scenario passes for the first time in production, update Section 13. When a price changes or a tier is added, update Section 14.
- No version-history sediment. The document reflects current state. Past state is in git and in `DRIFTS.md`.
- One source of truth per fact **within this document**. If a fact appears in two sections of `CANONICAL_RECAP.md`, delete one. Across the three canonical documents (`CANONICAL_RECAP.md` business view, `ARCHITECTURE.md` system view, `DRIFTS.md` integrity view), the same fact stated from each document's own angle is the triangulation that gives the audit story its integrity and is required — see `DRIFTS.md` §1 source-of-truth rule and §6 maintenance lifecycle.
- **2026-05-22 (Arc 4 tenancy collapse):** This document was revised to reflect the Admin → Instance → Lead model collapse and the doctrinal reversal of §275 (tiers are now one product, capacity-gated, not genuinely different products). Prior framing preserved in audit chain at DRIFTS §3 `D-tenancy-collapse-admin-instance-lead-2026-05-22` and `~~D-tier-semantics-realignment-2026-05-20~~` (resolved by this revision).
- **2026-05-23 (Arc 5 Commit 2 — Option A entitlement-table revision pass):** The numeric values inside the §14 entitlement matrix were revised to the founder-locked Option A table (`arc5-out/A-arc5-arc4-plan-defects.md` §6.5 is now the single source of truth for the numeric values; the buyer-facing §14 table mirrors it). Seven of eight rows diverge from the Arc 4 v2 draft, all in the direction of strict expansion: Free leads 10→100, Free API disabled→enabled at 30rpm + 1 embed key, Pro instances 3→10, Pro leads 2000→5000, Pro API 60→300rpm, Pro embed keys 3→10, Pro seats 5→25, Enterprise API default 1000→3000rpm; only Pro audit retention (1y) and the model-tier-per-instance and composition / billing-model axes are unchanged. The seat-row semantic was clarified to **"admin-team dashboard logins at admin-account scope" (Meaning 1)** — NOT a per-instance count. A NEW Axis 5 sub-row was opened for **Widget custom-domain CNAME** with Free=0 / Pro=1 / Enterprise=unlimited, backed by a new `admin_widget_domains` table landing at Arc 6 (NOT in the legacy `domains` table dropped at Arc 5). Sibling document changes: `arc4-out/A-tier-matrix-detail.md` §2.1 / §2.3 / §3.1 / §3.3 / §6.1 / §6.2 / §7.1 / §11.2 (NEW) / §14.1 / §18.2 / §19 body-rewritten to Option A numbers; DRIFTS §3.0 V2-anchored row for `D-free-tier-captcha-missing-2026-05-22` annotated with the 2026-05-23 escalation; DRIFTS §3 body for that drift escalated P2→P1 with rationale; DRIFTS §3 NEW drift `D-pro-tier-rate-limit-abuse-surface-2026-05-23` opened at P1; `arc5-out/A-arc5-arc4-plan-defects.md` §6 records Q1/Q2/Q3 + Gap 1 partner resolutions and §6.5 locks the entitlement numeric table; `arc5-out/A-arc5-preflight.md` §6 Gap 1 marked RESOLVED with pointer. Pattern E: this is a numeric revision in place of an earlier numeric draft, applied under the truth-on-first-read doctrine — the Arc 4 v2 draft is retired from canonical doctrine and preserved only in git at `66f6528` / `8c3e0b7`. The Solo→Pro and Company→Enterprise label-rename surface is **unchanged**; the rename map updates only the numeric column to the post-2026-05-23 Option A values. No customer-facing tightening notice is required because every revised number is strictly more generous than its Arc 4 v2 predecessor.
- **2026-05-23 (Arc 5 Commit 5 — Revision A local smoke test):** The Revision A migration (`alembic/versions/arc5_a_admin_instance_additive.py`, landed at Commit 4 `42958b5`) was end-to-end smoke tested against a fresh `pgvector/pgvector:pg16` compose container on host port 5433, isolating it from the standalone `luciel-postgres` dev container on 5432 (the Path-3 isolation strategy authored at this commit's prep stage `2a0006a`). All six smoke steps passed: (0) fresh DB stood up; (1) 30+ base migrations replayed empty → `b2e5f17a3d9c` (the prod-equivalent baseline) cleanly; (2) `alembic upgrade head` landed Revision A in a single jump (24 → 30 tables); (3) all seven sub-checkpoints confirmed (table inventory, `admins` shape per Q1/Q2/Gap 1 locks, `subscriptions.billing_model` add + `CHECK`, `admin_tier_overrides` wide-row + 6 `CHECK`s, grant-table no-self + scope `CHECK`s, `metering_emissions` composite-PK + UNIQUE idempotency-key + 2 `CHECK`s + `ON DELETE RESTRICT` FK); (4) `alembic downgrade -1` returned to `b2e5f17a3d9c` byte-identical (rollback escape hatch real); (5) `alembic upgrade head` (cycle 2) re-landed Revision A and live-catalog `information_schema.table_privileges` confirmed all 10 expected `luciel_worker` GRANTs intact across both cycles — the initial 901-line `Compare-Object` against the pg_dump file was pg_dump session-noise (ownership formatting + extension emission order + `\restrict` session tokens), not a real schema delta; (6) `docker compose down -v` cleanly removed compose artifacts while the standalone container survived. **Revision A is production-ready.** Five protocol-vs-migration drifts surfaced and were truthified at this commit (`admins.tier_source` default, `knowledge_share_grants.scope` `CHECK` width, `ck_composition_grants_no_self_composition` constraint-name shortening, pg_dump-diff technique unreliable for idempotency proofs, PowerShell double-quote `\d+` escape gotcha) — bundled into umbrella drift `D-arc5-commit5-smoke-protocol-truthification-2026-05-23` (CLOSED) plus the partner-entrusted standalone drift `D-arc5-knowledge-share-scope-tight-2026-05-23` (CLOSED-with-rationale: keep tight per YAGNI + asymmetric reversibility). Sibling document changes: `arc5-out/A-arc5-commit5-smoke-protocol.md` appended with an `ACTUAL RUN — 2026-05-23` section recording outcomes + drifts + forensic assertions; `docs/DRIFTS.md` body authored for the two new drift IDs; `docs/CANONICAL_RECAP.md` this entry. **The lesson now canonical for future arcs:** for migration round-trip idempotency tests, the comparison technique is direct `information_schema` / `pg_catalog` queries on the live DB, NOT file diffs against pg_dump output. **Doctrine guardrail recorded at this commit:** the partner re-affirmed the three-canonical-doc symmetry (`CANONICAL_RECAP.md` + `ARCHITECTURE.md` + `DRIFTS.md`), with `arc-out/` / `runbooks/` / `incidents/` / `designs/` / `_session_notes/` / `in-flight/` artifacts as a second operational tier governed by the hard rule that every satellite file MUST be referenced from §17 or another canonical anchor — unreferenced satellites are sediment and get archived at the next arc close. This Commit 5 closure is the first execution of that rule (no new `arc5-out/B-*.md` file authored; the smoke verdict landed as this single §17 paragraph). Closing tag deferred to post-prod-execution of Revision A (TODO item 11) under the same `arc-5-revision-a-smoke-complete` tag so the tag covers the local+prod pair. Operationalises Q1, Q5, Q8 (the Admin → Instance → Lead schema collapse is now production-applicable).
- **2026-05-23 (Arc 5 Commit 6 — Sandbox prod-control-plane posture established):** The sandbox-agent gained standing prod-touching capability so that prod migrations, prod state reads, and post-apply verification can be driven directly from the sandbox instead of round-tripped through the partner's Windows PowerShell — the pre-Commit-6 posture had partner-driven Windows-paste as the **only** prod-control-plane edge, which the Arc 5 Commit 5 smoke test established works but is too slow as a steady-state pattern for the prod-touching arcs ahead (Commit 7 Revision A apply, Revisions B/C, Arc 6 Stripe SKU restructure, Arc 8 WU-7/WU-8, Phase 6 E2E replay). Partner-confirmed intent ("from now on you can work on the prod as well … so we can make our code and prod is clean and there is no staleness") locks this as a **deliberate posture shift across sessions and arcs**, not an Arc-5-specific enabler. The shift is implemented as a single new IAM principal in the AWS account with deliberately narrow scope — the system-view facts (principal identity, inline policy contents, capability surface, boundary surface, relationship to pre-existing IAM topology, audit-trail integration, runtime binding) are canonical at `ARCHITECTURE.md` §3.2.8 Arc 5 Commit 6 bullet, the operational procedures (rotation, revocation, scope-expansion, paste protocol, discipline rules) are canonical at `docs/runbooks/aws-sandbox-credential-posture.md`, and the integrity exposure (cumulative scope-creep risk + the 5-gate enforcement) is canonical at `DRIFTS.md` `D-prod-credential-scope-expansion-protocol-2026-05-23`. **Pillar mapping.** Traceability is strengthened (CloudTrail records every sandbox-principal API call in the same audit trail as every other IAM actor; the sandbox-side runbook ACTUAL RUN sections provide the why-and-who second layer). Reliability is strengthened (continuous in-sandbox apply→verify flow without context loss across paste cycles). Security is consciously traded (a long-lived programmatic credential now exists in the account where none did before for sandbox-agent use — the trade is mitigated by minimum-viable scope, by the disjoint relationship to all pre-existing IAM topology, by the operational discipline rules in the runbook, and by the 5-gate guardrail against scope creep). Maintainability and scalability are unaffected today and become positive at the next prod-touching arc. **Why this commit lands BEFORE Arc 5 Commit 7 (Revision A prod apply).** The posture is made explicit — truthified across all three canonical views — before any capability under it is used, so the audit story for the first prod-touching invocation reads in order: posture established (Commit 6) → capability used (Commit 7). Pause-between-commits discipline preserved. **Sibling document changes at this commit:** `ARCHITECTURE.md` §3.2.8 NEW bullet `Arc 5 Commit 6 — sandbox-agent prod-control-plane principal landed 2026-05-23` (the system-view); `docs/runbooks/aws-sandbox-credential-posture.md` NEW (the operational runbook, anchored from this entry per Posture B); `docs/DRIFTS.md` NEW drift `D-prod-credential-scope-expansion-protocol-2026-05-23` (OPEN, P3, the integrity-view enforcement of the 5-gate scope-expansion procedure). The credential's existence was verified by `sts:GetCallerIdentity` at posture-establishment time returning ARN `arn:aws:iam::729005488042:user/luciel-sandbox-agent`. No prod-touching capability was used at this commit — establishment only.
- **2026-05-23 (Arc 5 Commit 7 — Revision A applied to prod RDS):** The `arc5_a_admin_instance_additive` migration landed on the production RDS instance in a single transactional alembic jump from prior head `b2e5f17a3d9c`, executing the first prod-touching capability under the Commit-6 sandbox-agent posture. **What the business gained today.** The Admin → Instance → Lead schema additions (6 new tables `admins` / `instances` / `instance_composition_grants` / `knowledge_share_grants` / `admin_tier_overrides` / `metering_emissions` + new column `subscriptions.billing_model VARCHAR(16) NULL` backfilled to `'flat'` for all existing Pro rows) are now physically present in prod, unblocking Arc 6 Stripe SKU restructure (which needs `admin_tier_overrides` and `subscriptions.billing_model` to model the Free / Pro / Enterprise + Enterprise-hybrid Price-pair shape), WU-8 Phase A entitlements work (which needs `admin_tier_overrides` and `metering_emissions`), and the Revision B + Revision C work-units (which assume Revision A as the schema baseline). Operationalises Q1 (the data-belongs-to-the-Admin shape now has its physical table in prod), Q5 (the `billing_model` axis is now a column in `subscriptions`), and Q8 (the composition + knowledge-share grant surface is now a physical pair of tables in prod). **Pillar mapping.** Scalability is strengthened (the wide-row `admin_tier_overrides` shape with 22 nullable entitlement axes lets future axes land as column-adds rather than schema redesigns — verified at Commit 5 smoke). Reliability is strengthened (the migration applied transactionally in a single 50-second Fargate task with exit code 0; the verify task confirmed `arc5_a_admin_instance_additive (head)` on the live DB; the `downgrade()` round-trip was proven at Commit 5 Step 4 and remains the rollback escape hatch). Maintainability is strengthened (in-prod schema now matches the canonical Revision A specification recorded at the migration file and the §14 entitlement matrix — no drift between doctrine and reality). Traceability is strengthened (every API call this commit made is in CloudTrail; the runbook `docs/runbooks/arc5-revision-a-prod-apply-and-rollback.md` is the partner-side leg of the audit story; the sibling document changes are explicit below). Security: a deliberate trade was made — the sandbox-agent's inline policy `LucielSandboxArc5MigrateScope` expanded from 8 `Sid` blocks at Commit 6 establishment to **10 `Sid` blocks** at Commit 7 landing, gaining `EcrPushBackendOnly` (scoped to the `luciel-backend` repository only) and `EcsRegisterTaskDef` (necessarily `Resource = "*"` because AWS does not support resource-level scoping on `ecs:RunTask`'s sibling `RegisterTaskDefinition` action — the IAM Console rejected the family-condition attempt with `Invalid Service Condition Key: ecs:family`; the narrow scope-bound lives at the execution side via the already-family-scoped `ecs:RunTask` / `ecs:UpdateService` / `ecs:DescribeTasks` boundary, so the blast radius of the broad RegisterTaskDefinition is essentially zero — an over-broad registration produces a task-def row this principal cannot subsequently execute, mutate, or even describe). The expansion ran through the 5-gate scope-expansion procedure canonical at `D-prod-credential-scope-expansion-protocol-2026-05-23` and is recorded in the runbook's scope-expansion log section. **Why the 22-late entry below is older than this entry above it.** Section 17 ordering is forward-chronological by action date — the 22-late entry was a retrospective doc-truthing correction applied to the canonical doctrine on a date its label preserves; this Commit 7 entry records a forward action and lands at the temporal top of the section. **Sibling document changes at this commit:** `ARCHITECTURE.md` §3.2.8 NEW bullet `Arc 5 Commit 7 — Revision A additive migration applied to prod RDS 2026-05-23` (the system-view — execution topology, IAM topology delta, capability surface after Commit 7, rollback affordance, application-code-unchanged note); `docs/runbooks/arc5-revision-a-prod-apply-and-rollback.md` NEW operational runbook (the procedural-view — full ARNs / digests / task IDs / log streams / rollback procedure preserved, plus the §6 scope-expansion-log section recording the Commit-7 expansion under the 5-gate procedure); `docs/DRIFTS.md` Commit-7-expansion stanza added to `D-prod-credential-scope-expansion-protocol-2026-05-23` recording the first execution of the 5-gate procedure as closure-evidence-of-the-procedure-itself (the drift remains OPEN by design — it is held open as the standing guardrail for all future scope expansions). The closing tag for this work-unit `arc-5-revision-a-smoke-complete` covers the local-smoke (Commit 5) + prod-apply (this Commit 7) pair per the deferred-tag note in the Commit 5 entry above and is cut after this commit lands.
- **2026-05-22-late (Arc 4 tier-shape revision — doc-truthing correction pass):** Tier shape revised from four tiers (Solo / Team / Company / Enterprise) to three tiers (**Free / Pro / Enterprise**), with Enterprise on a **hybrid billing model** (platform fee + included usage + overage; committed-use discount available). Cell bodies were **rewritten in place** across §11 (Q1–Q8 status cells), §14 (prose + tier table + 18-dim entitlement matrix), and §11.5; the v1 4-tier shape is **retired from canonical doctrine** (preserved only in git at `66f6528` / `8c3e0b7` and in the DRIFTS umbrella). Historical rows in §12 (Step 30a.3 / 30a.4 / 30a.5 / 30a.6) and §13 (T1 / T2 / T5 / T10) carry strikethrough-in-place treatment with a single current-truth replacement line above the legacy body — the step rows themselves remain as audit history. Tier rename surface (`admin_id` / `instance_id`) and the Admin → Instance → Lead hierarchy are unchanged. Sibling document changes: ARCHITECTURE §3.2 / §3.2.12 / §3.2.13 / §4.1 / §4.7 receive 2026-05-22-late body rewrites; ARCHITECTURE §3.2.14 NEW (metering hook design + `billing_model` enum: `flat` / `hybrid` / `consumption` on the `subscriptions` table); DRIFTS §3 umbrella `D-tenancy-collapse-admin-instance-lead-2026-05-22` body rewritten to three-tier shape; DRIFTS §3 two NEW drifts (`D-enterprise-metering-not-implemented-2026-05-22` P1 OPEN, `D-free-tier-captcha-missing-2026-05-22` P2 OPEN); arc4-out/A-tier-matrix-detail.md rewritten from 4-tier×18-dim to 3-tier×18-dim + new Enterprise hybrid-billing axis; arc4-out/A-tenancy-collapse-arc-record.md §3 / §6 / §7 / §9 / §12 body-rewritten including the Arc 8-before-Arc 5 sequence correction (Arc 8 security hardening must precede Arc 5 schema migration because Arc 8 touches middleware/audit files Arc 5 will rename). The doctrine of this revision: canonical documents must read as **truth on first read**, not as layered annotation — annotations belong in DRIFTS and arc-records (which ARE audit logs); CANONICAL_RECAP / ARCHITECTURE / DRIFTS must BE the current state.
- **2026-05-23 (Arc 5 Commit 18 — Revision C subtractive migration + ORM FK repoint):** The forward-only Revision C migration `alembic/versions/arc5_c_admin_instance_subtractive.py` was authored and landed on `main` at commit `2b3aa76`, completing the schema design of the Admin → Instance → Lead tenancy collapse. Revision C does five things in a single transaction: (1) drops the seven FKs still pointing at legacy tables — `fk_api_keys_luciel_instance_id`, `fk_knowledge_embeddings_luciel_instance_id`, `fk_memory_items_luciel_instance_id`, `fk_conversations_tenant_id_tenant_configs`, `fk_identity_claims_tenant_id_tenant_configs`, `fk_scope_assignments_tenant_id_tenant_configs`, and the PG-auto-named FK on `user_invites.tenant_id` (discovered at runtime via `information_schema` introspection because no explicit `name=` was set when the table landed); (2) re-creates them with stable explicit names pointing at the V2 tables `instances.id` / `admins.id`, preserving the column names (`luciel_instance_id`, `tenant_id`) so app-layer code does not need a rename; (3) drops the five legacy tables in leaf-first order with CASCADE: `agents`, `agent_configs`, `domain_configs`, `luciel_instances`, `tenant_configs`; (4) drops the V1 back-pointer columns and their partial-unique indexes: `admins.legacy_tenant_id`, `instances.legacy_luciel_instance_id`, `instances.legacy_agent_id`; (5) tightens the `admins.tier` CHECK by dropping the transitional `ck_admins_tier_valid_during_migration` (which accepted both legacy and V2 tier values during Revision B) and creating the final `ck_admins_tier_valid` restricted to the V2 vocabulary `('free', 'pro', 'enterprise')` only. The migration is **forward-only by design** — `downgrade()` raises `NotImplementedError` to make this explicit; rollback path is the RDS snapshot taken immediately before `alembic upgrade head` runs Revision C. Companion ORM repoint at this same commit re-points the seven ORM FK declarations in `app/models/api_key.py`, `app/models/conversation.py`, `app/models/identity_claim.py`, `app/models/knowledge.py`, `app/models/memory.py`, `app/models/scope_assignment.py`, `app/models/user_invite.py` from the legacy tables to `instances` / `admins` so the live ORM matches the post-Revision-C schema (the relationship surface in `knowledge.py` was also simplified to drop the legacy_luciel_instance_id back-pointer indirection now that the FK is direct). Verification: `alembic` revision graph reports a single head at `arc5_c_admin_instance_subtractive` chained to `arc5_b_admin_instance_cutover`; `DATABASE_URL=postgresql://stub python3 -c 'import app'` exits 0. **Pillar mapping.** Maintainability is strengthened (no more dual-schema bookkeeping after Revision C runs in prod; one tenancy hierarchy, one tier vocabulary, one set of FK targets). Scalability is strengthened (the legacy tables go to zero rows in storage and zero references in catalog after Revision C; future arcs add to the V2 surface only). Reliability and traceability: the migration is gated behind RDS snapshots per the Revision B+C runbook \u00a73.3 / \u00a73.5 / \u00a73.6; the forward-only contract is explicit. Security: no new IAM surface at this commit. **Sibling document changes:** this CANONICAL \u00a717 entry; `docs/runbooks/arc5-revision-b-c-prod-apply-and-rollback.md` (the planned-execution runbook landed at the next commit, Commit 19); no DRIFTS updates required (Revision C's forward-only nature was authorised at the Arc 5 Commit 2 / 22-late doctrine framing and is recorded at `D-tenancy-collapse-admin-instance-lead-2026-05-22`).
- **2026-05-23 (Arc 5 Commit 19 \u2014 Revisions B+C prod apply & rollback runbook):** New Posture B operational satellite `docs/runbooks/arc5-revision-b-c-prod-apply-and-rollback.md` (commit `8de8c3c`) documents the planned PROD execution of Arc 5 Revisions B and C in a single coordinated window, mirroring the structure of the Revision A runbook and anchored from this CANONICAL \u00a717 entry per Posture B. Sections: (1) scope and forward-only contract; (2) pre-execution state to confirm at apply time; (3) planned execution sequence with explicit commit + snapshot boundaries (3.1-3.2 buildah build + ECR push; 3.3 pre-B snapshot; 3.4 ECS rolling deploy with the new image; 3.5 `alembic upgrade arc5_b_admin_instance_cutover` + post-B snapshot + Revision B sanity probe; 3.6 `alembic upgrade head` Revision C + post-C snapshot; 3.7 V2 schema verification probe + live read-path smoke); (4) rollback paths per failure point (ECS rollback for app-layer issues; RDS snapshot restore for schema-layer issues; no Alembic downgrade for Revision C by design); (5) post-execution closure list (CANONICAL / ARCHITECTURE / DRIFTS updates, arc-5-execution-record.md, tag `arc-5-tenancy-collapse-complete`); (6) credential posture (env-var-only, SSM-resident DB URL, retained Commit-7 IAM scope). **Smoke posture note recorded in \u00a71:** the sandbox has no docker daemon and no pgvector container, so the pre-prod wet smoke is deferred to the PROD replay itself, gated by per-step RDS snapshots. The introspection-based design of both migrations (Revision B reads legacy tables and writes V2; Revision C uses `information_schema` for the one PG-auto-named FK on `user_invites`) makes this safe under the snapshot gate. **Pillar mapping.** Reliability and traceability are strengthened (the runbook is the partner-side leg of the audit story for the prod-touching commits that follow). Maintainability: the runbook explicitly catalogues the 5 SQL verification assertions and the 4 SQL sanity assertions so the verification logic is reviewable independent of the migration code.
- **2026-05-23 (Arc 5 Commit 20 \u2014 Build + ECR push `luciel-backend:arc5-bc-8de8c3c`):** Image built with `buildah --isolation chroot --storage-driver vfs` against `Dockerfile` at repo HEAD `8de8c3c2e4ddf2b36e9952bf66e78ebf78b4a084` on `main`, with `--build-arg BUILD_GIT_SHA=8de8c3c` baked into the image as `ENV` so `/api/v1/version` reports the deployed SHA at runtime. Pre-push verification inside the built image confirmed: `arc5_a_admin_instance_additive.py`, `arc5_b_admin_instance_cutover.py`, and `arc5_c_admin_instance_subtractive.py` all present at `/app/alembic/versions/`; alembic script_directory HEADS == `['arc5_c_admin_instance_subtractive']`; `DATABASE_URL=postgresql://stub python -c 'import app'` exits 0. Push target `729005488042.dkr.ecr.ca-central-1.amazonaws.com/luciel-backend:arc5-bc-8de8c3c`, digest `sha256:1184e1e912b2b4531fc320e07f0e7dbd69f13d0159d9a621f438696c64a9d4f6`, size 222.5 MB, pushed at `2026-05-23T20:10:57Z`. ECR auth via `buildah login --password-stdin` from `boto3 ecr.get_authorization_token` (token decoded in-memory, never written to disk). IAM scope: `LucielSandboxArc5MigrateScope` (retained from Arc 5 Commit 7). Commit `0b501c4` is an empty-tree commit \u2014 the image is a binary artifact in ECR, not in git; the local buildah store at `/home/user/workspace/.buildah` is not committed.
- **2026-05-23 (Arc 5 Commit 21 — Sandbox credential scope expansion for RDS snapshot capability + pre-Revision-B RDS snapshot):** The sandbox-agent's `LucielSandboxArc5MigrateScope` inline policy expanded from the post-Commit-7 10-`Sid` shape to **11 `Sid` blocks** with the addition of `RdsSnapshotLucielDbOnly` (Actions `rds:CreateDBSnapshot`, `rds:DescribeDBSnapshots`, `rds:AddTagsToResource`; Resource scoped to `arn:aws:rds:ca-central-1:729005488042:db:luciel-db` + `arn:aws:rds:ca-central-1:729005488042:snapshot:luciel-arc5-*`). The expansion satisfies all 5 gates of `D-prod-credential-scope-expansion-protocol-2026-05-23`: (Gate 1 Necessity) TODO #12 requires three RDS snapshots (pre-B, post-B, post-C) per the Revision B+C runbook \u00a73.3 / \u00a73.5 / \u00a73.6 for the snapshot-gated rollback path; (Gate 2 Minimality) Resource is scoped to the single `luciel-db` DB instance and the `luciel-arc5-*` snapshot-name prefix \u2014 the principal cannot snapshot any other instance, cannot list snapshots outside the Arc-5 prefix, and crucially **cannot delete or restore snapshots** (no `DeleteDBSnapshot` / `RestoreDBInstanceFromDBSnapshot`); (Gate 3 Surfacing) the 4 surfacing artifacts (need-TODO anchor, copy-pasteable JSON Sid block, blast-radius analysis, three rejected tighter alternatives with rejection rationale) were presented via `ask_user_question` at 4:15 PM EDT; (Gate 4 Approval) partner approved verbatim `"Approved \u2014 I'll add the Sid block in Console now"` and confirmed the saved policy state at 4:31 PM EDT by echoing the full 11-block JSON; (Gate 5 Audit trail) this CANONICAL \u00a717 entry + `docs/runbooks/aws-sandbox-credential-posture.md` \u00a78 expansion-log entry land in the single Commit 21 atomic commit. **Capability used at this same commit:** pre-Revision-B RDS snapshot `luciel-arc5-pre-revision-b-20260523203158` (ARN `arn:aws:rds:ca-central-1:729005488042:snapshot:luciel-arc5-pre-revision-b-20260523203158`) created at `2026-05-23T20:32:04Z`, allocated storage 20 GB, engine `postgres 16.13`. **Blast-radius analysis (worst case agent malfunction / credential compromise):** attacker can create arbitrary snapshots of `luciel-db` and list snapshots matching `luciel-arc5-*`; exposure is storage cost only; no data-loss or availability exposure because deletion and restore actions remain outside the principal's policy. **Tighter alternatives rejected (full record in the expansion-log entry):** ECS-pg_dump-to-S3 (rejected: not point-in-time consistent under load; weakens recovery guarantee); skip-snapshots-rely-on-automated-backups (rejected: ~24h granularity vs the per-step snapshots the runbook needs); resource-scope-to-DB-only (rejected: `DescribeDBSnapshots` needs the snapshot ARN in Resource for status polling). The drift `D-prod-credential-scope-expansion-protocol-2026-05-23` remains OPEN by design as the standing guardrail for all future scope expansions.
- **2026-05-23 (Arc 5 Commits 22–27 — Revisions B + C applied to PROD and Arc 5 tenancy-collapse closure):** The Admin → Instance → Lead schema collapse landed end-to-end in production today. **Commits 22–25 (Revision B + C apply path).** Revision B (`arc5_b_admin_instance_cutover.py`) backfilled the V2 `admins` and `instances` rows from the legacy `tenant_configs` / `luciel_instances` data with four sequential hotfixes against the first apply attempt: (22) `admins.name` was missing from the INSERT statement (Revision A creates the column `name` not `display_name`), `instances.instance_slug` was missing from the INSERT, the `legacy_agent_id Integer` column needed an explicit `CAST(... AS varchar(100))` against a source `String(100)` column, and the `tier_source` value violated the `ck_admins_tier_source_valid` CHECK constraint vocab. (23) The two `admin_audit_logs` audit-row blocks inside Revision B referenced the wrong real schema (the live table uses `actor_label` / `resource_natural_id` / `after_json` not `actor_user_id` / `resource_id` / `payload`) and the Python-level `try/except` was leaving the PostgreSQL transaction in `InFailedSqlTransaction` state — per the aggressive-cleanup doctrine, both audit blocks were dropped entirely (traceability preserved via alembic_version, the three RDS snapshots, the git commits 22–26, and the Step 28 p3e2 application audit-chain hash chain that records every row write at runtime). Post-B sanity probe: 23 admins all `tier=free`, `tier_source=revision_b_backfill`, 1 instance with `legacy_luciel_instance_id=129`, 23 tenant_configs active + 1 luciel_instance active (legacy and V2 row counts match). (24) Revision C (`arc5_c_admin_instance_subtractive.py`) first attempt hit a `ForeignKeyViolation` on `fk_api_keys_luciel_instance_id` because 110 api_keys rows pointed at a `luciel_instance_id=1` that was never backfilled into the V2 `instances` table, plus 143 scope_assignments orphans (74 distinct tenant_ids, mostly `step24-5b-p12/p14` test fixtures), 6 user_invites orphans, and 204 knowledge_embeddings orphans. A pre-FK-cutover orphan-purge block was added at the start of Revision C: NULL-out for nullable FKs (api_keys 110, knowledge_embeddings 204, memory_items 0), DELETE for NOT-NULL FKs (scope_assignments 101, user_invites 6, conversations 0, identity_claims 0). (25) Revision C second attempt landed the schema body successfully but exit-1'd at alembic's own UPDATE: the revision id `arc5_c_admin_instance_subtractive` is 33 chars and the `alembic_version.version_num` column was `VARCHAR(32)`; the first statement of Revision C now widens it to `VARCHAR(255)`. Revision C third attempt **exited 0 cleanly**. **Final V2 verification probe.** alembic head = `arc5_c_admin_instance_subtractive` (255-char column), all 5 legacy tables gone (`agents`, `agent_configs`, `domain_configs`, `luciel_instances`, `tenant_configs`), all 3 V1 back-pointer columns gone (`admins.legacy_tenant_id`, `instances.legacy_luciel_instance_id`, `instances.legacy_agent_id`), all 7 newly-named V2 FKs present (`fk_api_keys_luciel_instance_id`, `fk_conversations_tenant_id_admins`, `fk_identity_claims_tenant_id_admins`, `fk_knowledge_embeddings_luciel_instance_id`, `fk_memory_items_luciel_instance_id`, `fk_scope_assignments_tenant_id_admins`, `fk_user_invites_tenant_id_admins`), `admins.tier` CHECK tightened from `ck_admins_tier_valid_during_migration` (transitional, accepted V1 + V2 vocab) to final `ck_admins_tier_valid` (V2 vocab only: `free` / `pro` / `enterprise`), and `admins.tier_source` CHECK in place at `ck_admins_tier_source_valid`. Three RDS snapshots flank the apply window for snapshot-gated rollback: `luciel-arc5-pre-revision-b-20260523203158`, `luciel-arc5-post-revision-b-20260523205901`, `luciel-arc5-post-revision-c-20260523211325` (all `available`). ECR images produced this session: `arc5-bc-d84dba4` (Commit 22), `arc5-bc-920dba6` (Commit 23), `arc5-bc-1d45867` (Commit 24), **`arc5-bc-9f1af13` (Commit 25 — the schema-final image)**, `arc5-bc-1e68f3f` (Commit 26 — runtime stale-import sweep). **Commit 26 (runtime stale-import sweep).** The first attempted ECS rolling deploy of `arc5-bc-9f1af13` to `luciel-backend-service` boot-crashed with `ImportError: cannot import name 'ParentScopeInactiveError' from 'app.services.instance_service'` — the container-import-test gate at B5 (`DATABASE_URL=postgresql://stub python -c 'import app'`) short-circuits before `app.main`'s transitive import graph and therefore missed four stale references that survived the V2 collapse: (i) `app/api/v1/admin.py:57` imported retired `ParentScopeInactiveError` (unused at runtime, deleted); (ii) `app/api/v1/billing.py:389/421` imported `EmailDeliveryError` from `email_service` which only exports `WelcomeEmailError` / `MagicLinkError` / `RefundEmailError` (the OnboardingClaimResponse path only calls `send_welcome_set_password_email`, so swapped to `WelcomeEmailError`); (iii) `app/services/invite_service.py:97` imported `_slugify_agent_id_from_email` from the retired `tier_provisioning_service` helper — the single caller (invite-redeem audit-row payload) is legacy-only so a local helper was inlined to own the contract at the call site; (iv) `app/runtime/context_assembler.py:1` imported retired `LUCIEL_IDENTITY` constant — replaced with the canonical `build_system_prompt()` layer-builder which already drives the rest of the runtime. An AST-based full-repository sweep over all `from app.* import ...` statements confirmed no other stale references survive. **Commit 27 (rolling deploy + doctrine close).** The 5-gate scope-expansion protocol triggered for the final deploy step (`ecs:UpdateService` was outside `LucielSandboxArc5MigrateScope` and the user's per-principal inline-policy ceiling at 2048 non-whitespace chars was already saturated). Partner approved verbatim `"I will approve the scope expansion can you provide me with the online policy that I have to attach partner"` and `"attached partner"` after the policy attachment; the expansion landed as a **new managed policy** `LucielSandboxArc5EcsRollingDeploy` (rather than a new Sid in the inline policy), 2 actions (`ecs:UpdateService`, `ecs:DescribeServices`), Resource scoped to the two service ARNs only (`luciel-backend-service` + `luciel-worker-service`). Backend rolled to `luciel-backend:85` and worker rolled to `luciel-worker:39` (both off image `arc5-bc-1e68f3f`); both services reached `COMPLETED` rollout state with a single PRIMARY deployment; ALB target-group health checks (`GET /health`) returned `200 OK` continuously after the new backend task registered at 21:48:17 UTC; uvicorn boot logs show `Application startup complete`, LLM provider registry initialized (openai + anthropic), Redis rate-limit storage ready, and the celery step29y producer warmup OK. Worker explicit container `healthStatus=HEALTHY`. **What the business gained today.** The schema half of the Arc 4 tenancy collapse is now complete in production — the V1 `Tenant → Domain → Agent → Lead` four-level shape is physically gone from RDS and the V2 `Admin → Instance → Lead` three-level shape is the only shape that exists. The application runtime is serving customer traffic against the V2 schema with the V2 ORM models and zero stale-import boot crashes. This unblocks the entire downstream arc: Arc 6 Stripe SKU restructure (Free + Pro + Enterprise hybrid) can now physically land its Price-pair shape against the `subscriptions.billing_model` column and `admin_tier_overrides` table that are populated; Arc 8 WU-7 / WU-8 security hardening and Enterprise-metering work can land against the final schema without coordinating with a pending rename; Phase 6 Live E2E user-stories replay (the close of the Step 28 verification arc) can resume against a single canonical schema. **Pillar mapping.** Traceability: each commit 22–27 is anchored in this §17 entry + the runbook ACTUAL RUN sections + the three RDS snapshots + the CloudTrail records for every sandbox-agent prod-touching API call + the Step 28 p3e2 application audit-chain hash chain at runtime. Reliability: the snapshot-gated rollback path was real and used by design — each migration apply attempt that failed was recovered by reading the post-A snapshot state without touching the snapshot itself; the rollout protocol successfully isolated the runtime crash to one image revision and surfaced it via ALB health-check failure rather than letting it propagate through a steady-state deployment. Maintainability: the four runtime stale-import bombs disarmed at Commit 26 are exactly the kind of latent V2-collapse residue the aggressive-cleanup doctrine commits to removing, and the AST sweep proves no others survive. Scalability: the V1 noun set is gone from the physical schema, so future migrations land cleanly without conditional V1/V2 branches in service code. Security: the IAM scope expansion at Commit 27 was a brand-new managed policy (rather than an inline-policy widening) with 2 actions / 2 ARN resources, and is detachable cleanly post-Arc-5 — the smallest possible footprint for the smallest possible time window. The IAM ceiling discovery (2048 non-whitespace chars per-principal inline) is a known AWS limit that has not previously bound this work; the managed-policy approach is the canonical solution and survives all future scope expansions without further ceremony. **Drift closures.** Umbrella drift `D-tenancy-collapse-admin-instance-lead-2026-05-22` is **CLOSED** by this commit (struck through in DRIFTS §3; closure evidence: full V2 verification probe output preserved in arc-5-execution-record and in the snapshot trio above). Drift `D-prod-credential-scope-expansion-protocol-2026-05-23` remains OPEN by design as the standing guardrail — the Commit 27 expansion is the third invocation of the gate (after Commit 7's EcrPushBackendOnly + EcsRegisterTaskDef expansion and Commit 21's RdsSnapshotLucielDbOnly expansion) and all three are recorded in its expansion-log. **New sibling drift opened at Commit 26 (queued, P3):** the container-import-test gate at B5 missed the four stale references because it only does `import app` (a shallow import that does not exercise `app.main`'s transitive graph); the gate is being strengthened to also exercise the uvicorn app-load path (`uvicorn.importer.import_from_string('app.main:app')` under a stub `DATABASE_URL`) so that future Revisions land their boot-crash failures at CI time, not deploy time. Tag `arc-5-tenancy-collapse-complete` lands at this commit. **Phase 6 Live E2E user-stories replay is the next work-unit, gated on this commit; partner can pause here or continue inline.**
- **2026-05-23 (Arc 6 Commit 2 — Sandbox credential scope expansion for Stripe production SSM read/write + KMS via SSM via customer-managed policy `LucielSandboxStripeScope`):** The sandbox-agent gained the capability needed to drive Arc 6's Stripe Live wipe (this commit's destructive sub-step) and Arc 6 Commit 4's Stripe mint + 4 new SSM `PutParameter` calls. The expansion is delivered as a **customer-managed policy** (not a second inline policy) named `LucielSandboxStripeScope` with ARN `arn:aws:iam::729005488042:policy/LucielSandboxStripeScope` attached to IAM user `luciel-sandbox-agent`. Three new `Sid` blocks: (1) `Arc6SsmReadStripeProduction` — `ssm:GetParameter`, `ssm:GetParameters` on `arn:aws:ssm:ca-central-1:729005488042:parameter/luciel/production/stripe_*`; (2) `Arc6SsmWriteStripeProduction` — `ssm:PutParameter`, `ssm:AddTagsToResource` on the same Resource; (3) `Arc6KmsViaSsmForStripeProduction` — `kms:Decrypt`, `kms:Encrypt`, `kms:GenerateDataKey` on `Resource: "*"` constrained by `Condition: StringEquals: kms:ViaService = ssm.ca-central-1.amazonaws.com` (the CMK is only usable through SSM, never directly). **Why customer-managed not inline.** Appending the 3 new Sids to the Arc 5 inline policy `LucielSandboxArc5MigrateScope` would have produced a 2640-char minified body, exceeding the IAM inline-user-policy 2048-char hard limit (128.9% utilisation). The Arc 5 inline policy is therefore **byte-identical-unchanged** at its Arc 5 Commit 21 final 11-Sid form; the 3 Arc 6 Sids land in a structurally separate customer-managed policy (646 chars / 10.5% of the 6144-char customer-managed limit) that has its own version history (IAM retains up to 5 versions), is independently revocable without touching the Arc 5 surface, can be reused on future principals (e.g. a Stripe-only operator role minted later), and shows up in IAM Console under a stable named ARN. **Preflight miss honestly logged.** `arc6-out/A-arc6-preflight.md` initially asserted "no IAM expansion expected at Arc 6" — that claim was wrong: the Arc 5 inline policy had zero SSM `Sid`s and zero KMS `Sid`s (Arc 5's prod surface was Alembic + ECS-deploy + ECR push + RDS snapshot only; the sandbox-agent never touched Stripe credentials — only the ECS task-execution-role does, and that's a different principal). The miss was caught by the Commit 2 first call to `boto3 ssm.get_parameter` against `/luciel/production/stripe_secret_key` returning `AccessDeniedException` BEFORE any destructive Stripe action. Per the aggressive-cleanup doctrine + the standing scope-expansion protocol drift `D-prod-credential-scope-expansion-protocol-2026-05-23`, the 5-gate procedure was triggered inline. **5-gate satisfaction.** (Gate 1 Necessity) Arc 6 Commit 2 needs to read `STRIPE_SECRET_KEY` to drive the wipe + read the 6 old Price IDs for the audit record + write to SSM at Commit 4 to land 4 new Price IDs (`stripe_price_pro_monthly`, `stripe_price_pro_annual`, `stripe_price_enterprise_floor_annual`, `stripe_price_enterprise_metered_unit`); (Gate 2 Minimality) all three Sids are resource-scoped to `parameter/luciel/production/stripe_*` (the 9-key Stripe namespace) — the principal cannot read or write any other prod secret (`database-url`, `magic-link-secret`, `JWT_*`, `REDIS_URL`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `SES_SNS_TOPIC_ARN`, `platform-admin-key`, `worker_database_url`); KMS is gated by `kms:ViaService=ssm.ca-central-1.amazonaws.com` so the CMK cannot be used outside SSM; (Gate 3 Surfacing) four artifacts presented to partner inline before any prod work: TODO anchor (Arc 6 Commit 2 + Commit 4), proposed `Sid` JSON, blast-radius analysis, 4 tighter-alternatives-rejected (full text at `docs/runbooks/aws-sandbox-credential-posture.md` §8 Arc 6 entry); (Gate 4 Approval) partner approved verbatim `"I will give you the scope exapnsion partner"` (6:14 PM EDT) and then created the policy via IAM Console `"okay partner I have created the policy for you: LucielSandboxStripeScope"` (6:26 PM EDT); (Gate 5 Audit trail) this CANONICAL §17 entry + `docs/runbooks/aws-sandbox-credential-posture.md` §3-bis (NEW) + §8 expansion-log entry + `arc6-out/A-arc6-preflight.md` §7 update + `ops/iam/LucielSandboxStripeScope.json` + `ops/iam/arc6_stripe_ssm_scope_expansion.json` all land in this single Commit 2 paperwork-half atomic commit. **Verification (sandbox-side, post-attach).** Four `boto3.client('ssm')` probes at ≈22:26 UTC 2026-05-23 all PASSED: (1) `get_parameter` on `/luciel/production/stripe_price_intro_fee` → `price_*`-prefix value (30 chars), confirming SSM read on the Stripe namespace; (2) `get_parameter WithDecryption=True` on `/luciel/production/stripe_secret_key` → `sk_live_*`-prefix value (107 chars), confirming SSM read + KMS-via-SSM decrypt; (3) `put_parameter Type=SecureString Overwrite=True` to `/luciel/production/stripe_arc6_iam_probe` with value `arc6-iam-write-probe-2026-05-23-1826-EDT-noop` → HTTP 200, confirming SSM write + KMS-via-SSM encrypt; (4) round-trip `get_parameter` on the probe param → exact byte-for-byte match, confirming the full encrypt+decrypt cycle. CloudTrail captures all four API calls under principal `arn:aws:iam::729005488042:user/luciel-sandbox-agent` with `eventTime ≈2026-05-23T22:26:00Z`, constituting the second-layer audit anchor. The probe parameter `/luciel/production/stripe_arc6_iam_probe` is left in SSM through Arc 6 close and cleaned up at Commit 11 doctrine-close as part of the Arc 6 sediment-sweep. **Pillar mapping.** Security is consciously traded (the sandbox principal can now read the Stripe Live secret key — the same exposure the running ECS container has at every container start; with the `kms:ViaService` constraint, the CMK cannot be abused outside SSM; with the resource-scoped `stripe_*` namespace, all 9 other production SecureStrings remain unreadable). Traceability is strengthened (every `GetParameter` / `PutParameter` against `/luciel/production/stripe_*` is now CloudTrail-recorded under a single named principal, mirrored against the runbook ACTUAL RUN sections that will populate at Commit 2's destructive half and Commit 4). Reliability is strengthened (the wipe + mint surface can be driven from the sandbox without a partner-paste cycle for each Stripe API call, eliminating a paste-fatigue failure mode). Maintainability is strengthened (a single named customer-managed policy ARN is the canonical audit handle for the Stripe-side capability — future Arc 8 metering work doesn't need to expand this; the same policy already covers reads on any future `/luciel/production/stripe_*` path that lands). Scalability is unchanged. **Drift posture.** `D-prod-credential-scope-expansion-protocol-2026-05-23` remains OPEN by design as the standing guardrail; this expansion exercised it cleanly. The Arc 5 inline policy remains canonical at `ARCHITECTURE.md` §3.2.8; the Arc 6 customer-managed policy is anchored from this entry into `docs/runbooks/aws-sandbox-credential-posture.md` §3-bis (Posture B satellite). **Commit hash: this commit's paperwork-half.** The destructive sub-step (cancel 23 internal/test Stripe Live subs + archive 6 old Prices + write audit record `arc6-out/C-arc6-stripe-wipe-record.md`) is held until partner approval of this paperwork-half.
- **2026-05-23 (Arc 6 Commit 2 destructive-half — V1 SKU teardown in Stripe Live):** The destructive sub-step held at the paperwork-half above executed against Stripe Live account `acct_1TX2BmRytQVRVXw7` in a single ~3-second window at 22:57 UTC. **Prestate triangulation closed.** The Arc 6 brief's "23 internal/test subs in Stripe Live" was found at runtime to be a mixed-object count: 10 `Subscription` objects all `status=canceled` (terminal) + 13 `Checkout.Session` objects (10 `complete` that became the canceled subs + 3 `expired` that never minted subs) = 23 — recorded in `arc6-out/C-arc6-stripe-wipe-record.md` §2. Zero live (non-terminal) subscriptions existed at prestate; Stripe does not allow deletion of past Subscriptions or Checkout.Sessions in Live mode, so the strongest destructive action available — and the one needed to clear the V1 SKU surface for V2 mint at Commit 4 — is archival of Prices and Products. **Nine archive mutations executed (all `archived_now`, all `pre_active=true → post_active=false`).** Six V1 Prices (Resource IDs preserved for audit replay): `price_1TX3MGRytQVRVXw7TPKQiCAS` (`individual_monthly`, $30 CAD/mo), `price_1TX3MGRytQVRVXw7408HJhsG` (`individual_annual`, $300 CAD/yr), `price_1TX3MHRytQVRVXw7LazGyxZH` (`team_monthly`, $300 CAD/mo), `price_1TX3MHRytQVRVXw7pVbzGSX0` (`team_annual`, $3,000 CAD/yr), `price_1TX3MIRytQVRVXw79cYZwjVw` (`company_monthly`, $2,000 CAD/mo), `price_1TX3MIRytQVRVXw7b7qXMaX1` (`company_annual`, $20,000 CAD/yr). Three V1 Products: `prod_UW5XLqvK0A2PKl` (Luciel Individual), `prod_UW5XMLeRBpgCJH` (Luciel Team), `prod_UW5XobSmV02qQp` (Luciel Company). **Control held.** Intro-fee Price `price_1TXNmnRytQVRVXw7GGfyJiaj` ($100 CAD one-time) and parent Product `prod_UWQeeXJ3CwJQYu` (Luciel Pilot Intro Fee) both verified still `active=true` by post-archive control assertion — the script aborts loudly if either had been touched. **Safety properties.** The archive script `scripts/_arc6_stripe_archive_v1.py` is **idempotent** (re-runs emit `action="already_archived"` with zero state change), enforces `sk_live_` mode before any mutation, performs a pre-mutation sanity check that the 6 Prices map to exactly the 3 expected Products (aborts on drift), and runs the post-mutation control assertion as a belt-and-suspenders backstop. Order is Prices first then Products (child rows before parent rows — canonical pattern for clean audit trail). **Prod-credential posture held end-to-end.** AWS sandbox creds exported via env var only (never written to disk, never echoed, never passed to subagents) per the standing posture canonical at `docs/runbooks/aws-sandbox-credential-posture.md` §4 paste protocol; `sts:GetCallerIdentity` confirmed principal `arn:aws:iam::729005488042:user/luciel-sandbox-agent` before any Stripe action; Stripe `sk_live_*` key loaded from SSM `/luciel/production/stripe_secret_key` via `scripts/_arc6_load_stripe_env.sh` directly into env (loader prints only the `sk_live_` prefix, never the value). All execution inline per the due-diligence directive — zero subagents spawned this commit. **What this does NOT touch.** Stripe's immutable terminal records remain in the account by design: the 10 canceled Subscription rows, the 13 Checkout.Session rows, the 10 test Customer rows (`aryans.www+*@gmail.com` aliases), and all historical Invoices / BalanceTransactions / Charges / PaymentIntents are preserved as the read-only audit trail. Customers can be archived later if symmetry is wanted; the schema-side Free / Pro / Enterprise mint at Commit 4 does not depend on Customer archival. **Pillar mapping.** Maintainability is strengthened (the V1 SKUs cannot become a `ship-out-vs-vision` drift since they are now non-selectable in Stripe — checkout sessions referring to an archived Price are rejected by Stripe with `resource_missing`; the runtime cannot accidentally bind a new customer to an obsolete tier). Traceability is strengthened (full prestate + poststate JSON evidence preserved at `arc6-out/C-arc6-stripe-prestate.json` and `arc6-out/C-arc6-stripe-poststate.json`; CloudTrail recorded every SSM `GetParameter` against `/luciel/production/stripe_*` and every Stripe API call is recorded by Stripe itself with `acct_1TX2BmRytQVRVXw7` and timestamp). Reliability is strengthened (the V2 mint at Commit 4 can run against a clean SKU surface — no name collisions with V1 SKUs and no risk of a stale V1 Price being picked up by a misconfigured client; SSM params under `/luciel/production/stripe_price_*` will be overwritten at Commit 4 to point at the new V2 Price IDs, and the current values are preserved in the prestate JSON for audit replay). Scalability and security are unchanged. **Files in commit `2995d8d`:** `arc6-out/C-arc6-stripe-prestate.json` (prestate evidence, 113 lines), `arc6-out/C-arc6-stripe-poststate.json` (poststate evidence, 112 lines), `arc6-out/C-arc6-stripe-wipe-record.md` (full audit record, 187 lines), `scripts/_arc6_stripe_enumerate_prestate.py` (read-only enumerator, re-runnable), `scripts/_arc6_stripe_archive_v1.py` (idempotent archive script, re-runnable for evidence regeneration). Commit `2995d8d` pushed to `origin/main` at 2026-05-23 23:00 UTC after partner push approval. **Unblocks** Commit 3 (admin_widget_domains migration authoring — independent) and Commit 4 (V2 mint + 4 SSM puts overwriting current `/luciel/production/stripe_price_*` to V2 IDs). The prestate triangulation is the durable lesson: future-arc briefs that name an object count should be re-triangulated at runtime before any destructive action, because Stripe's object model bundles Subscription + Checkout.Session + Customer + Invoice into one user-facing "subscription" but they are four separate Resources from the API surface.
- **2026-05-23 (Arc 6 Commit 3 — admin_widget_domains migration authored, prod apply deferred to Commit 10b):** The Arc 6 schema half landed as a **forward-only Alembic migration authored in repo and committed locally**, with the actual `alembic upgrade head` execution against prod RDS held until Commit 10b after the new backend image carrying the migration deploys via ECS rolling deploy. This split mirrors the established prod-migration pattern in the repo (Arc 5 Revisions A/B/C, Arc 8 WU-6 SES chain `a91c4d2e7f08` → `b2e5f17a3d9c`, Step 24.5b chain) where every prod migration applies via ECS-exec / `ecs run-task` against a running backend container that already contains the migration — not from the sandbox directly. **Judgement-call rationale recorded (partner-entrusted).** Applying from the sandbox would have required either (a) a 5-gate scope expansion for RDS network reach + worker DB URL disk touch (rejected: no precedent, breaks prod-credential posture held all session) or (b) folding buildah/ECR/deploy work forward into Commit 3 (rejected: breaks commit boundaries and the pause-between-commits discipline). Splitting authoring from application keeps each commit doing one thing well, matches repo precedent, and produces a single observable transition at Commit 10b where new image + new schema activate atomically. **Migration shape.** Revision id `arc6_a_admin_widget_domains`; down_revision `arc5_c_admin_instance_subtractive` (the single Arc 5 leaf — verified by sweeping all 42 pre-existing `alembic/versions/*.py` files for `revision = "..."` and `down_revision = "..."` declarations and confirming the leaves-set is `{arc5_c_admin_instance_subtractive}` with zero orphans). Post-mint the alembic graph has 43 revisions and a single head at `arc6_a_admin_widget_domains`. The migration creates one table `admin_widget_domains` with columns `id BIGINT IDENTITY PRIMARY KEY`, `admin_id VARCHAR(100) NOT NULL` referencing `admins(id)` `ON DELETE CASCADE` (FK constraint name `fk_admin_widget_domains_admin_id`), `domain VARCHAR(253) NOT NULL` (RFC 1035 max hostname length), `created_at` and `updated_at` `TIMESTAMPTZ NOT NULL DEFAULT now()`, plus `UNIQUE (admin_id, domain)` named `uq_admin_widget_domains_admin_id_domain`, `CHECK (domain = lower(domain))` named `ck_admin_widget_domains_domain_lowercase`, `CHECK (length(domain) > 0 AND domain !~ '[[:space:]]')` named `ck_admin_widget_domains_domain_shape`, and lookup index `ix_admin_widget_domains_admin_id ON (admin_id)`. **FK shape — locked decision (partner-entrusted).** Option taken: `admin_id VARCHAR(100) → admins(id) ON DELETE CASCADE`. Reasoning recorded inline in the migration's module docstring and in this entry: (i) `admins.id` is `VARCHAR(100)` semantic slug, not BIGINT — confirmed by reading `app/models/admin.py` — so the FK must be `VARCHAR(100)` to match; (ii) the V2 Free tier has no Stripe row by design (CANONICAL §11.7 numeric lock), so tying allowlists to `subscription_id` would either require a synthetic "free subscription" row (rejected in Commit 1) or break the Free tier flow entirely; (iii) the noun is already V2 (`admin_id`, not `tenant_id`) so the Tenant→Admin rename at Commit 6 has zero touch on this table — born inside Arc 6 with canonical vocab; (iv) `ON DELETE CASCADE` is correct because domain rows have no audit value separate from the Admin they belong to (unlike `scope_assignments` and `user_invites`, which use `ON DELETE RESTRICT` because they represent durable user-side state); (v) the option-3 belt-and-suspenders shape (both `admin_id` and nullable `subscription_id`) was rejected by the aggressive-cleanup doctrine — two FKs with unclear conflict resolution is exactly the latent ambiguity Path A commits to removing. **Tier limit enforcement.** The CANONICAL §14 entitlement matrix's Pro-tier cap on domain count is enforced at the **app layer**, not the schema layer — the schema is tier-agnostic so future tier-axis changes (e.g., Enterprise hybrid metered domains) land as app-layer config rather than as schema changes. **ORM partner.** `app/models/admin_widget_domain.py` (NEW, 100 lines) mirrors the table with explicit lowercase + non-empty CHECK reproductions in `__table_args__`, the cascade-delete FK declaration, the composite UNIQUE, and the lookup Index. Exported from `app/models/__init__.py` (NEW import line + `__all__` entry, the existing `Instance` import is preserved). Both files plus the migration parse cleanly under `ast.parse` (sandbox-side syntactic verification — full app-import smoke is deferred to Commit 10 buildah pre-push verification per repo precedent). **Doctrine guardrail caught at authoring time.** The naive "set of revisions minus set of down_revisions" leaf-detection script initially reported two heads because it failed to match the modern `revision: str = "..."` Alembic form (matched only `revision = "..."`). Re-run with a corrected regex against all 42 files confirmed the single head `arc5_c_admin_instance_subtractive`. The lesson: any future alembic-graph sweep must accept both the `revision = "..."` and `revision: str = "..."` forms — recorded here so a future arc does not re-discover it. **Pillar mapping.** Maintainability is strengthened (the widget-domain allowlist is now first-class schema with explicit constraints; the app layer cannot bypass the lowercase contract or the per-Admin uniqueness contract). Traceability is strengthened (the FK shape decision is recorded in the migration docstring + the ORM file docstring + this §17 entry — three independent surfaces). Reliability is strengthened (the migration applies atomically at Commit 10b inside the same ECS deployment window as the new backend image, so new code and new schema activate together; rollback path is `alembic downgrade -1` because Revision A is the table's birth and the downgrade is safe). Scalability is unchanged (BIGINT primary key with `IDENTITY(always=False)` plus index on `admin_id` gives O(log n) per-request allowlist lookups indefinitely). Security is unchanged at this commit; the Commit 9 CAPTCHA gate on Free signup remains the primary security control for the public domain-registration surface. **Files in this commit:** `alembic/versions/arc6_a_admin_widget_domains.py` (NEW, 142 lines), `app/models/admin_widget_domain.py` (NEW, 100 lines), `app/models/__init__.py` (modified, +3 lines: import line + `__all__` entry + section header). No IAM scope expansion at this commit; no SSM touch; no Stripe touch; no prod RDS touch (deferred to Commit 10b). The closing tag for the full Arc 6 schema half lands at Commit 11 alongside the rest of Arc 6 doctrine close.
- **2026-05-23 (Arc 6 Commit 4 — V2 SKU mint in Stripe Live + 3 SSM SecureString puts + app/core/config.py rewrite to V2 vocabulary):** The replacement surface for the V1 SKUs archived in Commit 2 landed in Stripe Live account `acct_1TX2BmRytQVRVXw7` in a single mint window at 23:16-23:18 UTC, with config.py vocabulary fully realigned in the same commit. **Stripe mutations.** Two new Products created — `prod_UZXsUqCuumvw1v` "Luciel Pro" (metadata `tier=pro`, `billing_model=flat`, `arc=arc6`, `vocabulary=v2`) and `prod_UZXsY0kLJsFfop` "Luciel Enterprise" (metadata `tier=enterprise`, `billing_model=hybrid`, `arc=arc6`, `vocabulary=v2`). Three new recurring CAD Prices created on those Products: `price_1TaOmORytQVRVXw77yRoEC8m` Pro monthly ($349 CAD, 34,900 cents, month×1, `tier=pro`, `cadence=monthly`), `price_1TaOmORytQVRVXw7ElbQotvK` Pro annual ($2,990 CAD, 299,000 cents, year×1, `tier=pro`, `cadence=annual`, ~28.6% discount vs monthly-equivalent), and `price_1TaOmPRytQVRVXw7ozfKMFps` Enterprise floor annual ($24,000 CAD, 2,400,000 cents, year×1, `tier=enterprise`, `cadence=annual`). All five new objects `active=true`, `usage_type=licensed`, `currency=cad`. Every mutation carried a dated idempotency key (`arc6-mint-v2-product-luciel-pro-2026-05-23`, `arc6-mint-v2-product-luciel-enterprise-2026-05-23`, `arc6-mint-v2-price-pro-monthly-2026-05-23`, `arc6-mint-v2-price-pro-annual-2026-05-23`, `arc6-mint-v2-price-enterprise-floor-annual-2026-05-23`) so the mint script is safely re-runnable. **Control assertions.** The intro fee Price `price_1TXNmnRytQVRVXw7GGfyJiaj` on Product `prod_UWQeeXJ3CwJQYu` "Luciel Pilot Intro Fee" verified `active=true` both PRE-mint (before any V2 mutation) and POST-mint (after all V2 mutations) to catch any accidental side-effect of the V2 mint sweep on the retained V1 intro-fee surface — both assertions passed. **SSM writes.** Three SecureString parameters written via `ssm:PutParameter` in `ca-central-1`: `/luciel/production/stripe_price_pro_monthly` → `price_1TaOmORytQVRVXw77yRoEC8m` (v1, 23:16:29.514 UTC), `/luciel/production/stripe_price_pro_annual` → `price_1TaOmORytQVRVXw7ElbQotvK` (v1, 23:16:29.651 UTC), `/luciel/production/stripe_price_enterprise_floor_annual` → `price_1TaOmPRytQVRVXw7ozfKMFps` (v1, 23:16:29.777 UTC). Read-back via `ssm:GetParameter` confirmed all three values match the minted Price IDs exactly. No `ssm:DescribeParameters` was invoked at any point — the `LucielSandboxStripeScope` policy's deliberate omission of that action remained respected (see Commit 2 paperwork-half and this session's doctrine finding #3); the post-state regen worked from known parameter names instead. **Enterprise metered Price DEFERRED per partner direction.** CANONICAL §11.7 / §14 locks Enterprise as hybrid (flat floor + metered overage), but partner directed on 2026-05-23 at 7:14 PM EDT ("Let's not the metered part right now partner we can do this we can notice our business booming") to mint the floor only and defer the metered Price until the Enterprise pipeline materializes. Until then Enterprise Checkout sessions use floor-only billing and any per-unit overage is invoiced manually outside Stripe by ops. `app/core/config.py` intentionally omits the `stripe_price_enterprise_metered_unit` slot (no empty placeholder field) so no future code path can accidentally construct a Checkout with `price_id=""`. When the deferral is lifted, the work is: mint a new metered Price with `recurring={usage_type:"metered", aggregate_usage:"sum"}` + tiers, SSM-put under `/luciel/production/stripe_price_enterprise_metered_unit`, add the config slot, extend BillingService Checkout line-items, and wire `stripe.SubscriptionItem.create_usage_record` into the per-unit emitter. **config.py rewrite (V1 → V2).** Removed six V1 fields (`stripe_price_individual`, `stripe_price_individual_annual`, `stripe_price_team_monthly`, `stripe_price_team_annual`, `stripe_price_company_monthly`, `stripe_price_company_annual`); added three V2 fields (`stripe_price_pro_monthly`, `stripe_price_pro_annual`, `stripe_price_enterprise_floor_annual`); retained `stripe_price_intro_fee` slot and value unchanged; preserved `stripe_secret_key` / `stripe_publishable_key` / `stripe_webhook_secret`. Header comment block fully rewritten to lead with the V2 tier topology table (Free $0 CAPTCHA-gated / Pro flat-rate self-serve / Enterprise hybrid flat+metered-deferred), cross-reference CANONICAL §11.7 + §14, document the Enterprise metered deferral with the partner-direction quote inline, and document the V1 deprecation pointing to Arc 6 Commits 2/4/6. The boot-safe pattern (§3.2.9) language was preserved verbatim — backend still boots with empty Stripe config and `/billing/*` routes return 501 (never 500) when a needed slot is missing. AST parse OK, V1 names fully expunged from config.py (verified by grep sweep). **Call-site refactor deferred to Commit 5.** Five other Python files still reference V1 field names (`app/services/billing_service.py`, `app/services/billing_webhook_service.py`, `scripts/stripe_create_live_prices.py`, `tests/api/test_step30a_2_first_time_gate.py`) — they will be rewritten in Commit 5 (BillingService + TierProvisioningService + SubscriptionService → V2 vocab). Prod is unaffected during the Commit 4-through-9 window because the running container is `luciel-backend:85` which still has V1 config attrs + V1 SSM env injection intact via the existing TaskDef; Commit 10's `luciel-backend:86` build is the atomic cutover for both halves. **Mint-script JSON crash + remediation — a new Stripe-SDK-15.1.0 gotcha for the doctrine list.** The mint script succeeded on all 8 mutations but raised AttributeError on the final post-state JSON write because Stripe SDK 15.1.0's `StripeObject.metadata` does NOT support `.get(...)` and `dict(metadata)` raises `KeyError: 0` (StripeObject iter is positional, not dict-keyed). Correct accessor is `getattr(stripe_obj.metadata, key, default)`. Remediated by authoring `scripts/_arc6_stripe_mint_poststate_regen.py` (129 lines, read-only Stripe + SSM, hardcoded IDs from mint stderr), which initially repeated the bug, was caught by a sanity-print before paperwork was finalized, and was corrected to use `getattr` on the second pass. Both scripts retained in the commit for audit + future re-runs. Doctrine list of Stripe SDK 15.1.0 gotchas now reads: (1) `dict(stripe_obj)` raises KeyError, (2) `.to_dict_recursive()` renamed to `_to_dict_recursive()` (private), (3) `stripe_obj.metadata.get(key)` raises AttributeError and `dict(metadata)` raises `KeyError: 0` — use `getattr(stripe_obj.metadata, key, default)`. **Files in this commit:** `app/core/config.py` (rewrite), `scripts/_arc6_stripe_mint_v2.py` (NEW 290 lines, idempotent), `scripts/_arc6_stripe_mint_poststate_regen.py` (NEW 129 lines, idempotent read-only), `arc6-out/E-arc6-stripe-mint-poststate.json` (NEW machine-readable evidence), `arc6-out/E-arc6-stripe-mint-record.md` (NEW 225-line audit), and this CANONICAL §17 entry. No prod schema, no IAM, no ECS, no buildah, no marketing-site changes in this commit. Per Arc 6 doctrine §Pause-between-commits, Commit 4 is one of the two no-pause mutation groups; partner pre-authorized the Stripe-Live cutover at session start. **Branch HEAD pre-Commit-4:** `d659523`. **Up next — Commit 5:** Backend Stripe-surface rewrite (`BillingService` + `TierProvisioningService` + `SubscriptionService`) to V2 vocab, consuming the new config fields.
- **2026-05-23 (Arc 6 Commit 5 — Backend Stripe-surface rewrite to V2 vocab: BillingService PRICE_ID_KEY + webhook fallbacks + first-time-gate test + legacy V1 mint-script retirement):** Closes the TODO-arc6 marker that has lived in `app/services/billing_service.py` line 137 since Step 30a.1 — the backend Stripe surface now consumes the V2 config slots that landed in Commit 4 with no V1 vestige in live code paths. **`app/services/billing_service.py` PRICE_ID_KEY corrected.** Pre-Commit-5 the table had FOUR rows and TWO bugs: it referenced `stripe_price_enterprise_monthly` (no such config slot — Enterprise has no monthly cadence at V2 launch) and `stripe_price_enterprise_annual` (wrong name — the actual minted/SSM slot is `stripe_price_enterprise_floor_annual`). Post-Commit-5 the table has THREE rows: (TIER_PRO, BILLING_CADENCE_MONTHLY) → `stripe_price_pro_monthly`, (TIER_PRO, BILLING_CADENCE_ANNUAL) → `stripe_price_pro_annual`, (TIER_ENTERPRISE, BILLING_CADENCE_ANNUAL) → `stripe_price_enterprise_floor_annual`. Enterprise+monthly callers now correctly raise BillingNotConfiguredError → 501 at the route layer (was a quiet `AttributeError` from `getattr(settings, 'stripe_price_enterprise_monthly')` since the slot does not exist on Settings). The PRICE_ID_KEY block comment was rewritten to document the V2 tier topology inline (Free no-Stripe-row / Pro flat / Enterprise annual-only floor + deferred metered) with CANONICAL §11.7 + §14 cross-references. **`app/services/billing_service.py` adjacent docstring sweeps.** Three V1 vocabulary leakages corrected: `require_configured` docstring updated to reference Arc 6 Commit 5 (was Step 30a.1 talking about `stripe_price_individual`); `resolve_instance_count_cap` docstring fixed (claimed fallback was Individual cap 3, actual code falls back to `TIER_FREE` cap = 1 — the comment was wrong since Arc 5 Path A landed; now correctly states Free=1 default + V2 caps Free=1/Pro=10/Enterprise=unlimited); `create_checkout` metadata-stamp docstring updated from V1 `'individual' | 'team' | 'company'` to V2 `'pro' | 'enterprise'` with a note that Free never reaches this route (it has its own /signup-free path landing in Commit 8) and `display_name` carrying onto `AdminConfig.display_name` (was V1 `TenantConfig.display_name`). **`app/services/billing_webhook_service.py` three surgical updates.** (1) `_mint_tenant_id_from_email` docstring rewritten — the live `_TIER_PREFIX` dict at lines 101-105 was already V2 (`free`/`pro`/`ent`) since Arc 5 B8 but the docstring still claimed V1 prefixes `ind`/`team`/`co`, and the fallback default was `"ind"` instead of `"pro"`; both corrected. The function signature default `tier: str = TIER_PRO` was already V2-correct, so the V1 fallback was unreachable in practice but documentation-toxic and a future-bug-magnet — fixed. (2) `_handle_subscription_event` defensive-fallback comment block expanded — the line that says "fall back to the safest tier (Individual)" updated to say "fall back to the safest tier (Pro — Arc 6 Commit 5 update; was Individual at V1)" with the rationale spelled out for future maintainers: Pro does not over-provision like Enterprise (unlimited Instance cap) and does not under-provision like Free ($0 tier would disable paid features for a buyer Stripe has already authorized payment for). (3) `_extract_price_id` checkout.session fallback REMOVED — the line `return settings.stripe_price_individual or None` was the last live-code reference to the deleted V1 config field; deleting it would have caused an AttributeError at the first checkout.session.completed webhook with no inline items. Replaced with `return None` plus a detailed comment explaining V2 has multiple Prices so no defensible blind-default exists, callers should let `stripe_price_id` be NULL on the Subscription row and a reconciler can backfill via Stripe API expansion on the session-id. **`scripts/stripe_create_live_prices.py` (706 lines) DELETED via `git rm`.** This was the Step 30a.2 V1 mint script that originally created the six V1 Prices ($30 / $300 individual + $300 / $3,000 team + $2,000 / $20,000 company) which were archived in Arc 6 Commit 2. Retaining it would have been actively harmful: it would mint a parallel V1 catalog if mistakenly re-run by an operator (its idempotency keys do not collide with the V2 mint script's keys), and it still referenced `settings.stripe_price_individual`-and-five-friends as ORM-style targets, which now do not exist on Settings. Its post-mint SSM PowerShell emitter would also write to V1 parameter names (`/luciel/production/stripe_price_individual` etc.) that nothing reads in V2. Deletion is per Arc 6 Path A doctrine ("whatever we ship out in our code and prod and schema must be aligned with this vision"). The Arc 6 V2 mint flow lives at `scripts/_arc6_stripe_mint_v2.py` + `scripts/_arc6_stripe_mint_poststate_regen.py` (both retained in Commit 4) and is the canonical replacement. **`tests/api/test_step30a_2_first_time_gate.py` V2 refresh.** Six edits: docstring rewritten to note the V1→V2 vocabulary lift while preserving the Step-30a.2 work-unit attribution (the first-time gate behaviour under test is unchanged across the transition); `_setup_settings` swaps `stripe_price_individual="price_TEST_ind_m"` for `stripe_price_pro_monthly="price_TEST_pro_m"`; four `tier="individual"` callsites updated to `tier="pro"`; one new positive-assertion added on `kwargs["metadata"]["luciel_tier"] == "pro"` to catch regressions where the V2 metadata stamp drifts back to V1 strings. **Test results:** all 15 tests in the file PASS against the rewritten BillingService surface (verified locally with `DATABASE_URL` shim set; the 18 unrelated psycopg2-missing failures in sibling files are pre-existing environmental issues in the sandbox and were confirmed identical against a stashed baseline). **Files NOT touched in this commit (deferred to later Arc 6 commits or out of scope):** `app/services/tier_provisioning_service.py` (already fully V2 since Arc 5 Path A — grep confirms no V1 leakage); deeper Tenant→Admin noun-rename across the webhook handler body (comment at line 25 mentioning "tenant" and similar surface-tissue references) — deferred to Commit 6 per the Arc 6 plan which dedicates that commit to webhook noun-rename + Stripe metadata key adjustments; no migration changes (admin_widget_domains migration still authored-only, prod apply still deferred to Commit 10b); no IAM, no ECS, no buildah, no marketing site. **Boot-safety check.** `app/core/config.py` parses; `app/services/billing_service.py` parses; `app/services/billing_webhook_service.py` parses; `tests/api/test_step30a_2_first_time_gate.py` parses; the full target test file runs green. The running container in prod (`luciel-backend:85`) still has V1 config attrs + V1 SSM env injection intact via the existing TaskDef rev, so this commit is functionally invisible to prod buyers — the V2 cutover is atomic at Commit 10's `luciel-backend:86` build. **Files in this commit:** `app/services/billing_service.py` (PRICE_ID_KEY + 3 docstrings), `app/services/billing_webhook_service.py` (3 surgical updates), `tests/api/test_step30a_2_first_time_gate.py` (6 edits), `scripts/stripe_create_live_prices.py` (DELETED), `docs/CANONICAL_RECAP.md` (this entry). Per Arc 6 doctrine §Pause-between-commits, Commit 5 is a pause-for-push commit (NOT one of the two no-pause groups); stage + commit landed locally, push pending partner approval. **Branch HEAD pre-Commit-5:** `f0f2c0c`. **Up next — Commit 6:** Webhook noun rename Tenant→Admin in billing_webhook_service.py + Stripe metadata key adjustments (the V1 noun-residue this commit deliberately left untouched).
- **2026-05-23 (Arc 6 Commit 6 — Webhook noun rename Tenant→Admin in `billing_webhook_service.py`, tight-scope file-local rename only, no cross-codebase blast):** The webhook handler's local vocabulary now matches the Arc 5 B8 Admin rename while every external interface (DB column, ORM attribute, sibling service method names, audit-row column kwarg, audit `after`-dict payload key, return-dict observability key) is deliberately retained at its Arc-5 Tenant name. **Scope discipline (partner-approved at session-start).** Three options were posed to partner: (tight) webhook-file-only rename including local vars + log strings + comments + file-local helper rename; (expand) tight scope PLUS cross-codebase rename of `OnboardingService.onboard_tenant` → `onboard_admin` and `AdminService.deactivate_tenant_with_cascade` → `deactivate_admin_with_cascade` across ~15 files (`app/api/v1/admin.py`, `app/services/admin_service.py`, `app/services/onboarding_service.py`, `app/services/instance_service.py`, `app/services/api_key_service.py`, `app/services/billing_service.py`, `app/middleware/session_cookie_auth.py`, `app/models/conversation.py`, `app/models/identity_claim.py`, multiple alembic migrations); (skip) defer the entire webhook noun rename to a future arc and jump to Commit 7. Partner chose tight scope. The expand path was rejected as a separate arc unit because it touches multiple Arc-5-stable migrations and would inflate the commit beyond what Arc-6's Stripe-restructure brief demands; the skip path was rejected because letting the webhook handler keep V1 vocabulary while the rest of the Arc 5/6 surface speaks V2 admin-noun would re-open the doctrine-symmetry concern that drove the rename in the first place. **What renamed (file-local only).** The module-level helper `_mint_tenant_id_from_email` → `_mint_admin_id_from_email` (verified zero external callers via cross-codebase grep — it was always a `_`-prefixed file-local primitive); the local variable `tenant_id` holding the freshly-minted slug → `admin_id` everywhere inside `_on_checkout_completed` (8 line-local assignments + reads); the `_on_checkout_completed` docstring "Mint tenant + user + subscription atomically" → "Mint admin + user + subscription atomically"; the section-header banner comment "Tenant id minting" → "Admin id minting (Arc 6 Commit 6 — webhook-noun rename Tenant→Admin)" with a 6-line block documenting precisely which surfaces deliberately did NOT rename (the DB column, the ORM attribute, the two sibling-service method names); log-string format substrings "tenant=%s" → "admin=%s" in the tier-pre-mint exception logger (best-effort post-commit path) and in the welcome-set-password email-send exception logger; log-string "deactivate tenant cascade" in the customer.subscription.deleted audit-row `note` → "deactivate admin cascade"; the "Cascade deactivate the tenant" comment header in `_on_subscription_deleted` → "Cascade deactivate the admin" with a 3-line block flagging that the called method `AdminService.deactivate_tenant_with_cascade` retains its Arc-5 external name; the "cascade-deactivate failed tenant=%s sub=%s" exception logger → "cascade-deactivate failed admin=%s sub=%s"; the `_record_unknown_event` "The tenant_id for an unknown event is unknowable" comment refined to "The `tenant_id` column value for an unknown event is unknowable (no admin can be resolved)" to clarify the noun distinction. **What deliberately did NOT rename (with reasoning recorded inline as code comments).** (1) DB column `subscriptions.tenant_id` — Arc 5 Path A retained it physically with the FK retargeted from `tenant_configs.tenant_id` to `admins.id`, per `arc5_c_admin_instance_subtractive.py` docstring: "tenant_id so app-layer code does not need a rename"; renaming the column is its own separate work-unit since it requires an Alembic migration + cross-codebase rename of every `Subscription.tenant_id` ORM access (the model defines this attribute at `app/models/subscription.py`). (2) ORM attribute `Subscription.tenant_id` — mirrors the column, so the constructor kwarg `Subscription(tenant_id=admin_id, ...)` stays as-is with an inline comment marking the column-symmetry intent. (3) `OnboardingService.onboard_tenant(tenant_id=admin_id, ...)` and `AdminService.deactivate_tenant_with_cascade(sub.tenant_id, ...)` call sites — these are external method-name + kwarg-name contracts of sibling services; renaming them would have pulled the commit into the rejected expand-scope path. Three sibling-service helper calls (`AdminAuditRepository.record(tenant_id=admin_id, ...)`, `TierProvisioningService.premint_for_tier(tenant_id=admin_id, ...)`, `mint_set_password_token(tenant_id=admin_id, ...)`) similarly keep their column-mirrored `tenant_id` kwarg; the local variable being passed in is `admin_id`, so the value semantics are V2 even though the kwarg name reads V1. (4) Audit-row `after`-dict payload key `"tenant_id": admin_id` — the audit row's JSON payload key was kept as `tenant_id` to mirror the column the row is keyed on (`admin_audit_logs.tenant_id`); adding a parallel `"admin_id"` key would invite consumer-side confusion about which one to trust. (5) Return-dict observability key `{"tenant_id": admin_id, ...}` in all four handlers — Stripe ignores the 200-response body (`POST /api/v1/billing/webhook` returns this via `JSONResponse(content=result)` at `app/api/v1/billing.py` L303) so the key has no internal consumer; kept as `tenant_id` for log-tail symmetry with the column. (6) `_record_replay(*, event_id, tenant_id, stripe_subscription_id)` and `_record_unknown_event(*, event_id, event_type)` private helpers — `_record_replay`'s `tenant_id` kwarg threads directly into `audit_repo.record(tenant_id=...)` so its name mirrors the destination; renaming it would have produced a `_record_replay(admin_id=...)`-but-`record(tenant_id=admin_id)` asymmetry inside a 5-line function body, which is the opposite of the discipline this commit enforces. (7) Three V1-vocab tests in `tests/api/test_step30a_2_pilot_webhook_dict.py` (`TestMaterialisationOrder::test_json_loads_appears_before_handle_call`) and `tests/api/test_step30a_2_pilot_commit3f.py` (`test_webhook_handler_fetch_is_wrapped_in_try`, `test_handle_is_called_with_event_dict_not_event`) were CONFIRMED pre-existing failures against the `f2393f3` (Commit 5) baseline by stash+retest — they target text-shape patterns broken by Arc 2's stripe-attribute-error fix (`D-billing-webhook-service-stripe-attribute-error-2026-05-18`) at `app/api/v1/billing.py` L296 which changed the constructor form from `BillingWebhookService(db).handle(event_dict)` to `BillingWebhookService(db, stripe_client=stripe_client).handle(event_dict)`. Repairing these text-shape tests is a future doctrine drift (`D-pilot-webhook-text-shape-tests-stale-2026-05-23`) that lands outside Commit 6 because it has no bearing on the noun rename. **Stripe metadata keys NOT renamed.** Cross-codebase grep over `luciel_email`, `luciel_display_name`, `luciel_tier`, `luciel_billing_cadence`, `luciel_intro_applied` confirms all five Stripe Checkout / Subscription metadata keys are already noun-neutral (the brand prefix is `luciel_`, never `tenant_`); the Commit 6 brief's "+ Stripe metadata keys" sub-clause therefore had zero deltas to execute. The TODO checklist entry retains this clause for arc-tail audit symmetry but the §17 entry records the finding-of-no-rename inline here. **Boot-safety + test results.** AST-parse OK (892 lines); the target test suite `tests/api/test_step30a_2_first_time_gate.py` runs 15/15 PASS with `DATABASE_URL="postgresql://test:test@localhost:5432/test"` shim; broader sweep across `test_step30a_2_first_time_gate.py + test_step30a_2_pilot_refund_shape.py + test_step30a_3_auth_shape.py` shows IDENTICAL 17-failed-45-passed-1-skipped totals against pre-Commit-6 (`f2393f3`) and post-Commit-6 (this commit) — every failure is environmental (`argon2`/`psycopg2` missing in sandbox venv) or a pre-existing text-shape regression from Arc 2's stripe-attribute-error fix. Zero new test regressions introduced by Commit 6. **Pillar mapping.** Maintainability is strengthened (the webhook handler's local vocab no longer asks a future reader to translate "tenant" mentally to "admin" when chasing a log line or reading the docstring; the deliberately-retained external surfaces are each annotated with an inline comment so the symmetry-break is documented at the call site, not just in CANONICAL §17). Traceability is strengthened (every audit row + every log emission from this file now carries the V2 admin noun in its human-readable portion while continuing to write to the unchanged `tenant_id` column for query-stable observability — the union of the two surfaces is unambiguous). Reliability is unchanged (zero behavioural deltas — every renamed identifier carries the same value through the same control flow to the same destination). Scalability is unchanged. Security is unchanged. **Drift posture.** `D-arc5-tenant-id-column-physical-retention-2026-05-23` opens at this commit as an ARCHITECTURAL-INTENT drift (not a bug — the physical retention is deliberate per Arc 5 Path A) anchored to the docstring of `arc5_c_admin_instance_subtractive.py` and cross-referenced from this §17 entry. The drift's resolution is gated on a future arc that elects to perform the column rename (Alembic migration `subscriptions.rename_tenant_id_to_admin_id` + cross-codebase ORM-attribute rename + matching kwarg renames across the seven retained sibling-service entry points). `D-pilot-webhook-text-shape-tests-stale-2026-05-23` opens as a TEST-DEBT drift covering the three text-shape pilot tests broken by Arc 2's stripe-attribute-error fix; resolution is a follow-up commit (not blocking) that updates the text patterns to match `BillingWebhookService(db, stripe_client=stripe_client).handle(event_dict)`. **Files in this commit.** `app/services/billing_webhook_service.py` (modified, +63 / -29 lines: helper rename + 14 surgical comment/var/log-string updates) and `docs/CANONICAL_RECAP.md` (this entry). No prod schema, no IAM, no SSM, no ECS, no buildah, no marketing site, no test edits in this commit. Per Arc 6 doctrine §Pause-between-commits, Commit 6 is a pause-for-push commit (NOT one of the two no-pause mutation groups); stage + commit lands locally, push pending partner approval. **Branch HEAD pre-Commit-6:** `f2393f3`. **Up next — Commit 7:** Marketing site `Pricing.tsx` 3-card grid (Free / Pro / Enterprise) + CANONICAL §14 entitlement matrix regenerated against the V2 tier topology.
- **2026-05-23 (Arc 6 Commit 7 — Marketing site V2 alignment, Option B Full cleanup: 13 files rewritten across `Luciel-Website/src/` to swap V1 tier vocabulary `individual`/`team`/`company` for V2 vocabulary `free`/`pro`/`enterprise`, Pricing.tsx restructured to 3-card grid with V2 numerics, Dashboard topology rewritten to V2 tier-gating, pilot.test.tsx V2 assertions, all 36 tests pass, `npx tsc --noEmit` clean):** The marketing surface that buyers see on `luciel.ai` now matches the backend Stripe surface that landed at Commits 4-6 — zero V1 tier strings remain in the source tree (cross-codebase grep for double-quoted and single-quoted `"individual"`, `"team"`, `"company"` across `Luciel-Website/src/**/*.{ts,tsx}` returns zero matches post-commit). **Scope discipline (locked at prior segment).** Two options were posed: (A) minimal vocab swap touching only the literal tier strings; (B) full cleanup = A + Dashboard topology rewrite + intro-pilot Pro-only semantics + `pilot.test.tsx` V2 + `Signup.tsx` copy. Partner chose B with the framing "Heavier, single sweep" — rejected the A path because letting Dashboard keep V1 tab-gating while Pricing speaks V2 would have produced the exact ship-out-vs-vision drift the Path A doctrine commits to removing. **Files rewritten (13 source + 1 test).** (1) `src/pages/Pricing.tsx` — full rewrite: `Tier` union narrowed from `"individual"|"team"|"company"` to `"free"|"pro"|"enterprise"`; `TierCard.primary` extended to discriminated union `"signup-free"|"checkout"|"contact-sales"` so the three CTA shapes are type-safe at the render site; `tiers` array rebuilt with V2 numerics ($0 Free / $349 CAD/mo + $2,990 CAD/yr Pro / $24,000 CAD/yr Enterprise floor) and §14 6-axis matrix bullets (instance cap, seat cap, composition depth, audit retention, model access, CNAME / SSO); Free CTA renders `<Link to="/signup-free">Sign up free</Link>` (route lands in Commit 8); Pro CTA renders `<WaitlistButton mode="checkout">` against the V2 Stripe Prices; Enterprise CTA renders `<Link to="/contact?tier=enterprise">Talk to sales</Link>`; V1 "billed once × 10" cadence headline removed entirely, replaced with "~28% off monthly" subtitle on the Pro annual toggle (matches the actual 28.6% discount math: $349 × 12 = $4,188 vs $2,990 annual = $1,198 saved = 28.6% off); FAQ "Monthly or annual" rewritten to explain the Pro toggle + Enterprise annual-only-at-V2-launch with metered overage deferred per CANONICAL §11.7; pilot hint narrowed to Pro-monthly-only (Free=no pilot since $0 has nothing to pilot against, Enterprise=committed-use discount conversation instead per Pro-only pilot lock decided 2026-05-23); pilot refund footnote text updated to "first-time Pro-monthly customers" (was "first-time customers" with no tier scope). (2) `src/lib/billing.ts` — `Tier` union swap V2; Dashboard CompanyTab/TeamTab comment block updated to V2 vocabulary. (3) `src/lib/analytics.ts` — `Tier` union swap V2; new `Cadence` type (`"monthly" | "annual"`); two new event types added: `pricing_cadence_toggled` (carries `cadence`) and `pricing_cta_clicked` (carries `tier`, `cadence`, `cta`) so the V2 pricing-page funnel is instrumented at the source. (4) `src/lib/submissions.ts` — `Tier` union swap V2 so the Contact + Signup form POST payloads carry the V2 noun. (5) `src/components/WaitlistModal.tsx` — `Tier` union swap V2; doc comment update for the Stripe checkout-mode flow now points at the V2 Price slot names. (6) `src/components/admin/CreateLucielForm.tsx` — `TIER_SCOPES` table rewritten with the V2 scope mapping: `free → [agent]`, `pro → [agent, domain]`, `enterprise → [agent, domain, tenant]` (matches §14 axis-3 composition-depth matrix where Free=no composition, Pro=depth≤2, Enterprise=unlimited); default tier in form-state initializer changed `"individual"` → `"free"` so a new admin-created Luciel defaults to the V2 cheapest tier (was V2's deleted lowest). (7) `src/pages/Contact.tsx` — `Tier` union swap V2; the URL-param validator + the SelectItems list rewritten so `?tier=free`, `?tier=pro`, `?tier=enterprise` are the three accepted values; any V1-leftover `?tier=team` link from a stale email or external bookmark now falls through to the default-no-tier state instead of mis-routing. (8) `src/pages/Signup.tsx` — `Tier` union swap V2; `checkoutEnabled` boolean refined to `isBillingEnabled() && tier === "pro"` (Pro is the ONLY Stripe-Checkout tier on this surface — Free goes via the public `/signup-free` route landing Commit 8, Enterprise goes via Contact-sales); per-tier `trialDays` field removed (was V1-shaped with three different values), replaced with a flat `const TRIAL_DAYS = 7` fallback for the rare case where a non-billing-enabled environment renders this surface; `pilotEligibleSurface` narrowed to Pro-monthly-only (matches the Pricing.tsx pilot scope); copy rewritten end-to-end ($349/mo + $2,990/yr framing, "~28% off" annual framing, "Card billed in CAD. Cancel from your account at any time." footer line). (9) `src/pages/Luciel.tsx` L261 — internal product-page CTA target updated from `?tier=team` to `?tier=pro` (both the `<Link to>` and the matching `trackCta` event payload). (10) `src/pages/Account.tsx` L360 — the "Start a trial" link that targeted `?tier=individual` rewritten to `?tier=pro` "Start your Pro plan" so users who land on Account without a subscription are funnelled to the V2 mid-tier (matches the §14 ladder of Free→Pro→Enterprise where Pro is the default conversion target). (11) `src/pages/Dashboard.tsx` — three tier-gating updates: L1078 link target updated; L1102-L1163 tier-gating rewritten with `const isTeamLikeTier = tier === "pro" || tier === "enterprise"` (was `"team" || "company"`) and `const showCompany = tier === "enterprise" && companyRoleAllowed` (was `"company"`); CompanyTab doc comment block (L352-L374) rewritten to clarify Enterprise-only visibility with explicit note that Pro tier does NOT see this tab; TeamTab doc comment block (L650-L658) rewritten to clarify Pro+Enterprise visibility (Free does NOT see this tab); L795 tier-equality check `tier === "company"` updated to `tier === "enterprise"`. **Dashboard topology decisions (locked in this commit).** Free = 3 tabs (Overview + Luciels + Account); Pro = 4 tabs (+ Team); Enterprise = 5 tabs (+ Company). Mirrors §14: Free has 1 seat so no Team tab needed; Pro has 25 admin-team seats so Team tab gates the seat management UI; Enterprise has knowledge_share_grants + cross-instance composition so Company tab gates the multi-Admin governance UI. (12) `src/pages/About.tsx` L138 — internal CTA target updated `?tier=team` → `?tier=pro` to match Luciel.tsx + Account.tsx pattern. (13) `src/pages/Index.tsx` L173-L175 — three tier cards on the home page hero renamed Individual/Department/Company → Free/Pro/Enterprise with V2-aligned body copy (Free: "Try Luciel free — one instance, one seat", Pro: "Scale your team — 10 instances, 25 seats", Enterprise: "Custom topology + SSO + unlimited CNAMEs"); editorial copy on L35 and L70 describing deployment scopes intentionally left intact since those words refer to the scope-vocabulary (`agent`/`domain`/`tenant` scopes — see CreateLucielForm `TIER_SCOPES` mapping above) which is V2-stable, not the tier-vocabulary which this commit swept. **Test refresh.** `src/test/pilot.test.tsx` rewritten to V2: `baselinePilot.tier` updated `"team"` → `"pro"`; `instance_count_cap` updated to V2 Pro cap of 10 (was V1 team cap of 5); new field `active_role: "owner"` added since the test fixture's `PilotInfo` shape gained that field in Arc 5 Path A and the test had been silently relying on undefined; pricing primary-CTA test rewritten — monthly cadence assertion changed from `toHaveLength(3)` (V1 had three identical "Start 90-day pilot" CTAs across the three tier cards) to `toHaveLength(1)` (V2 has the pilot CTA on Pro card only) plus two new positive assertions that the Free card renders a "Sign up free" link AND the Enterprise card renders a "Talk to sales" link, so the test catches regressions where the three V2 CTA shapes drift; annual cadence assertion changed to `toHaveLength(1)` for "Start annual plan" (only Pro annual has this CTA — Enterprise floor is annual but uses "Talk to sales"); pilot refund footnote regex narrowed from `/first-time customers/i` to `/first-time .*customers/i` to accommodate the new "first-time Pro-monthly customers" wording without breaking. **Verification.** `grep -E "\"individual\"|\"team\"|\"company\"" src/**/*.{ts,tsx}` and single-quoted-variant sweep both return zero matches; `npx tsc --noEmit` exits 0 with no output (full TypeScript surface compiles clean against the narrowed V2 union); `npm test` reports 36/36 pass (admin.test.ts 18 + Dashboard.test.tsx 3 + pilot.test.tsx 14 + example.test.ts 1 — pilot.test.tsx gained one assertion vs the V1 baseline since the "Sign up free" + "Talk to sales" positive checks are NEW). **Files NOT touched (deferred or out of scope).** The marketing site has no Stripe-Price-ID references in client code (the Checkout flow constructs sessions server-side via the existing `/api/v1/billing/checkout` route consuming `app/services/billing_service.PRICE_ID_KEY` which was rewritten at Commit 5), so zero changes to client-side billing config; the public `/signup-free` route + the `SignupFree.tsx` page itself are deferred to Commit 8 (Pricing.tsx links to a route that does not yet exist at this commit — this is intentional and matches the Pause-between-commits discipline since Commit 8 is a backend+frontend pair that does not benefit from being folded into Commit 7's pure-marketing sweep); no backend file touches in this commit; no IAM, no SSM, no Stripe, no migrations, no Buildah, no ECS, no prod RDS. **Pillar mapping.** Maintainability is strengthened (the marketing source tree no longer carries V1 tier vocabulary anywhere — a future engineer reading `Tier = "free" | "pro" | "enterprise"` and grepping for any of those values gets a coherent answer; the discriminated-union `primary` on `TierCard` makes the three CTA shapes type-safe so adding a fourth tier-flow in the future requires extending the union explicitly rather than slipping in via a string-typed prop). Traceability is strengthened (the `pricing_cta_clicked` and `pricing_cadence_toggled` analytics events instrument the V2 funnel from the source, so partner can answer "how many Pro-monthly vs Pro-annual conversions" from the analytics table without needing to derive it from Stripe). Scalability is strengthened (the Dashboard topology decisions of Free=3/Pro=4/Enterprise=5 tabs match §14 axis-2 seat cap and axis-3 composition-depth, so a future tier-axis change lands as a single tab-visibility predicate rather than scattered ternaries). Reliability is strengthened (the V2 pilot footnote, V2 pricing strings, V2 CTA targets, and V2 Dashboard tab-gating are all locally consistent — no V1 cell exists that could mis-route a buyer mid-funnel). Security is unchanged at this commit; the Commit 9 CAPTCHA gate on Free signup remains the primary security control for the public Free signup surface that Commit 8 introduces. **Files in this commit (Luciel-Website repo).** `src/components/WaitlistModal.tsx`, `src/components/admin/CreateLucielForm.tsx`, `src/lib/analytics.ts`, `src/lib/billing.ts`, `src/lib/submissions.ts`, `src/pages/About.tsx`, `src/pages/Account.tsx`, `src/pages/Contact.tsx`, `src/pages/Dashboard.tsx`, `src/pages/Index.tsx`, `src/pages/Luciel.tsx`, `src/pages/Pricing.tsx`, `src/pages/Signup.tsx`, `src/test/pilot.test.tsx` (14 files modified). **Files in this commit (luciel backend repo).** `docs/CANONICAL_RECAP.md` (this entry only — no code touch). Per Arc 6 doctrine §Pause-between-commits, Commit 7 is a pause-for-push commit (NOT one of the two no-pause mutation groups); stage + commit lands locally on both repos, push pending partner approval. **Branch HEAD pre-Commit-7 (luciel):** `bea7540`. **Branch HEAD pre-Commit-7 (Luciel-Website):** `d41bc23`. **Up next — Commit 8:** Free-signup route POST `/api/v1/billing/signup-free` + `SignupFree.tsx` page wired to the `/signup-free` URL that Pricing.tsx's Free CTA now points at.
- **2026-05-23 (Arc 6 Commit 8 — Unified-signup wire-up: `POST /api/v1/billing/signup-free` now mints the Admin row + emails the set-password magic link; `Luciel-Website/src/pages/SignupFree.tsx` is the new dedicated Free funnel page; `users.email_verified` column added + atomic write on magic-link consumption; latent Pro pre-mint defect fixed as a side-fix; V2 `OnboardingService` column-guard added):** The Free-tier signup funnel is now end-to-end operational from a buyer dropping their email on `/signup-free` to a signed-in dashboard session, modulo the Commit 9 CAPTCHA widget that adds the bot-gate on the marketing side and the Commit 10 deploy that runs the new migrations against prod RDS. **What the business gained today.** A buyer landing on the V2 Pricing page can now click "Sign up free", land on `/signup-free`, drop email + display_name, receive the welcome-set-password magic link, click through to `/auth/set-password`, set their password, and arrive at the dashboard — all without ever talking to a human. The 24-hour magic-link window is the soft retry envelope; the `users.email_verified BOOLEAN` column added at the same alembic head ensures the set-password click is also the email-verification click in the same SQL transaction (no two-step verify flow, no second email, no chance of the password hash and the verified flag disagreeing). Pro + Enterprise funnels are unchanged at this commit (Pro continues to use Stripe Checkout via `Pricing.tsx`'s `<WaitlistButton mode="checkout">`; Enterprise continues to use `/contact?tier=enterprise`); the unified-signup judgement was to keep `/signup-free` distinct from `/signup` because the two routes are honest about their semantics — `/signup` is the Stripe-Checkout funnel and `/signup-free` is the no-card magic-link funnel, and collapsing them would have produced a route that branches internally on `?tier=` (the V1 shape the Arc 6 cleanup is removing). **Scope discipline.** Three options were posed: (A) backend-only mint with the marketing site continuing to show "coming soon"; (B) backend + dedicated SignupFree page + new route; (C) full unified `/signup` that branches on tier. Partner-locked B at the resume-from-summary point because the route honesty preserves the Arc 6 cleanup vision and because the WaitlistButton-driven Pro funnel does not need a redesign. The `/signup` route is left as-is (it serves the Pro Stripe-Checkout flow with cadence pass-through); `/signup-free` is the new Free-only route. **Backend rewrite — `app/api/v1/billing.py::signup_free`.** Pre-Commit-8 the route was an Arc 8 W5 stub that did a captcha verify and returned `status="pending-arc-5"` with no Admin row. Post-Commit-8 the route mints the full 5-collaborator sequence: (1) soft-gate captcha verify — if `captcha_token` is present, run `verify_captcha(token, remote_ip=...)` and bounce 422 on `CaptchaInvalidError` / 501 on `CaptchaNotConfiguredError`; if absent, emit a structured `signup_free.captcha_soft_pass` WARN log against the user's email + remote_ip with a `window=commit-8-to-commit-9` tag so the gap can be audit-grepped in CloudWatch when Commit 9 lands. (2) Resolve-or-create the User row via `BillingWebhookService._resolve_or_create_user(email, display_name)` — reused the existing helper rather than authoring a parallel one so the User-row shape stays consistent across paid checkout and free signup. (3) Mint admin_id via `_mint_admin_id_from_email(email, tier="free")` which produces a V2 slug shape `free-<8hex>`; on slug collision the route raises 409 with the underlying detail. (4) Onboard the Admin via `OnboardingService.onboard_tenant(tenant_id=admin_id, display_name=display_name, tier="free", tier_source="free_signup", description=..., api_key_display_name=..., created_by="signup_free", audit_ctx=AuditContext.system(label="signup_free"))` — the audit context's `label` tag is the forensic isolator for the Free funnel. (5) Pre-mint ScopeAssignment + primary Instance via `TierProvisioningService.premint_for_tier(tenant_id=admin_id, tier="free", primary_user=user, audit_ctx=audit_ctx)`. (6) Mint the set-password magic link via `mint_set_password_token(user_id=user.id, email=email, tenant_id=admin_id, purpose="signup")`, build the URL via `build_set_password_url(token)`, and send the welcome email via `send_welcome_set_password_email(to_email=email, set_password_url=url, display_name=display_name)`. The pre-mint + welcome-email steps are best-effort post-onboard (the Admin row is already committed; failures here are logged at `EXCEPTION` level and a reconciler can re-run); a buyer whose welcome email fails can still recover via `POST /api/v1/auth/forgot-password` against the same email since the password-reset and set-password tokens are interchangeable at the route layer. **`OnboardingService` V2 column rewrite (P0 fix).** Pre-Commit-8 the service still referenced the V1 `agents`/`tenant_configs` shape via a `RESOURCE_DOMAIN` import; this would have IntegrityError'd at the first Free signup attempt against the V2 schema. Post-Commit-8 the service uses ONLY V2 Admin columns (`id`, `name`, `tier`, `tier_source`, `active`); added `ALLOWED_TIERS_V2 = {"free", "pro", "enterprise"}` as a module-level constant and a guard at the top of `onboard_tenant` that raises `ValueError` if a V1 vocab string slips through (the route's 409-on-ValueError handler surfaces this clearly to the buyer instead of as a 500). The `RESOURCE_DOMAIN` import was removed; the audit-row payload was rewritten to V2 shape; the module docstring was updated to reflect the V2 single-table shape. **`TierProvisioningService` Free-routing + Pro pre-mint defect fix.** Pre-Commit-8 the service had an L172 guard that rejected `TIER_FREE` outright; this would have made every Free signup attempt 500 at the pre-mint step. Post-Commit-8 the guard is lifted and Free routes through the same provisioning service as Pro/Enterprise (matches the "single place that turns Admin into tier-shaped Admin" doctrine). Side-fix: pre-Commit-8 the service passed `domain_id=None` into `ScopeAssignment` which would have IntegrityError'd against the NOT NULL FK in production — Pro pre-mint had this latent defect too. Added `_DOMAIN_COLLAPSE_SENTINEL = "default"` as a module-level constant and rewrote the pre-mint INSERT to use the sentinel so all three tiers (Free/Pro/Enterprise) flow through the same pre-mint shape without an IntegrityError at the FK boundary. This is a side-fix in Commit 8 because it is load-bearing for Commit 10 deploy — without it the first Pro signup post-deploy would have hit the same defect. **Webhook noun rename completion — V2 vocab now threaded.** `BillingService.create_checkout_session`, `BillingService.process_pilot_refund`, `TierProvisioningService.premint_for_tier`, and `BillingWebhookService._on_checkout_completed` all now pass `tier=tier, tier_source="stripe_webhook"` (paid path) or `tier_source="free_signup"` (free path) explicitly through `OnboardingService.onboard_tenant`, so the audit row + Admin row consistently carry V2 vocab values regardless of which funnel minted them. The `_on_subscription_deleted` cascade path is unchanged — it reads the existing Admin's `tier` column rather than re-deriving it. **`AuthService.set_password` — atomic email-verified write.** Pre-Commit-8 the magic-link click did a password-hash write but the User row's `email_verified` flag (a new column landing in this commit) was not touched. Post-Commit-8 the same SQL transaction that writes the password hash also sets `user.email_verified = True`, so the two facts can never disagree: if the password hash exists, the email is verified by construction. Magic-link consumption IS the email-verification step. The Arc 6 Commit 8 comment block in the file documents this semantic so a future engineer reading the auth flow understands why the magic-link flow does NOT have a separate `/verify-email` route. **Migration `arc6_b_users_email_verified.py`.** Adds `users.email_verified BOOLEAN NOT NULL DEFAULT false`; down_revision = `arc6_a_admin_widget_domains` so the head chain reads `arc5_c_admin_instance_subtractive → arc6_a_admin_widget_domains → arc6_b_users_email_verified`. Both Arc 6 migrations are deferred to Commit 10b for prod RDS apply via ECS-exec (sandbox-side authoring + repo commit lands here; the buildah image at Commit 10 picks them up). All existing `users` rows backfill to `email_verified=false` at apply time; the next time each pre-existing user does a password-set or password-reset (via the magic-link flow), their row flips to `true` atomically — no separate backfill job needed, and no production user is locked out because the flag is not yet read by any access-control gate (Commit 9 + the unified-signup gate doctrine introduce a "verified" check on a future arc, not at this commit). **`Luciel-Website/src/pages/SignupFree.tsx` (NEW).** Single-form V2 Free signup page: email + display_name + submit. On 200 swaps to a "check your email" panel showing the echoed email address. Error handling tiered by status code: 409 surfaces the backend detail directly (collision copy is user-facing), 422 surfaces the captcha-failed detail, 501 routes the buyer to `hello@vantagemind.ai` (treated as a config error on our side), other / network errors get generic retry copy. The page does NOT yet render the hCaptcha widget — that lands in Commit 9; until then the form submits with no `captcha_token` and the backend soft-passes with the WARN log. **`Luciel-Website/src/lib/billing.ts` extension.** Added `SignupFreeRequest` interface (`email`, `display_name`, optional `captcha_token`), `SignupFreeResponse` interface (`status`, `admin_id`, `email`, `message`), and `signupFree(req): Promise<SignupFreeResponse>` helper that POSTs to `/api/v1/billing/signup-free` via the existing `postJson` machinery. Type-safe end-to-end from the SignupFree.tsx form to the FastAPI route's Pydantic schema. **`Luciel-Website/src/App.tsx` route.** Added `<Route path="/signup-free" element={<SignupFree />} />` alongside the existing `/signup` route, with a comment block explaining why the two routes are distinct (Pro Stripe-Checkout funnel vs Free no-card magic-link funnel). **`Pricing.tsx` deliberately unchanged.** The three CTA shapes already differ correctly at Commit 7: Free → `/signup-free`, Pro → `<WaitlistButton mode="checkout">`, Enterprise → `/contact?tier=enterprise`. The resume plan's "rewrite CTAs to all point at `/signup`" was overridden by partner judgement at resume time — the three shapes already implement the unified-funnel decision (the Free CTA goes to the unified Free signup form; Pro/Enterprise are paid/sales paths that legitimately diverge), and collapsing them would have produced exactly the tier-branching `/signup` shape Commit 7 spent 14 files removing. **`SignupFreeRequest.captcha_token` Optional during Commit-8→9 window.** The Pydantic schema's `captcha_token` field is `Optional[str]` at this commit; Commit 9 flips it back to `Required[str]` once the marketing site ships the hCaptcha widget. The window is sandbox-only because Commit 10 is the deploy gate and Commit 9 ships before it; production never sees a captcha-less `/signup-free`. The route logs every soft-pass at `WARNING` level with the email + remote_ip so the audit gap can be reconciled when Commit 9 lands. **`SignupFreeResponse.email` echo field landed.** The response now echoes the (lowercased) email address the verification link was sent to so the marketing site can render "We sent a link to alice@example.com" without needing to re-derive it from the form state — also useful for the future case where the backend canonicalises the email differently than the form. **Tests — `tests/api/test_arc6_signup_free.py` (NEW, 10 sandbox-passing + 3 CI-gated).** The new Arc 6 Commit 8 contract pins: (i) `SignupFreeResponse.email` echo field exists and is Optional; (ii) admin_id is V2 slug shape `free-<8hex>` via direct import of `_mint_admin_id_from_email`; (iii) the route's inline-imported service symbols (`AuditContext`, `BillingWebhookService`, `_mint_admin_id_from_email`, `OnboardingService`, `TierProvisioningService`, `mint_set_password_token`, `build_set_password_url`, `send_welcome_set_password_email`) all resolve at import time so a future rename trips a clear failure instead of a silent 500; (iv) `OnboardingService.ALLOWED_TIERS_V2` is a module-level set containing `{free, pro, enterprise}` and excluding the V1 vocab `{individual, team, company}` so a V1 backslide is caught at the boundary; (v) `TierProvisioningService.TIER_FREE` and `_DOMAIN_COLLAPSE_SENTINEL = "default"` are present so the Pro pre-mint defect fix cannot be reverted silently; (vi) `User.__table__.columns` contains `email_verified` so the column add is verified at ORM-level; (vii) `auth_service.set_password` source contains `email_verified` so the atomic-write semantic cannot be reverted silently (source-grep via `Path.read_text` rather than `inspect.getsource` because the sandbox lacks `argon2` and importing the module fails at import time — the source-grep boundary is sandbox-safe and CI-equivalent). Three live-route tests (`TestSignupFreeLiveRouteOk`, `TestSignupFreeCaptchaSoftPassLog`, `TestSignupFreeCaptchaInvalidStillHardFails`) are class-decorated with `@pytest.mark.skipif(not _HAS_PSYCOPG, ...)` because the FastAPI app's startup wiring constructs the SQLAlchemy engine eagerly and psycopg is not installed in the sandbox — CI / the runtime image has the driver, so the live-route assertions exercise there. Result: **10 passed, 3 skipped** in sandbox; 13/13 expected in CI. **Tests — `tests/api/test_signup_free_shape.py` (4 stale assertions retired in-place).** The Arc 8 W5 shape test had four assertions that asserted `captcha_token` was required + that the success path returned `status="pending-arc-5"` with no admin_id; both contracts changed in Commit 8. Rather than delete the file (which still pins valuable hCaptcha service unit tests + the Settings field tests + the router-registration test), the four stale assertions were rewritten in-place with `Arc 6 Commit 8` annotation comments explaining the supersession: `test_request_accepts_with_captcha_token` (renamed from `test_request_requires_three_fields`), `test_request_accepts_without_captcha_token_commit8_window` (new, replaces `test_request_rejects_missing_captcha_token`), `test_request_accepts_empty_captcha_token_commit8_window` (new, replaces `test_request_rejects_empty_captcha_token`), `test_missing_token_is_soft_pass_in_commit_8_window` (renamed from `test_missing_token_returns_422_pydantic` with relaxed assertion to `resp.status_code != 422`), and the legacy `test_captcha_success_returns_200_pending_arc_5` is replaced with a comment block pointing readers at `test_arc6_signup_free.py::TestSignupFreeLiveRouteOk` for the new success-path contract. The hCaptcha-service unit tests + Settings tests + router-registration tests are unchanged. **Verification.** AST-parse OK on all 12 touched Python files (9 modified + 1 new migration + 2 test files). Sandbox `python -m pytest tests/api/test_arc6_signup_free.py -v` reports 10 passed + 3 skipped (expected — live-route tests gated on psycopg, present in CI / runtime image). `Luciel-Website` `npx tsc --noEmit` exits 0; `npm test` reports 36/36 pass. **Pillar mapping.** Reliability is strengthened (the route now mints in a single atomic chain rather than half-committing in the Arc-8-W5 stub state where the Admin row never existed; the OnboardingService V2 guard catches V1-vocab slips at the boundary instead of as a 500 in production; the pre-mint defect fix removes a latent IntegrityError that would have hit the first Pro signup post-Commit-10). Maintainability is strengthened (the route docstring + the inline comments at the soft-pass branch + the schema docstring all triangulate on the Commit-8→9 window semantics so a future engineer reading any one of them gets the full picture; the test reshape preserves the Arc 8 W5 hCaptcha service coverage while updating the four contract-shape assertions in place). Traceability is strengthened (every Free signup attempt produces a `signup_free.captcha_soft_pass` WARN line that records email + remote_ip + window tag; the audit row carries `actor_label="signup_free"` for forensic isolation; the AdminAudit chain is unbroken). Scalability is strengthened (the Free / Pro / Enterprise funnels now share a single pre-mint pathway through `TierProvisioningService` with only entitlement caps differing — adding a future tier lands as a CAP table extension rather than a new branch in the route). Security: the Commit-8→9 captcha-soft-pass window is the deliberate trade — the route is sandbox-shipping with a soft-gate that emits structured WARN logs so the gap is audit-grep-recoverable; Commit 9 closes `D-free-tier-captcha-missing-2026-05-22` with the hCaptcha widget on the marketing side + the schema flip back to required + the one-per-email-and-IP accounting; Commit 10 is the deploy gate so production never sees a captcha-less route. **Files in this commit (luciel backend repo).** `alembic/versions/arc6_b_users_email_verified.py` (NEW), `app/models/user.py`, `app/schemas/onboarding.py`, `app/schemas/billing.py`, `app/api/v1/admin.py`, `app/api/v1/billing.py`, `app/services/onboarding_service.py`, `app/services/auth_service.py`, `app/services/billing_webhook_service.py`, `app/services/tier_provisioning_service.py`, `tests/api/test_signup_free_shape.py`, `tests/api/test_arc6_signup_free.py` (NEW), `docs/CANONICAL_RECAP.md` (this entry). **Files in this commit (Luciel-Website repo).** `src/pages/SignupFree.tsx` (NEW), `src/lib/billing.ts`, `src/App.tsx`. **Pause-between-commits.** Per Arc 6 doctrine §Pause-between-commits, Commit 8 is a pause-for-push commit (NOT one of the two no-pause mutation groups Commits 4 + 10). Stage + commit lands locally on both repos at this entry; push pends partner approval. **Branch HEAD pre-Commit-8 (luciel):** `5e9f567`. **Branch HEAD pre-Commit-8 (Luciel-Website):** `fcf6cf8`. **Up next — Commit 8.5:** Account/Billing tier-transition UI — Free→Pro Stripe Checkout (upgrade webhook branch for existing Admin), Pro→Free cancel-at-period-end with block-until-prune safety, Enterprise → contact-sales — adds the in-product tier-transition affordance the unified-signup product redesign is gated on.

- **2026-05-23 (Arc 6 Commit 8.5b — In-product deferred-downgrade paths with LRU overflow archive (Pro→Free, Ent→Pro, Ent→Free), `cancel_at_period_end` timing model, soft-warn confirmation modal, four-axis preview projection, audit-traced schedule→apply state machine, frontend CTAs on `/account` Billing tab):** The reverse side of the in-product tier transitions that Commit 8.5a opened is now closed end-to-end for the three valid downgrade pairs (Pro→Free, Ent→Pro, Ent→Free). A signed-in admin sitting on `/account` under the Billing tab now sees a tier-appropriate "Downgrade to ..." CTA next to the upgrade button, clicking it fetches a per-axis overflow preview, the buyer reads the soft-warn modal showing exactly what would be archived at the period boundary, and confirming arms the Stripe `cancel_at_period_end` state machine — the buyer keeps their current tier until `current_period_end`, can reverse the decision at any time before the boundary by re-upgrading, and at the boundary the webhook handler picks up `customer.subscription.deleted` and runs the LRU archive cascade to land the buyer on the target tier. **What the business gained today.** Three buyer paths that previously required either a sales call (for Enterprise step-downs) or a customer-success ticket (for Pro→Free) now work as in-product self-serve flows with the same caliber of confirmation surface a refund or cancel gets. A Pro admin clicking "Downgrade to Free" sees their over-cap leads, audit retention shortening from 1y to 30d, seat reduction from 25 to 1, and instance reduction from 10 to 1 — each row with the target cap, current usage, and exact archive count — before they commit; an Enterprise admin sees the same surface for both the intermediate Pro target and the floor Free target so they can choose the right landing tier; and the reversibility note in the modal copy makes explicit that the schedule is a *state* (Stripe holds `cancel_at_period_end`), not an event — the buyer can re-upgrade any time before the boundary and the apply branch never fires. Three Locked Decisions anchor the architecture: (1) **Ent→Pro path is cancel-and-email-rebuy** rather than Stripe subscription-update across Products — the apply branch cancels the Enterprise subscription cleanly at boundary and fires a `/signup?tier=pro` magic-link email so the buyer rebuys the Pro Price on a fresh subscription, avoiding the metadata-stamp drift that a Stripe `Subscription.modify(items=[...])` cross-Product swap would introduce; (2) **All downgrades use deferred `cancel_at_period_end` timing**, never immediate — the buyer paid for the current period and keeps every entitlement until that period ends, matching the reversibility doctrine and the audit-friendly "no partial-period entitlement state" property; (3) **Overflow policy is auto-archive LRU with restore-on-re-upgrade within audit_retention**, so an Ent buyer with 47 instances who downgrades to Pro at the boundary keeps the 10 most-recently-used and the other 37 are archived (not deleted) with restoration possible until the Free-tier 30-day audit window closes (or the Pro 1-year window for Ent→Pro). **Scope discipline (locked at slice-1 prep).** Three options were posed for the timing model: (A) deferred for all downgrades (matches upgrade-arc reversibility, matches Stripe-native `cancel_at_period_end` semantics, requires schedule→apply state machine in webhook); (B) immediate for all downgrades (simpler one-shot path, but breaks the period-paid-for entitlement contract and creates partial-period billing reconciliation work); (C) hybrid (immediate for Pro→Free, deferred for Ent transitions). Partner chose A with the framing "deferred for all — matches upgrade arc, reversible, no partial-period state" — rejected B because charging a buyer for a full month and revoking their entitlements mid-period is the kind of trust erosion the six pillars commit to never doing, and rejected C because the asymmetry would require two parallel state machines and two test surfaces for one architectural problem. The Ent→Pro path option-tree similarly weighed Stripe-native subscription-update against cancel-and-email-rebuy and chose the latter under the AGGRESSIVE-CLEANUP doctrine — letting a single Stripe Subscription row carry two different Products across its lifetime would have produced exactly the kind of metadata-stamp drift the Arc 6 Stripe restructure is removing. **What landed (6 slices, file-by-file).** (Slice 1, schema) `alembic/versions/arc6_c_pending_downgrade_columns.py` revision id `arc6_c_pending_downgrade_columns` chained to `arc6_a_admin_widget_domains` adds five columns: `subscriptions.pending_downgrade_target_tier VARCHAR(16) NULL` (CHECK in `'free','pro'`), `subscriptions.pending_downgrade_scheduled_at TIMESTAMPTZ NULL`, `subscriptions.pending_downgrade_audit_id BIGINT NULL` referencing `admin_audit_logs(id)` `ON DELETE SET NULL` for the audit-row backpointer, plus on `scope_assignments` the existing `end_reason` CHECK constraint is extended with a new permitted value `downgrade_overflow_archive` (the literal LRU-archive end reason for seats archived at downgrade boundary); the migration is forward-only with a working `downgrade()` because no data loss is involved (the columns are NULL-able and additive). (Slice 2, ORM) `app/models/subscription.py` gains three `Mapped[Optional[...]]` attributes for the new columns + a `pending_downgrade_audit` relationship; `app/models/instance.py` gains an `is_archived_for_downgrade BOOLEAN` attribute; `app/models/api_key.py` and `app/models/admin_widget_domain.py` gain matching archive flags so the LRU cascade can soft-archive non-seat axes; `app/models/scope_assignment.py` `EndReason` enum gains `DOWNGRADE_OVERFLOW_ARCHIVE = "downgrade_overflow_archive"` exactly matching the new CHECK constraint vocab. (Slice 3, services) `app/services/downgrade_archive_service.py` (NEW) is the LRU-archive engine — `preview_overflow_for_admin(*, admin_id, target_tier) -> OverflowSummary` reads current usage across the four cap axes (instances, seats, leads-per-instance, api-keys-as-proxy-for-widget-domains), reads the target tier's caps from `app/policy/entitlements.py`, and returns a structured `OverflowSummary` with per-axis (cap, current, overflow, archived_ids) where `archived_ids` is the LRU-sorted slice that would be soft-archived if the downgrade applies now; `apply_overflow_for_admin` is the matching write-side that runs in the webhook apply branch and stamps `is_archived_for_downgrade=true` + writes the `EndReason.DOWNGRADE_OVERFLOW_ARCHIVE` scope_assignment audit row. `app/services/tier_provisioning_service.py` gains `downgrade_admin_tier(*, admin_id, target_tier, audit_ctx)` which is the in-place tier flip (mirror of the upgrade-side `provision_upgrade_admin_tier`) called from the webhook apply branch. (Slice 4, Stripe + webhook) `app/integrations/stripe/client.py` gains `schedule_cancellation_at_period_end(stripe_subscription_id)` calling `stripe.Subscription.modify(cancel_at_period_end=True)` (idempotent under Stripe's state-not-event model — calling it twice is a no-op rather than an error); `app/services/billing_service.py` gains `schedule_downgrade(*, admin_id, target_tier, audit_ctx) -> {admin_id, old_tier, target_tier, effective_at, stripe_subscription_id}` which is the schedule-side entry-point (writes the three `pending_downgrade_*` columns, stamps the `ACTION_SUBSCRIPTION_DOWNGRADE_SCHEDULED` audit row, calls the Stripe client method, returns the response dict consumed by the route layer); `app/services/billing_webhook_service.py` `_on_subscription_deleted` handler grows a V2-vs-V1 branch — V2 (any `subscriptions` row carrying a non-NULL `pending_downgrade_target_tier`) routes to `_apply_v2_downgrade` which calls `DowngradeArchiveService.apply_overflow_for_admin` + `TierProvisioningService.downgrade_admin_tier` + stamps `ACTION_SUBSCRIPTION_DOWNGRADE_APPLIED`, V1 (no pending downgrade — buyer canceled outright via the Customer Portal) routes to the existing `_apply_v1_hard_cancel` path which runs the deactivate-cascade unchanged; the branch is keyed on a SQL-level NULL check on `pending_downgrade_target_tier` so a buyer who armed a downgrade and then canceled outright in the Portal still gets the hard-cancel path (the latest-action-wins rule). (Slice 5a, schemas) `app/schemas/billing.py` gains five Pydantic shapes inserted between `UpgradeResponse` and `OnboardingClaimRequest`: `DowngradeRequest` (`target_tier: Literal["free","pro"]`), `DowngradeResponse` (admin_id / old_tier / target_tier / effective_at: str|None / stripe_subscription_id mirroring the service return), `DowngradePreviewRequest`, `AxisOverflowResponse` (axis: Literal of the four canonical labels / cap: int|None for unlimited / current / overflow / archived_ids: list[str]), `DowngradePreviewResponse` (admin_id / current_tier / target_tier / any_overflow / axes: list[AxisOverflowResponse] in stable order). (Slice 5b, routes) `app/api/v1/billing.py` gains two routes: `POST /downgrade` calls `BillingService.schedule_downgrade()` with target-rank-vs-current-rank validation (rejecting `target_rank >= current_rank` as `not_a_downgrade` → 400) and a Free-admin guard mapping the service's `ValueError("no active subscription")` to 400 `no_subscription`; `POST /downgrade/preview` calls `DowngradeArchiveService.preview_overflow_for_admin()` and projects to the four-axis stable-ordered response. Both routes derive `admin_id` via the same `_resolve_owner_admin_id` helper used by the upgrade route (cookie → user → session JWT tenant_id → ScopeAssignment fallback → owner role gate) which raises 401 / 400 `no_admin_for_user` / 403 `downgrade_requires_owner` for the three failure modes — the same cross-admin-attack mitigation Commit 8.5a hardened for upgrades is reused unchanged for downgrades. (Slice 5c, tests) `tests/api/test_arc6_downgrade.py` (NEW, 498 lines) covers six surfaces: `TestDowngradeSchemaShape` (the five Pydantic shapes round-trip through `.model_validate` / `.model_dump` with the locked Literal vocab); `TestDowngradePreviewSchemaShape` (the four-axis projection in the preview response is stable-ordered); `TestDowngradeServiceSignaturePins` (`BillingService.schedule_downgrade` keyword-only signature with the four required args is pinned via `inspect.signature` so any future kwarg-rename surfaces as a test break); `TestDowngradeWebhookSurfacePins` (the three webhook helper names `_on_subscription_deleted` / `_apply_v2_downgrade` / `_apply_v1_hard_cancel` are pinned, and the V2-vs-V1 branch is pinned via the `pending_downgrade_target_tier` NULL-check predicate); `TestDowngradeAuditConstants` (`ACTION_SUBSCRIPTION_DOWNGRADE_SCHEDULED` and `ACTION_SUBSCRIPTION_DOWNGRADE_APPLIED` are present in `ALLOWED_ACTIONS` and `EndReason.DOWNGRADE_OVERFLOW_ARCHIVE` is the enum member with value `"downgrade_overflow_archive"`); `TestDowngradeRouteRegistration` (psycopg-gated — confirms the two routes are registered against the billing router); `TestDowngradeRouteLive` (fixture-gated — exercises the full route → service → DB path in CI). Sandbox run posts **29 passed + 8 psycopg-gated skipped**; live route tests run in CI. (Slice 5d, frontend) `Luciel-Website/src/lib/billing.ts` gains the five matching TypeScript interfaces + two API helpers `downgradeToTier(req)` calling `POST /api/v1/billing/downgrade` and `previewDowngrade(target_tier)` calling `POST /api/v1/billing/downgrade/preview`, both inserted before `signupFree()`; `Luciel-Website/src/lib/analytics.ts` gains three events `account_downgrade_clicked` / `account_downgrade_confirmed` / `account_downgrade_cancelled` matching the upgrade-side analytics shape; `Luciel-Website/src/pages/Account.tsx` `BillingTab` gains the soft-warn modal helper component `DowngradeConfirmModal` (using the existing `@/components/ui/alert-dialog` primitives, with `AXIS_LABEL` map for human-friendly axis names and `formatCap` for the unlimited case), three state hooks `downgradePending` / `downgradePreview` / `downgradeSubmitting`, three handlers `onDowngrade(target_tier)` / `onDowngradeConfirm()` / `onDowngradeCancel()`, and the actual CTAs wired into the Pro branch (single "Downgrade to Free" button with `data-testid="downgrade-to-free"`) and the Enterprise branch (two buttons "Downgrade to Pro" + "Downgrade to Free" with matching test ids), all hidden when `isCanceled || s.cancel_at_period_end` and disabled when a preview fetch or confirm POST is in-flight. The modal is rendered defensively in both the Free branch and the Pro/Enterprise branch as a peer of the subscription card so the state pattern stays uniform. **`npx tsc --noEmit` clean post-rewrite; all 36 vitest tests still pass.** (Slice 5e, this CANONICAL §17 entry.) **Pillar mapping.** Traceability is strengthened (every schedule and every apply writes a row to `admin_audit_logs` with the canonical `ACTION_SUBSCRIPTION_DOWNGRADE_SCHEDULED` / `ACTION_SUBSCRIPTION_DOWNGRADE_APPLIED` actions chained back to the actor's admin_id via `actor_tenant_id` and to the subscription via `pending_downgrade_audit_id`; the LRU-archive write side stamps the matching `EndReason.DOWNGRADE_OVERFLOW_ARCHIVE` on every archived seat). Reliability is strengthened (the deferred-timing model means a webhook failure at boundary surfaces as a missing apply row not as a misbilled buyer — the schedule audit is the source of truth and a retry sweep can replay the apply branch). Maintainability is strengthened (the schedule→apply state machine is keyed on a single column `pending_downgrade_target_tier` whose NULL value is the V1 branch and whose non-NULL value is the V2 branch — no parallel boolean flags, no ambient state). Security is strengthened (the same `_resolve_owner_admin_id` helper that hardened upgrades against cross-admin-attack in Commit 8.5a guards downgrades unchanged — a non-owner ScopeAssignment cannot arm a downgrade on the Admin they happen to share access to). Scalability is preserved (the LRU-archive cascade is bounded by the largest tier-cap delta — Ent→Free at worst archives `unlimited - 1 = ~all` rows, but the soft-archive `is_archived_for_downgrade` flag means the writes are single-column updates not row-deletes, and the audit_retention window gives the buyer restoration headroom). The sixth pillar (designability) is upheld by the soft-warn modal copy — the buyer sees the exact archive count per axis before committing, and the reversibility note makes the schedule→apply distinction unambiguous. **Sibling document changes.** `ARCHITECTURE.md` §3.2.13 grows a Commit 8.5b sub-bullet documenting the schedule→apply state machine, the V2-vs-V1 webhook branch, the `pending_downgrade_*` column triplet, and the `_resolve_owner_admin_id` reuse pattern (deferred to a follow-up commit if not landed in this commit — flagged in the CANONICAL entry so the gap is visible). `DRIFTS.md` carries no new drift from Commit 8.5b — the locked-decision Ent→Pro cancel-and-email-rebuy path is documented in this CANONICAL entry as a deliberate architectural choice not a drift; the metered-Enterprise deferral ("Let's not the metered part right now") remains tracked under the existing Arc 6 Commit 4 deferral entry. **Repo state at end of commit.** `luciel` and `Luciel-Website` are both staged + committed locally with the standard signing pattern (`user.name="Aryan Singh"`, `user.email="aryans.www@gmail.com"`, ai-trailer); push is paused for partner approval per pause-between-commits doctrine. The next Arc 6 commit is Commit 9 (CAPTCHA gate on `/signup-free`) which closes `D-free-tier-captcha-missing-2026-05-22` P1.

- **2026-05-23 (Arc 6 Commit 8.5a — Account/Billing in-product upgrade transitions + Enterprise self-serve checkout scope expansion + `/me` Option A always-200 rewrite + `POST /api/v1/billing/upgrade` endpoint + cross-admin-attack mitigation via cookied-session admin_id derivation + analytics-event registry catch-up):** The in-product upgrade affordance that the unified-signup product redesign was gated on now exists end-to-end for the three forward tier transitions (Free→Pro, Free→Enterprise, Pro→Enterprise). A signed-in admin sitting on the dashboard `/account` route under the Billing tab now sees tier-appropriate upgrade CTAs that open Stripe-hosted Checkout for the target Price, and the webhook landing the resulting `checkout.session.completed` event recognises the upgrade-shape metadata stamp and flips the existing `admins.tier` row in place rather than minting a new Admin. **What the business gained today.** Three buyer paths that previously required either a sales call (for Enterprise) or a deactivate-and-re-onboard dance (for Pro tier-up) now work as in-product self-serve flows: a Free admin clicking "Upgrade to Pro" lands on the Pro-monthly Checkout (cadence defaults to monthly to match the marketing-site default; annual is available from `/pricing`); a Free admin clicking "Upgrade to Enterprise" lands on the Enterprise floor-annual Checkout (the page does not surface a cadence toggle because Enterprise is annual-only at V2 launch per Commit 4 SKU mint); a Pro admin clicking "Upgrade to Enterprise" lands on the same Enterprise floor-annual Checkout, with the webhook upgrade-branch handling the tier flip and the Stripe subscription update so the admin does not end up double-billed across two subscriptions. The pilot intro fee surface (`price_1TXNmnRytQVRVXw7GGfyJiaj`) is deliberately NOT applied on upgrade Checkouts — `create_upgrade_checkout` passes `intro_fee_price_id=None` and `trial_period_days=None` — closing the Free→Pro arbitrage path where a Free admin would otherwise be able to obtain the $100 CAD pilot rate intended for first-time Pro buyers. **Scope discipline + scope expansion (partner-locked at segment start).** Three pre-flight answers were captured before code touched: (1) downgrade safety = **soft-warn confirm modal** applying to ALL downgrades including Enterprise→{Pro,Free} and Pro→Free, deferred to Commit 8.5b; (2) upgrade billing anchor = **Stripe Checkout** (rejected: Stripe Customer Portal, Stripe Billing Update API — both close on the Admin-id derivation path required for the webhook to recognise which Admin to upgrade); (3) proration = **immediate** (charge_automatically + prorate_immediately, the Stripe default). A FOURTH pre-flight scope expansion landed mid-segment: **Enterprise is now self-serve**, not contact-sales. Partner directive verbatim: "It should all be self serve. We have already set the features for each tier so no need to call sales." The Enterprise floor-annual Price ($24,000 CAD) is now buyable through the same WaitlistButton-checkout funnel as Pro; the metered overage surface remains deferred to Arc 7 per the prior "Let's not the metered part right now" directive. This expansion required: (a) marketing Pricing.tsx Enterprise card CTA changed from `<Link to="/contact?tier=enterprise">Talk to sales</Link>` to `<WaitlistButton mode="checkout" cadence="annual">Start Enterprise plan</WaitlistButton>` with `effectiveCadence` forced to `"annual"` regardless of the page-level monthly/annual toggle; (b) the `TierCard.primary` discriminated-union value for Enterprise flipped from `"contact-sales"` to `"checkout"`, the `"contact-sales"` branch removed entirely from `renderCta`; (c) the marketing Signup.tsx hero+SEO copy gained Enterprise branches so the `?tier=enterprise` query-string path lands on a proper Stripe-Checkout-bound view rather than the legacy contact form. **Commit 8.5 was split into 8.5a + 8.5b at this segment.** The original Commit 8.5 spec covered all six tier transitions (three upgrade, three downgrade); the downgrade paths require additional product surface (the soft-warn confirm modal listing what the admin loses on downgrade, the `archive_for_downgrade` overflow logic deciding which instances/leads to retain when capacity tightens, and a `customer.subscription.deleted` webhook tier-flip branch) which is itself a coherent half-commit. Splitting keeps each commit doing one thing well per the pause-between-commits discipline and matches the Arc 5 Commit 5/6 precedent where atomic capability units land separately. **Backend surface changes — schemas (`app/schemas/billing.py`).** Three changes. (1) Drift fix in place: `CheckoutSessionRequest.tier` Literal narrowed from V1 `"individual"|"team"|"company"` to V2 `"pro"|"enterprise"` (the V1 vocabulary had been carried in the schema definition since Step 30a.1 — `D-arc6-checkout-schema-tier-v1-vocab-2026-05-23` opened and closed at this same commit). (2) NEW `UpgradeRequest` Pydantic model with fields `target_tier: Literal["pro","enterprise"]` + `billing_cadence: Literal["monthly","annual"] = "monthly"`; the request shape deliberately does NOT include `admin_id` — the route handler derives it from the cookied session JWT's `tenant_id` claim to prevent cross-admin upgrade injection (see §cross-admin-attack mitigation below). (3) NEW `UpgradeResponse` Pydantic model returning `checkout_url: str` + `session_id: str` mirroring `CheckoutSessionResponse` so the frontend can use one error/success contract for both new-buyer and upgrade-buyer flows. (4) `SubscriptionStatusResponse` extended with `has_subscription: bool` — the discriminant for the `/me` Option A rewrite (see below). **Backend surface changes — services.** (1) `app/services/billing_service.py` gained `create_upgrade_checkout()` at line 446 which is a sibling to `create_checkout()` but stamps the V2 upgrade-metadata pair `luciel_admin_id=<existing admin id>` + `luciel_flow="upgrade"` + `luciel_intro_applied="false"` on the Stripe `subscription_data.metadata` and `checkout_session.metadata` so the webhook can detect upgrade-shape sessions without needing a separate webhook endpoint. (2) `app/services/tier_provisioning_service.py` gained `upgrade_admin_tier()` at line 289 + a new `TierUpgradeNoopError` exception class at line 115 + a `_TIER_ORDINAL` map `{free:0, pro:1, enterprise:2}` enforcing strict-upgrade-direction (downgrades are blocked at the service layer with `TierUpgradeNoopError`, and same-tier "upgrades" are blocked with the same exception — the webhook upgrade-branch catches this and treats it as a noop). (3) `app/services/billing_webhook_service.py` gained a new `_UpgradeBranchFallthrough` sentinel exception at line 96, an upgrade-branch detection block in `_on_checkout_completed` at ~line 381 that inspects the incoming session metadata for `luciel_flow="upgrade"` and routes to `_on_checkout_completed_upgrade()` at line 618 which calls `upgrade_admin_tier()` and updates the existing `subscriptions` row's `stripe_subscription_id` to the new Subscription minted by Checkout — preserving the audit-row chain (`admin_audit_logs`) across the tier transition rather than emitting a synthetic deactivate+reactivate pair. **Backend surface changes — routes (`app/api/v1/billing.py`).** Two changes. (1) **`GET /api/v1/billing/me` rewrite to Option A semantics.** Pre-Commit-8.5a the route returned 404 for Free admins (because no `subscriptions` row exists for Free) which forced the frontend to branch on HTTP status and threaded a `no_sub` UI state through three components — a brittle shape that broke immediately when Free admins gained the in-product upgrade affordance. Post-Commit-8.5a the route ALWAYS returns 200 for any cookied user, carrying `has_subscription: false` for Free admins (with `tier` sourced from `admins.tier`, `status="free"` sentinel, `billing_cadence="none"`, `instance_count_cap` from the new `TIER_INSTANCE_CAPS` `{free:1, pro:10, enterprise:None}` map) and `has_subscription: true` for paid admins (existing shape preserved byte-for-byte). The frontend BillingTab now branches on a single boolean rather than on HTTP status, removing the `no_sub` intermediate state entirely. (2) **NEW `POST /api/v1/billing/upgrade` endpoint at ~line 741.** Cookied-auth via the same dependency the existing `/me` route uses; derives `admin_id` from the session JWT's `tenant_id` claim NOT from the request body (cross-admin-attack mitigation, see below); validates that the calling user has the `owner` role on the target admin (returns 403 `detail="upgrade_requires_owner"` for `admin` role and below); validates that `target_tier` strictly exceeds current tier per `_tier_order` (returns 400 `detail="not_an_upgrade"` for same-tier and downgrade attempts); validates `target_tier="enterprise"` implies `billing_cadence="annual"` (returns 400 `detail="enterprise_monthly"` for Enterprise+monthly — Enterprise has no monthly Price at V2 launch); calls `create_upgrade_checkout()` and returns `UpgradeResponse(checkout_url, session_id)`. Three additional 400 `detail` codes are surfaced for edge cases: `no_admin_for_user` (the cookied user has no Admin assignment at all — should be impossible in normal flow but surfaces clearly if a race lands one), `admin_inactive` (the user's Admin is marked deactivated — upgrade blocked until reactivation), and a generic `BillingNotConfiguredError` → 501 if the Stripe Price IDs are not configured (mirrors `/checkout` behaviour). **Cross-admin-attack mitigation (security pillar).** The naive shape of an `/upgrade` endpoint accepts `{admin_id, target_tier, billing_cadence}` in the request body — but ANY authenticated user could then POST `{admin_id=<some-other-admin-id>, target_tier=enterprise}` and either (a) trigger a Checkout under their card that flips someone else's tier (denial-of-service against the victim Admin), or (b) trigger a Checkout that the attacker then abandons but which still mints a Stripe Subscription against the victim Customer if Stripe's metadata-based webhook routing misfires. The Commit 8.5a shape defends against this by NEVER reading `admin_id` from the request body — the route handler extracts it from the session JWT's `tenant_id` claim (set at login by the session-cookie auth middleware, signed with the SSM-resident `jwt_session_secret`, validated on every request before the route handler runs). A cookied user can therefore only ever upgrade THEIR OWN admin, and they must additionally hold the `owner` role on that admin (verified by an additional `admin_user_roles` lookup inside the route body). The `tenant_id` claim name is retained at its Arc-5 legacy value for cookie-compat reasons; the Arc-5 Commit 6 webhook noun-rename decision applies here too (file-local rename only, claim name unchanged). **Frontend surface changes (`Luciel-Website/src/`).** (1) `src/lib/billing.ts` extended: `SubscriptionStatus` interface gains `has_subscription: boolean`; `billing_cadence` widened from `"monthly"|"annual"` to `BillingCadence | "none"` so the Free shape is type-honest; NEW `UpgradeRequest` + `UpgradeResponse` TypeScript interfaces mirroring the backend Pydantic models; NEW `upgradeToTier({target_tier, billing_cadence}): Promise<UpgradeResponse>` helper that POSTs to `/api/v1/billing/upgrade` with `credentials: "include"` for cookie auth + the standard `BillingApiError` error-mapping pattern used by the rest of the billing client. (2) `src/pages/Pricing.tsx` Enterprise CTA self-serve (see scope expansion above). (3) `src/pages/Signup.tsx` `checkoutEnabled = isBillingEnabled() && (tier === "pro" || tier === "enterprise")` (was Pro-only); forces `cadence="annual"` when `tier===enterprise`; gains Enterprise hero+SEO copy branches. (4) `src/pages/Account.tsx` BillingTab fully restructured: the `no_sub` intermediate state is REMOVED (replaced by the Free branch which renders the upgrade CTAs); the Free branch shows "Upgrade to Pro" + "Upgrade to Enterprise" buttons + an explanatory copy block; the Pro branch shows an "Upgrade to Enterprise" button ABOVE the existing "Manage billing" portal-link button; the Enterprise branch is unchanged (no upgrade target); a new `onUpgrade(target_tier)` handler maps the backend `detail` codes to toast.error copy (`not_an_upgrade` → "You're already on or above this tier", `enterprise_monthly` → "Enterprise is annual-only", `upgrade_requires_owner` → "Only the account owner can change tiers", etc.) and on success calls `window.location.assign(checkout_url)` to hand off to Stripe-hosted Checkout. (5) The WaitlistButton component (which already supports `tier="enterprise"` since Commit 7) is the rendering primitive for both Pro and Enterprise Checkout CTAs on the marketing site — no new component was needed for the Enterprise self-serve scope expansion, just a configuration shift in Pricing.tsx. **Analytics-event registry catch-up (drift-fix bundled in this commit per Path A doctrine).** The `Luciel-Website/src/lib/analytics.ts` `AnalyticsEvent` discriminated-union type was three iterations behind the call sites: it was missing the three `pilot_refund_*` events (added at the Step 30a.5 pilot refund flow), the two `signup_free_*` events (added at Commit 8), and would have been missing the new `account_upgrade_clicked` event added at this commit; additionally the `onboarding_claim_succeeded` payload shape was declared as `{tier}` but the call site at `src/pages/Onboarding.tsx:63` passes `{state}` (the `pending`/`ready`/`unknown` state is what's available at the claim-response layer, the resolved tier is not — F5-2 comment explains this). All four gaps were closed in one sweep: the three pilot events + two signup-free events + the new account-upgrade event registered with their actual call-site payload shapes; the onboarding_claim_succeeded payload type narrowed to `{state: "pending"|"ready"|"unknown"}` matching the call site. The `src/test/pilot.test.tsx` baseline mock SubscriptionStatus was extended with `has_subscription: true` to track the schema change; the same test file's Pricing-card assertions were updated to expect the Enterprise self-serve "Start Enterprise plan" button instead of the legacy "Talk to sales" link. After the catch-up `npx tsc --noEmit -p tsconfig.app.json` is clean and all 36 tests pass. **Drift posture this commit.** NEW + CLOSED at the same commit: `D-arc6-checkout-schema-tier-v1-vocab-2026-05-23` (P3, closed by the `CheckoutSessionRequest.tier` Literal update — was discovered when I AST-grep'd the schema file for V1 vocabulary leakage and found the Literal still naming the V1 tiers, which the route handler did not actually validate against because the body was decoded as a free `dict` in the request handler). NOT touched: `D-free-tier-captcha-missing-2026-05-22` (still P1 OPEN, closes Commit 9 when the hCaptcha widget lands on `/signup-free` and `captcha_token` flips required in the signup-free schema). NOT touched: `D-arc5-tenant-id-column-physical-retention-2026-05-23` (OPEN by design — DB column name retained at the Arc-5 webhook-rename decision boundary). NOT touched: `D-pilot-webhook-text-shape-tests-stale-2026-05-23` (OPEN test-debt, pre-existed before this commit). NOT touched: `D-prod-credential-scope-expansion-protocol-2026-05-23` (OPEN by design — the 5-gate is a process drift not a code drift). **Pillar mapping.** Security is strengthened (cross-admin-attack closed at the request-handler boundary; the cookied-session admin_id derivation pattern is recorded as the canonical shape for any future endpoint that operates on Admin state). Reliability is strengthened (the `/me` Option A rewrite removes the brittle 404-as-data-state pattern; the webhook upgrade-branch preserves the `subscriptions` row identity across tier transitions). Scalability is consciously deferred (downgrade-shape paths land at 8.5b; metered Enterprise overage at Arc 7). Maintainability is strengthened (the analytics-event registry catch-up closes the gap between the type definitions and the call sites; future contributors get a tsc error rather than a runtime no-op when they emit an unregistered event). Traceability is strengthened (every upgrade flips the `admins.tier` row through `upgrade_admin_tier()` which emits an `admin_audit_logs` row with `event_type="tier_upgraded"`, `before_json={tier: <old>}`, `after_json={tier: <new>, source: "stripe_checkout_upgrade", session_id: <checkout session id>}` — the audit chain captures who upgraded what when, settled against which Stripe session). Brand is unchanged (no public-facing copy beyond the in-product upgrade affordance, which uses the established VantageMind voice). **Test posture.** `tests/api/test_arc6_upgrade.py` adds 22 collected tests across four classes: `TestUpgradeSchemaShape` (7 fast tests, schema validation pins, run anywhere); `TestSubscriptionStatusFreeAdminShape` (2 fast tests, schema accepts the Free-admin null-billing shape); `TestUpgradeServiceSignaturePins` (6 fast tests, service methods + exception classes are exported with the expected signatures — these catch any rename without needing the DB); `TestUpgradeRouteRegistration` (3 tests, psycopg-gated because importing `app.api.v1.billing` transitively touches the SQLAlchemy engine factory which needs the psycopg DBAPI — the route registration tests therefore only run in CI/runtime image where psycopg is installed, skipped in the sandbox); and 4 psycopg-gated live route harness tests (`TestMeFreeAdminLive`, `TestUpgradeRouteLive`). Sandbox run: 15 passed, 7 skipped (the 7 psycopg-gated tests skip cleanly with the standard `_skip_no_psycopg` mark mirroring the `test_arc6_signup_free.py` pattern). Out-of-scope this commit: downgrade paths (Pro→Free, Ent→Pro, Ent→Free), soft-warn confirm modals listing what the admin loses on downgrade, `archive_for_downgrade` overflow logic, `customer.subscription.deleted` webhook tier-flip branch — all deferred to Commit 8.5b. The unified-signup product redesign is now fully end-to-end self-serve across the three upward tier transitions; the three downward transitions land in the next commit; the bot-gate (CAPTCHA on `/signup-free`) lands at Commit 9; the migrations carrying the supporting schema additions land in prod at Commit 10/10b.
