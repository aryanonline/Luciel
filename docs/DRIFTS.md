# Luciel — Drifts (Open and Resolved)

**What this document is:** The single ledger of every gap between the design (`ARCHITECTURE.md` + `CANONICAL_RECAP.md`) and the implementation (this repository, and the running production environment in AWS `ca-central-1`). Each gap is a token with a stable id, a status, and a resolution path.

**What this document is not:** A changelog. A version-history dump. A historical narrative. Closed drifts stay in the doc with a strikethrough so the audit chain of decisions remains walkable, but the document does not accumulate sediment beyond that.

**Maintenance protocol:** Surgical edits only. New drifts are appended with a unique id. Closed drifts get the closing commit/tag noted and the heading wrapped in strikethrough (`~~`). Reopened drifts get a new id; the original stays closed.

---

## Section 1 — Status legend and id scheme

**Status values:**

- `OPEN` — gap exists; no fix in flight
- `IN-PROGRESS` — fix is in flight on a named branch or PR
- `RESOLVED` — fix has landed on `main`; closing commit/tag noted; heading wrapped in `~~strikethrough~~`
- `DEFERRED` — gap acknowledged, deliberate decision to not fix this cycle; rationale recorded inline
- `WONTFIX` — gap re-examined and judged not a drift (design changed, or claim was wrong); rationale recorded inline

**Implementation marker scheme** (used in `ARCHITECTURE.md` and `CANONICAL_RECAP.md` once Phase 2 reconciliation begins applying them):

- ✅ Implemented — repo and prod both match the design
- 🔧 Partial — repo or prod implements some of the design; gaps tracked as drifts here
- 📋 Planned — design committed, no implementation yet; tracked as drift here
- 🔬 Decision-gate — design says "we will choose later"; not a drift, an open product decision

**Drift id scheme:** `D-<short-slug>-YYYY-MM-DD`. The date is when the drift was first opened, not when it was discovered later. Slug is short, hyphenated, and stable — never rename a slug after creation.

**Source of truth:** When this document, the architecture document, the canonical recap, the repository, or production disagree, the resolution is recorded here as a drift. The four artifacts converge by closing drifts, not by editing one and hoping the others catch up.

---

## Section 2 — Phase 2A repo reconciliation: how this register was built

This document was rebuilt on 2026-05-09 by walking `ARCHITECTURE.md` and `CANONICAL_RECAP.md` section by section against the repository at commit `c3ed8b6` on `main`. The previous DRIFTS.md (a partial, history-shaped register) was archived to `docs/archive/DRIFTS_pre-reconciliation.md` rather than discarded; that file remains the historical record up to that point.

The comparison covered:

- Architecture §2 — Development environment
- Architecture §3 — Production environment (every subsection)
- Architecture §4 — Cross-cutting properties
- Architecture §5 — Conceptual model
- Canonical Recap §1 — The two layers (Soul and Profession)
- Canonical Recap §2 — Six components
- Canonical Recap §3 — Cognitive abilities
- Canonical Recap §4 — Behavior contracts
- Canonical Recap §6 — Operating loop
- Canonical Recap §10 — Fixed-vs-configurable matrix
- Canonical Recap §11 — Eight strategic answers
- Canonical Recap §13 — End-to-end testing scenarios

Every drift below is tagged with the section that raised it. Production-only drifts (where the repo matches the design but production does not, or where we cannot tell from the repo alone) are tagged `[PROD-PHASE-2B]` and will be confirmed during Phase 2B with the operator at the PowerShell.

---

## Section 3 — Open drifts (repository scope)

### D-channels-only-chat-implemented-2026-05-09

**Status:** OPEN — tracked against roadmap Step 34a
**Raised by:** Architecture §3.2.1, §3.3 step 1; Canonical Recap §13 T8
**Design claim:** Customers reach Luciel through chat widget, voice, email, SMS, and a programmatic API. A channel adapter normalizes each channel into the same internal format.
**Repo state:** Only the chat / programmatic API surface exists (`app/api/v1/chat.py`, sessions, etc.). There is no voice gateway, no email gateway, no SMS gateway, and no channel-adapter layer. `RuntimeRequest.channel` exists as a string field but no dispatch logic differentiates by it.
**Owning roadmap step:** Step 34a (Channel adapter framework — SMS, voice, email, governed by the same scope policy as the chat widget). This drift closes when Step 34a closes.
**Doc-truthing this pass:** Architecture §3.2.1, §3.3 step 1, §3.6 receive 📋 markers pointing to Step 34a. Recap §13 T8 receives a footnote that the multi-channel scenario currently demonstrates chat + programmatic only. No code change.

### D-external-integrations-llm-only-2026-05-09

**Status:** OPEN — tracked against roadmap Step 34
**Raised by:** Architecture §2.2 ("calendar, CRM, email, SMS, voice, payments") and §3.2 (production tools); Canonical Recap §2 (six components: tools), §6 (operating loop step 5)
**Design claim:** External integrations include calendar, CRM, email, SMS, voice, and payments, sandboxed in development and real in production.
**Repo state:** `app/integrations/` contains only `llm/` (Anthropic and OpenAI clients). `app/tools/implementations/` has three internal tools (`escalate_tool`, `save_memory_tool`, `session_summary_tool`) — none of them external integrations. The tool registry shape (`app/tools/registry.py`, `app/tools/broker.py`) is in place — the gap is the integrations themselves, not the framework.
**Owning roadmap step:** Step 34 (Workflow actions — book appointments, send emails, create leads, query business systems on behalf of the user). This drift closes when Step 34 closes.
**Doc-truthing this pass:** Architecture §3.2 gets a "what integrations exist today" subsection so the doc stops implying the full slate is present. Recap §2 (tools component) and Recap §6 step 5 receive 🔧 markers pointing to Step 34.

### D-scope-assignments-history-table-missing-2026-05-09

**Status:** RESOLVING THIS PASS — closes in commit (b) of this reconciliation
**Raised by:** Architecture §4.3 lists `scope_assignments_history` as one of three append-only tables that matter most.
**Repo state:** `app/models/scope_assignment.py` exists; the model docstring describes append-on-change discipline ("create a new one. Two rows in history, never UPDATE in place") using the same `scope_assignments` table as an append-only log. There is no separate `scope_assignments_history` table or migration anywhere in `alembic/versions/` or `app/models/`.
**Resolution:** The design is option (a) — `scope_assignments` is itself the append-only history. The architecture doc was wrong to imply a separate table. Fix Architecture §4.3 to describe the append-on-change pattern explicitly, removing the separate-table claim. Code is correct; doc was wrong.

### D-celery-task-surface-thin-2026-05-09

**Status:** OPEN — partially tracked against existing roadmap; retention purge is the load-bearing gap
**Raised by:** Architecture §3.2.4 (background workers handle "ingesting documents the customer uploaded, refreshing search indexes, sending follow-up emails, running scheduled retention purges, and similar")
**Repo state:** `app/worker/tasks/` contains only `memory_extraction.py`. Document ingestion is built but as foreground code (`app/knowledge/ingestion.py`), not a worker task. There is no search-index refresh task, no follow-up-email task, no scheduled retention-purge task. `app/policy/retention.py` defines the policy; the worker that runs it does not exist.
**Owning roadmap mapping:** Document ingestion-as-worker and follow-up emails are subsumed by Step 34 (workflow actions). **Retention purge is not currently on the roadmap** and it is the load-bearing gap because Architecture §4.4's soft-delete + scheduled-purge guarantee depends on it actually running. Without a running purge worker, soft-deleted rows accumulate forever and the storage-cost story in §4.4 ("the retention worker handles that") is fiction.
**Doc-truthing this pass:** Architecture §3.2.4 gets per-responsibility markers (✅ memory extraction, 🔧 document ingestion as foreground, 📋 the rest). The retention-purge gap is escalated to its own drift token below (`D-retention-purge-worker-missing-2026-05-09`) because it has a load-bearing guarantee depending on it.

### D-context-assembler-thin-2026-05-09

**Status:** OPEN — code-quality drift, no owning roadmap step yet
**Raised by:** Architecture §4.2 ("at runtime, both layers are composed in a single foundation-model context, with the Soul layer placed structurally so it cannot be overridden by injected Profession-layer content")
**Repo state:** `app/runtime/context_assembler.py` produces a 6-line prompt: identity + tenant id + domain id + channel + user message + a stock instruction. The actual production prompt assembly happens in `app/services/chat_service.py` and is more substantive than the assembler suggests, but is not consolidated. Specifically missing: per-scope Profession-layer loading as a coherent step, a structural Soul-vs-Profession separation that prevents prompt injection from overriding Soul rules, and an output-side policy check that catches a model attempting to violate Soul-layer rules.
**Owning roadmap mapping:** None today. This is a refactor + a small policy addition, not a feature, so it does not naturally belong to one of the existing roadmap steps. Best fit is as a prerequisite of Step 33 (evaluation framework) since evaluation needs a single observable assembly point. Defer assignment; track as a code-quality drift.
**Doc-truthing this pass:** Architecture §4.2 gets a 🔧 marker. The drift stays open as a known refactor with no scheduled date — flagged for a future roadmap-grooming pass.

### D-untracked-diag-files-persist-2026-05-09

**Status:** OPEN — sandbox-side gitignore widening attempted in commit (d) of this pass; operator-side survey deferred to Phase 2B
**Raised by:** Operator observation; not a doc claim, but a repo-hygiene drift carried forward from the previous reconciliation pass.
**Repo state:** Approximately 47 untracked diagnostic files persist in the working tree across recent sessions despite the gitignore-widening commit `cea23af`. The sandbox tree is clean (these accumulate on the operator's local clone during diagnosis sessions); we cannot survey them from here directly. We can only widen `.gitignore` for patterns we anticipate.
**Resolution path:** This pass widens `.gitignore` for any plausibly-missing patterns based on what we know operators produce (PowerShell transcripts, ad-hoc probe outputs, evidence captures). Phase 2B confirms the actual untracked set on the operator's clone and tightens further if needed.

### D-canonical-recap-q1-q8-roadmap-genericized-2026-05-08

**Status:** OPEN (self-correction, will be moved to Section 5 in commit (a) of this pass)
**Raised by:** Operator catch on 2026-05-08; the Q1-Q8 strategic answers and roadmap had been genericized in an earlier pass before being restored from `archive/CANONICAL_RECAP_v3.4.md`.
**Repo state:** Restored verbatim in commit `2903bc4` (Step 29.y postmerge, recap restore from v3.4) and re-incorporated into the product-philosophy-first rewrite in commit `e56887b`. The canonical recap on `main` at `c3ed8b6` carries the substantive answers.
**Resolution path:** Self-correction recorded for audit. Moves to Section 5 in this commit (commit (a) of the reconciliation pass).

### D-retention-purge-worker-missing-2026-05-09

**Status:** OPEN — load-bearing; needs a roadmap home
**Raised by:** Split out from `D-celery-task-surface-thin-2026-05-09` because it has a load-bearing architectural guarantee depending on it.
**Design claim:** Architecture §4.4 ("The retention worker handles that, in batches sized to coexist with live traffic without lock contention"); Architecture §3.2.4 lists "running scheduled retention purges" as a worker responsibility; Architecture §3.4 commits "a retention purge cannot break the audit chain" which presumes a purge worker exists.
**Repo state:** `app/policy/retention.py` defines the retention policy. There is no scheduled task in `app/worker/tasks/` that runs the purge. Soft-deleted rows accumulate without bound today.
**Operational impact:** Storage cost grows with every soft-delete and never shrinks. Customers cannot be told "data is purged after the contracted retention period" with a straight face until this lands. Compliance posture is also weaker than the architecture claims.
**Owning roadmap step:** None today. Recommend adding to roadmap (this pass does not unilaterally add it because retention purge is enough work — schedule, idempotency, lock semantics, audit emission to `deletion_logs`, end-to-end test — that it deserves its own line and operator buy-in before scheduling). For now, flagged here as a known gap requiring a near-term roadmap decision.
**Doc-truthing this pass:** Architecture §3.2.4 and §4.4 receive 📋 markers; the prose stays unchanged because the design is correct, the implementation is missing.

### D-route-shipped-without-end-to-end-coverage-2026-05-10

**Status:** OPEN (broader-routes slice only) — widget-surface slice CLOSED on 2026-05-11 by Step 30d Deliverable C (PR #16 merged `146f133`); harness at `.github/workflows/widget-e2e.yml` + `ci/e2e/run_widget_e2e.sh` + `ci/e2e/assert_widget_stream.py` + `scripts/bootstrap_platform_admin_ci.py` behind `workflow_dispatch` in v1, with AST contract test `tests/api/test_widget_e2e_harness_shape.py` pinning the cross-file invariants in the existing backend-free CI lane; planned follow-up adds `pull_request` trigger alongside `workflow_dispatch` per Pattern E (never delete). The broader "any route can ship without an E2E gate" process gap stays OPEN beyond Step 30d and still needs a roadmap home
**Raised by:** Step 30b Phase 4 staging E2E. The widget endpoint (`POST /api/v1/chat/widget`) shipped to `main` with three latent first-request bugs, each of which would have been caught by a single end-to-end POST against a deployed instance:
  1. `D-bug-scope-policy-kwarg` — `create_embed_key` called `enforce_agent_scope(agent_id=...)` but the policy method's parameter name is `scope_id`. Mint route 500. Fixed in PR #4 / commit `1aba06a`.
  2. `D-bug-slowapi-limit-provider-signature` — `embed_per_minute_limit_string(request: Request)` did not match either of slowapi's two call shapes (zero-arg or one-arg-keyed). Every widget POST returned a 500 with no CORS headers, surfaced in the browser as a misleading `No 'Access-Control-Allow-Origin' header` error. Fixed in PR #5 / commit `a64cdba` by replacing the broken callable with a static `EMBED_WIDGET_RATE_LIMIT = "30/minute"`.
  3. `D-bug-sessionmodel-attr-name` — widget route read `session.session_id` after `session_service.create_session(...)`. `SessionModel`'s primary key column is `id`, not `session_id`. AttributeError on first widget turn. Fixed in PR #6 / commit `d4720ae`.
**Design claim:** Step 30b's success criterion (CANONICAL_RECAP.md §12) is "customers can drop a script tag and Luciel answers from the customer's content." That criterion was meant to be enforced by CI gating the merge.
**Repo state:** `.github/workflows/ci.yml` runs unit tests, AST checks, widget bundle size gate, and widget CDN deploy. It does **not** spin up the backend image and make a real HTTP POST against `POST /api/v1/chat/widget` with a freshly minted embed key. The widget surface was therefore covered only by Python imports and per-function unit tests, neither of which exercises the slowapi callback, the auth middleware chain, the ScopePolicy kwarg surface, the JSON request parser, or the SessionModel attribute the route reads. Three bugs landed on `main` and reached the staging deploy before any of them fired.
**Owning roadmap step:** Partially owned by **Step 30d (Widget content safety and scope guardrails)** — the CANONICAL_RECAP §12 row for Step 30d explicitly commits to "an automated test [that] proves both behaviors on every change to the widget surface," which is the containerized end-to-end gate this drift requires for the widget slice: on every PR touching `app/api/v1/chat_widget.py`, `app/api/v1/admin.py` (embed-keys handler), `app/api/widget_deps.py`, or `app/middleware/auth.py`, (a) brings up backend + Postgres + Redis, (b) seeds a tenant + domain + embed key, (c) POSTs a widget message, (d) asserts HTTP 200, `text/event-stream` content-type, and a non-empty first SSE frame, plus (e) asserts the scope-refusal and content-safety behaviors land. The non-widget slice of this drift — the general "any route can ship without an E2E gate" process gap — remains OPEN beyond Step 30d and needs a future roadmap home. This drift **partially closes** with Step 30d (widget-surface PRs gated end-to-end); it fully closes only when the gate is extended to all customer-reachable routes.
**Doc-truthing this pass:** No CANONICAL_RECAP or ARCHITECTURE prose change. The Step 30b row in §12 stays 🔧 until the CI gate is in place and the staging widget passes end-to-end (technical green + safety green per the drift below). This drift's widget-surface slice closes when Step 30d closes; the broader-routes slice stays open beyond Step 30d.
**Closeout update 2026-05-11:** Widget-surface slice closed on Step 30d landing as recorded in the Status line above; this entry stays in §3 because the broader-routes slice remains OPEN. Owning roadmap step for the residual slice is still pending. The Step 30b row stays 🔧 against its own success criterion (REMAX-customer visitors exchanging real conversations); the closure here does not flip that row.

### D-widget-chat-no-application-level-audit-log-2026-05-10

**Status:** OPEN — observability gap; belongs to Step 31 (dashboards + validation gate), not a Step 30b blocker
**Raised by:** Step 30b Phase 5 staging audit-log spot check. Four `POST /api/v1/chat/widget` calls were sent through the live widget at 02:09:42, 02:09:53, 02:10:44, 02:11:08 UTC on 2026-05-11. CloudWatch log group `/ecs/luciel-backend` carries only uvicorn access lines (HTTP method, path, source IP, status code) for those four turns. A targeted CloudWatch `filter-log-events` against the same time window with pattern `?tenant_id ?domain_id ?embed_key ?session_id ?widget_chat ?chat_completion` returned **zero** events. The widget chat request path emits no application-level structured log lines — no per-turn record carrying `tenant_id`, `domain_id`, `embed_key_prefix`, `session_id`, message length, latency, or model used.
**Design claim:** CANONICAL_RECAP §13 (end-to-end product acceptance) implies per-tenant observability for any customer-facing surface — "we can answer is Luciel earning its keep here in under a minute." ARCHITECTURE §3.2.7 (Audit trail) states that every consequential action produces an immutable audit record across three channels, one of which is the application log stream where the application emits audit events in human-readable form. The widget chat path emits none of these for a chat turn — only uvicorn access lines.
**Repo state:** `app/api/v1/chat_widget.py` does not emit explicit `logger.info(...)` records at request entry, after dependency resolution, or after the LLM response is streamed. The chat service does not appear to emit per-turn audit records either (no `audit_logs` table write, no structured log line). The only application-level evidence today that a widget conversation happened is the `messages` and `sessions` row writes — which are state, not observability — plus uvicorn's HTTP access line, which carries only path and status.
**Operational impact today:** For the REMAX preview, we have HTTP-level monitoring only. We cannot: (a) attribute a tenant-specific widget 500 to a specific tenant from logs alone (we would have to correlate source IP against CloudFront access logs and then DB rows); (b) build a usage-by-tenant chart for Step 31 from log data; (c) prove from logs that a sensitive-content refusal happened versus a generic 200 with empty SSE; (d) target a retention-purge sweep on widget audit records (none exist). For a single controlled demo customer this is tolerable. For a paying-customer cohort it is not.
**Owning roadmap step:** **Step 31 (dashboards + validation gate).** Step 31 already commits to a five-part pre-launch validation gate including operations and compliance categories — both of those require the structured logs this drift identifies as missing. Folding this into Step 31 rather than spawning a Step 30e because (1) the fix shape — structured loggers, audit-log table write, dashboard ingest — is the same fix that Step 31 needs anyway, (2) splitting it would force Step 31 to inherit a half-built audit pipeline, (3) the widget surface can ship to a single demo customer (REMAX) without it as long as we are honest with REMAX that detailed per-conversation metrics arrive in the next milestone. This drift closes when Step 31's validation gate exercises a widget turn and verifies the structured record landed with the documented field set.
**Doc-truthing this pass:** Step 30b row in CANONICAL_RECAP §12 updated to record stage-1 (Luciel-owned staging E2E) observed clean on 2026-05-10, with the three bugs found-and-fixed and the three open drifts (this one, route-coverage, content-safety) called out by id. Marker stays 🔧 — the row's literal success criterion is REMAX-customer visitors exchanging real conversations, which has not happened yet. ARCHITECTURE §3.2.7 receives a 📋 marker on the "Application log stream" bullet pointing to Step 31; the prose stays unchanged because the design is correct, the implementation for the widget surface is incomplete.

### D-celery-worker-runs-as-root-2026-05-11

**Status:** OPEN — pre-existing security hygiene gap surfaced (not introduced) during the Step 30c prod deploy of 2026-05-11.
**Raised by:** Post-deploy log review of the new `luciel-worker:19` task on `luciel-worker-service` after rolling out the action-classification gate. The `/ecs/luciel-worker` log group shows Celery emitting its standard `SecurityWarning: You're running the worker with superuser privileges: this is absolutely not recommended! Please specify a different user using the --uid option. User information: uid=0 euid=0 gid=0 egid=0` at process startup. This warning has been firing on every worker boot since at least rev 14 — it is not new to rev 19. Step 30c happens to be the first time we noticed and recorded it.
**Design claim:** ARCHITECTURE §6 (security) and the broader principle of least privilege in CANONICAL_RECAP §3 (senior-advisor voice extends to operational discipline) both imply the worker should not run as root. Containers should run as a least-privileged non-root user wherever the workload does not require root.
**Repo state:** `Dockerfile` does not contain a `USER` directive after the install steps; the image therefore runs as `root` (uid=0). The task definitions `td-backend-rev39.json` and `td-worker-rev19.json` do not override this via a `user` field in the container definition. The worker's actual workload (Celery against SQS, `/tmp/celery_alive` mtime heartbeat for the ECS health check) requires no root privilege.
**Operational impact today:** Low. The worker container is sandboxed by Fargate, talks only to SQS, RDS, and Redis via IAM-bound endpoints, and has no inbound network surface. A root-inside-the-container compromise still has to escape Fargate to do anything meaningful. But the warning is a hygiene-and-audit finding: it is the first thing a security reviewer screenshots, and we want a clean log on a customer-facing security questionnaire.
**Resolution path:** Add a non-root user to the `Dockerfile` (e.g. `RUN groupadd -r luciel && useradd -r -g luciel luciel` then `USER luciel` before the `CMD`), confirm `/tmp/celery_alive` is writable by that user (it is, since `/tmp` is world-writable in the base image), rebuild + redeploy both `luciel-backend` and `luciel-worker` through the same digest-pinned task-def revision pattern used for Step 30c. Owning roadmap step: not currently assigned — candidates are folding it into Step 31's compliance pillar or opening a small `Step 30e` hardening step. Decision deferred to a future planning pass.
**Cross-refs:** Surfaced in DRIFTS §5 `~~D-confirmation-gate-not-enforced-2026-05-09~~` (the Prod observed-clean stanza records where this was first noticed). `docs/runbooks/step-30c-action-classification-deploy.md` is the runbook whose post-deploy log review exposed it.

---

## Section 4 — Drifts pending production confirmation [PROD-PHASE-2B]

These are claims in `ARCHITECTURE.md` whose verification requires operator-side access to the AWS account. Each will be walked together with the operator step by step in Phase 2B. The repository alone cannot confirm or deny any of them.

### D-prod-waf-presence-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.1 and §3.6 diagram (WAF in front of ALB)
**Repo state:** No CFN template in `cfn/` provisions a WAF Web ACL. No infra-as-code claim of a WAF.
**Resolution path:** Operator confirms whether a WAF Web ACL is associated with the production ALB. If yes, capture its arn and the rule set. If no, mark §3.2.1's WAF claim as 📋 Planned and open a roadmap item.

### D-prod-multi-az-rds-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.5 ("hot standby in a second availability zone"), §3.6 diagram (PG_PRIMARY ↔ PG_STANDBY)
**Repo state:** No CFN template provisions the RDS instance. The repo cannot confirm Multi-AZ.
**Resolution path:** Operator runs `aws rds describe-db-instances` (read-only) and confirms `MultiAZ: true` and the standby AZ. If single-AZ, mark §3.2.5's standby claim as 📋 Planned.

### D-prod-app-tier-pool-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.3 ("a pool of application servers"), §3.5 ("application server crash" recovery story)
**Repo state:** Task definitions exist (`td-backend-rev39.json` as of the Step 30c prod deploy on 2026-05-11; previously `td-backend-rev34.json`) but desired-count and minimum-pool size are an ECS service property, not a task-def property.
**Resolution path:** Operator confirms desired count and minimum healthy count of the backend ECS service. If desired count is 1, mark §3.2.3's pool claim as 🔧 Partial and open a roadmap item to raise the floor.

### D-prod-worker-autoscaling-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.4 ("worker autoscaling on queue depth and CPU")
**Repo state:** `cfn/luciel-prod-worker-autoscaling.yaml` exists. Need to confirm it is deployed and active in the prod account.
**Resolution path:** Operator confirms the worker-autoscaling stack is in `CREATE_COMPLETE` or `UPDATE_COMPLETE` state and the scaling policies are attached to the worker service.

### D-prod-alarms-deployed-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.10 (four signal kinds, alarms thresholds: 1% error, 1000 queue depth, 80% DB connection saturation) — corrects a pre-existing off-by-one (was §3.2.8 Integrations, content described is Monitoring)
**Repo state:** `cfn/luciel-prod-alarms.yaml` exists. Need to confirm deployment.
**Resolution path:** Operator confirms the alarms stack is deployed and the alarms are in `OK` state (not `INSUFFICIENT_DATA`).

### D-prod-kms-customer-managed-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.8 ("encrypted with a customer-managed KMS key")
**Repo state:** Verify task role (`cfn/luciel-verify-task-role.yaml`) references KMS Decrypt via SSM service principal but does not name the key. Cannot confirm whether the key in use is AWS-managed (`aws/ssm`) or customer-managed.
**Resolution path:** Operator runs `aws ssm describe-parameters` for one of the SecureString parameters, captures the `KeyId`, then `aws kms describe-key` on it. Confirm `KeyManager: CUSTOMER`. If `AWS`, mark as 🔧 Partial — works the same operationally but rotation control is AWS's, not ours.

### D-prod-secrets-pattern-e-unverified-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.2.8 ("Pattern E: secrets are deactivated, never deleted")
**Repo state:** SSM Parameter Store does not natively support Pattern E (deactivated flag) — it supports versioning. The current implementation pattern needs documentation.
**Resolution path:** Operator inventories SSM parameters with `aws ssm describe-parameters` and confirms the operational pattern: are old versions retained (relying on SSM history) or are deactivated rotated parameters retained as separate parameter names with a `-deactivated-YYYY-MM-DD` suffix? The doc should reflect what we actually do; right now §3.2.8 implies a discipline the implementation may not have.

### D-prod-celery-broker-sqs-vs-redis-2026-05-09

**Status:** OPEN [PROD-PHASE-2B]
**Raised by:** Architecture §3.6 diagram (`Job queue Redis/SQS`); §3.2.4 prose does not name SQS.
**Repo state:** `app/worker/celery_app.py` lines 116-138 explicitly document a two-mode broker design: SQS in production, Redis in development. Production task definitions inject `REDIS_URL` from SSM. The architecture document does not commit clearly to SQS-in-prod.
**Resolution path:** Update Architecture §3.2.4 to state SQS as the production broker explicitly, with the rationale already in `app/worker/celery_app.py` lines 132-138 (ElastiCache cluster mode incompatible with kombu's MULTI/EXEC). Confirm with operator that production is in fact running SQS-mode (not Redis broker via SSM `REDIS_URL`). If REDIS_URL is the actual broker, the architecture is wrong; otherwise the architecture is incomplete and we tighten it.

### D-prod-sqs-stale-messages-2026-05-09

**Status:** OPEN [PROD-PHASE-2B] (operator-flagged, symptom description pending)
**Raised by:** Operator on 2026-05-09 ("SQS stale messages drift"); not yet captured in either design doc.
**Repo state:** Pending operator description.
**Resolution path:** Operator describes the symptom — what is stale, how it manifests, what was observed in CloudWatch or in queue inspection. Drift will be expanded with the description, then we decide whether the cause is a design gap (e.g., visibility timeout vs job duration), a code bug (idempotency or ack handling), or an operational incident (poison messages stuck redelivering).

### D-memory-truncation-2026-05-09

**Status:** OPEN [PROD-PHASE-2B] (operator-flagged, symptom description pending)
**Raised by:** Operator on 2026-05-09 ("memory truncation drift"); possibly distinct from the audit-note 256-char cap (test exists in `tests/integrity/test_audit_note_length_cap.py`).
**Repo state:** Pending operator description.
**Resolution path:** Operator describes which memory is being truncated — session, user preference, domain, or operational — and at what boundary (storage column length, retrieval window, model context). Drift will be expanded with the description, then we decide whether the truncation is a column-length cap, a retrieval-window heuristic, or a model-context budget cap.

---

## Section 5 — Resolved drifts (kept for audit)

Closed drifts are kept here permanently with a strikethrough heading. The closing commit, the closing tag (if applicable), and a one-line resolution note are recorded. This makes the audit chain of design decisions walkable backwards; nothing is rewritten or deleted.

### ~~D-step29y-impl-no-close-tag-2026-05-07~~

**Status:** RESOLVED on 2026-05-08
**Closing tag:** `step-29y-complete` on commit `5f297b7`
**Resolution:** Step 29.y implementation work was tagged `step-29y-complete`, providing a stable anchor for "all step-29.y code merged."

### ~~D-canonical-recap-v3.4-omits-step-29x-29y-2026-05-07~~

**Status:** RESOLVED on 2026-05-08
**Closing commit:** `3e8678d` (Step 29.y close-out C34/C35: 3-doc regime adoption + archive of legacy docs)
**Resolution:** The legacy `CANONICAL_RECAP_v3.4.md` (which omitted Step 29.x and 29.y) was archived to `docs/archive/CANONICAL_RECAP_v3.4.md`. The new `CANONICAL_RECAP.md` is the single living canonical recap going forward, written product-philosophy-first per the user's revised framing.

### ~~D-canonical-recap-q1-q8-roadmap-genericized-2026-05-08~~

**Status:** RESOLVED on 2026-05-08; entry retained for audit
**Closing commits:** `2903bc4` (verbatim restore from v3.4) → `e56887b` (rewrite with Q1-Q8 + roadmap intact, "How we'll know" columns filled in business-observable language) → merged to main as `08a830e`
**Resolution:** A pre-restore version of the recap had genericized the eight strategic answers (Q1-Q8) and the roadmap. Operator caught it; restore + targeted rewrite preserved the substantive content while reframing for product-philosophy-first.

### ~~D-untracked-diag-artifacts-no-gitignore-2026-05-08~~

**Status:** RESOLVED on 2026-05-08 (initial pass)
**Closing commit:** `cea23af` (Step 29.y gap-fix C33a: repo cleanup, delete 37 stale operational JSONs and extend .gitignore for diag artifacts)
**Resolution:** Initial sweep of stale operational JSONs and .gitignore widening landed. **Note:** A second wave of untracked diagnostic files has appeared since (see open drift `D-untracked-diag-files-persist-2026-05-09`); the original closure remains valid for the patterns it covered, and the new patterns are tracked separately rather than as a re-open of the same id.

### ~~D-pattern-e-architecture-doc-missing-2026-05-08~~

**Status:** RESOLVED on 2026-05-09
**Closing commit:** `bf7dff4` (Architecture rewrite as design-target document)
**Resolution:** Pattern E (deactivate, never delete) is now an explicit cross-cutting property in Architecture §4.6, with the rationale, and is also referenced in §3.2.8 (secrets) and §4.5 (cascade-correct departure). The implementation pattern in production for SSM specifically is still tracked as open (`D-prod-secrets-pattern-e-unverified-2026-05-09`) until verified.

### ~~D-architecture-doc-implementation-snapshot-not-design-2026-05-08~~

**Status:** RESOLVED on 2026-05-09
**Closing commits:** `bf7dff4` (Architecture rewrite as design-target) → merged to main as `c3ed8b6`
**Resolution:** Architecture is now explicitly a design-target document, with an explicit reconciliation protocol (this DRIFTS.md). The previous implementation-snapshot framing (which conflated "what we built" and "what we intend to build") is gone. Marker scheme adopted but not yet applied — that is Phase 2 work and is the work this document tracks.

### ~~D-tenant-language-vs-scope-language-2026-05-09~~

**Status:** RESOLVED on 2026-05-09
**Closing commit:** `bf7dff4` (architecture rewrite, terminology sweep)
**Resolution:** Operator caught isolation/enforcement claims framed at the tenant level when the actual guarantee is at the scope level (any of tenant, domain, agent, or instance). 7-edit terminology sweep applied: scope language used for isolation guarantees; tenant/domain/agent/instance reserved for cases where the specific scope level genuinely matters (per-tenant DB tier, named memory kinds, hierarchy diagrams).

### ~~D-embed-key-issuance-workflow-missing-2026-05-09~~

**Status:** RESOLVED on 2026-05-09
**Closing tag:** `step-30b-embed-key-issuance-complete` on the merge of branch `step-30b-embed-key-issuance` into `main`
**Closing commits:** `64ee2a3` (commit (a): EmbedKeyCreate / WidgetConfig / EmbedKeyRead / EmbedKeyCreateResponse schemas, four widget kwargs on `ApiKeyService.create_key`, `EMBED_REQUIRED_PERMISSIONS` re-exported from `app.api.widget_deps` for backward compat) → `fe296a9` (commit (b): `POST /admin/embed-keys` endpoint with three scope guards and audit-before-commit) → `f7b8b6f` (commit (c): `scripts/mint_embed_key.py` non-interactive flag-based CLI)
**Resolution:** The issuance path now exists in two operator-facing forms (HTTP and CLI), both funneling through the same `EmbedKeyCreate` schema and the same `ApiKeyService.create_key` entrypoint, so they cannot drift on validation or audit behavior. Embed keys land with `key_kind='embed'`, `permissions=['chat']`, validated `allowed_origins` (exact scheme+host+optional port, no wildcards/paths), `rate_limit_per_minute` capped at 10000, and a 3-knob `widget_config` (accent_color, display_name, greeting_message) with HTML rejection. SSM write is rejected for embed keys at the service layer because the customer cannot read SSM. The audit row lands in the same transaction as the `api_keys` INSERT (Invariant 4). 69 offline assertions cover schema, service, runtime gate, HTTP surface, and CLI. Step 30b's success criterion ("within an hour") still has one outstanding blocker: the widget bundle CDN, tracked separately at `D-prod-widget-bundle-cdn-unprovisioned-2026-05-09`.

### ~~D-ci-ast-job-red-on-main-2026-05-08~~

**Status:** RESOLVED on 2026-05-10 (this commit on `step-30b-cleanup`)
**Closing commit:** `9e9cb9e` (this branch's commit (b): `.github/workflows/ci.yml` step "run http_client params + health pytest tests" gains a step-scoped `env: DATABASE_URL: "sqlite:///:memory:"`)
**Closing verification:** Next CI run on the merge of `step-30b-cleanup` into `main` must show the AST job green.
**Raised by:** Discovery on 2026-05-10 while watching the first widget-cdn-deploy run end-to-end. The `gh run list --branch main` history showed the `ast-and-unit-tests` job had been silently red on every push to `main` for at least nine consecutive merges (2026-05-08 `5f297b73` through 2026-05-10 `85990c4`), in every case interrupted by the same collection error: `tests/api/test_health.py` imports `app.main`, which transitively imports `app.core.config` and instantiates `settings = Settings()` at module-load time; `Settings.database_url: str` has no default, so a CI runner with no `DATABASE_URL` exported fails the Pydantic field validator before any test runs.
**Why it persisted unseen:** The CI gate landed in Step 29 D (`89afbae`) at the same time `tests/api/test_health.py` was added. Both pieces work locally because development environments export `DATABASE_URL` from `.env`. The first push to `main` after Step 29 D was the first run with a bare-runner environment, and the failure was already in place. Subsequent merges all failed for the same reason and no one watched the run output. This is the doctrine "red signals stop being signals when ignored" failure mode — visible only because the widget-cdn-deploy run brought the Actions tab back under daily attention.
**Resolution:** `tests/api/test_health.py` calls `/health`, which returns `{"status": "ok", "service": settings.app_name}` and does not touch the DB. So the CI fix is to provide the minimum literal value that lets `Settings()` validate and `create_engine()` resolve a dialect: `sqlite:///:memory:`. SQLite ships with SQLAlchemy core; no install-set change is required. The `env:` block is scoped to the one step that needs it, so the next step (pillar harness collection) keeps its no-DATABASE_URL skip-cleanly contract.
**What is deliberately NOT done:** `Settings.database_url` is still required-with-no-default. Loosening that field would make the model tolerate a misconfigured production runtime, which is the wrong tradeoff. The CI shim is the right place for the workaround, not the model. A future refactor that wants AST-only static-analysis CI would lazy-instantiate `settings` or split `BootSettings` from `RuntimeSettings`; that's a Step-29 verification-doctrine question, out of scope here.
**Pattern E:** No application behavior changed. The production runtime path is untouched. This is a workflow-file-only edit adding a step-scoped env var.

### ~~D-prod-widget-bundle-cdn-unprovisioned-2026-05-09~~

**Status:** RESOLVED on 2026-05-10
**Closing tag:** `step-30b-widget-cdn-complete` on commit `5ffd42d` (merge of `step-30b-cleanup` into `main`)
**Closing commits, in order:** `ae87491` (cfn/luciel-widget-cdn.yaml, S3 + CloudFront + OAC + GitHub OIDC deploy role) → `903d39e` (scripts/deploy_widget_cdn.sh, hashed + stable-alias upload + invalidation) → `0cc2ecb` (.github/workflows/ci.yml, widget-cdn-deploy job using GitHub OIDC, HEAD-check after upload) → `cb0c00a` (docs progress on `step-30b-widget-cdn-deploy`) → `ef7cbf7` (cfn IAM fix: `cloudfront:GetInvalidation` added to deploy role) → `9e9cb9e` (CI fix: step-scoped `DATABASE_URL` stub on `tests/api/test_health.py`, closes the parallel AST red-window) → `8ad59e4` (docs progress on `step-30b-cleanup`) plus this chase-up commit's own SHA on `main`.
**Verifying CI run:** `25628714323` on merge `5ffd42d` — all three jobs green end-to-end: widget bundle build + size gate (9s), AST + unit tests (41s), widget bundle deploy to CDN (34s, including the previously-failing `aws cloudfront wait invalidation-completed` step now polling cleanly with `cloudfront:GetInvalidation`).
**Independent verification:** Outside-network `curl -I https://d1t84i96t71fsi.cloudfront.net/widget.js` returns `HTTP/2 200`, `content-type: application/javascript; charset=utf-8`, `content-length: 27300`, `cache-control: public, max-age=300, must-revalidate` (`x-cache: Miss from cloudfront` immediately after the verifying-run invalidation, with cache filling on subsequent hits). Hashed immutable alias `https://d1t84i96t71fsi.cloudfront.net/luciel-chat-widget.36a25740a60c.js` returns the same body with `cache-control: public, max-age=31536000, immutable`.
**Resolution:** Customers can now drop a `<script src="https://d1t84i96t71fsi.cloudfront.net/widget.js" data-luciel-api-base="..." data-luciel-embed-key="..."></script>` tag into any origin and the bundle loads under the stable cacheable URL the design claim required. Every push to `main` that touches `widget/`, `cfn/luciel-widget-cdn.yaml`, the deploy script, or the deploy job re-runs the size-gated bundle through the OIDC-assumed deploy role and re-publishes both the hashed and stable-alias paths idempotently.
**Pattern E:** No row mutations. CDN artifacts are forward-only — old hashed paths stay reachable for any customer that pinned a specific version. The IAM role identity stayed the same across the policy update; only its action list grew by one read-only verb (`cloudfront:GetInvalidation`).
**What remains scoped separately:** Step 30b row in CANONICAL_RECAP §12 still depends on a real customer exchanging messages through the published bundle (Luciel-owned staging first, then REMAX Crossroads as the first paying-customer drop). The CDN drift is closed; the customer-validation gate is tracked through the Step 30b row marker, not through this drift id.

### ~~D-widget-no-content-safety-or-scope-guardrail-2026-05-10~~

**Status:** OPEN — load-bearing for customer trust; tracked against roadmap Step 30d
**Raised by:** Step 30b Phase 4 staging E2E. With domain_config 374 (`tenant=luciel-staging-widget-test`, `domain=cloudfront-staging`, `system_prompt_additions=NULL`) bound to embed key 698, the live widget at `https://d1t84i96t71fsi.cloudfront.net/staging-widget-test.html` accepted and answered an off-topic request ("how are you doing") and engaged substantively with a sensitive-content request ("I need help making a sex toy"). The widget is generally helpful; it is not customer-deployable.
**Design claim:** CANONICAL_RECAP §4 commits Luciel to a behavior contract — staying in scope of what the customer site is for, declining requests outside that scope politely, and refusing sensitive-content engagement that would damage the customer's reputation. ARCHITECTURE §3.3 step 8 (action classification) covers tool-call gating but says nothing about message-content gating. Neither doc currently commits to a content-safety layer or a per-domain on-topic guardrail.
**Repo state:** No content-safety check exists in the request path. `app/policy/` has no moderation module. The chat service threads `domain_config.system_prompt_additions` into the system prompt — that column is the only existing knob — but the staging row was created with `NULL` and the issuance path does not require it to be non-null before a widget goes live. No provider-side moderation API call (OpenAI moderation, Anthropic content filters) is wired into the chat pipeline. The only existing constraint that touches content is `EMBED_REQUIRED_PERMISSIONS = frozenset(['chat'])`, which gates surfaces (no tool calls from widget) but not the content of conversational turns.
**Two distinct gaps inside this drift:**
  - **Gap A — on-topic scoping.** Per-domain prompt that constrains the widget to the customer's vertical. Mitigation today is operational (write `system_prompt_additions` correctly before going live). Real fix is a (1) preflight validator that refuses to mint an embed key against a domain with NULL or empty `system_prompt_additions`, (2) a template library of vetted scope prompts (real estate, support, sales) operators can pick from, and (3) a CI test that asserts an out-of-scope question receives a decline-with-redirect.
  - **Gap B — content safety.** Sensitive/adult/illegal-content refusal, independent of vertical. Cannot be done with prompt alone — needs a moderation gate (provider API or in-process classifier) before the LLM call, and a sanitized refusal response that does not leak the offending text into traces.
**Owning roadmap step:** Step 30d (Widget content safety and scope guardrails) — adopted into CANONICAL_RECAP §12 on 2026-05-10 (commit `d2b05c46`), sitting between Step 30c (action classification — tool gating) and Step 31 (dashboards + validation gate). Step 30c gates *what Luciel does*; Step 30d gates *what Luciel says and what it engages with*. They are sibling problems, not parent/child, and folding 30d into 30c would dilute both. Step 30d's success criterion: with a real REMAX-style domain config, the widget (1) declines an off-topic question with a graceful redirect to the customer's vertical and (2) refuses sensitive-content engagement with a fixed, sanitized response that never reaches the LLM provider's traces in raw form. This drift closes when Step 30d closes.
**Immediate mitigation (Step 30b tail, not Step 30d):** Populate `domain_configs.id=374` with a real-estate scoping and refusal prompt via `PATCH /api/v1/admin/domains/luciel-staging-widget-test/cloudfront-staging` so the staging widget refuses both the off-topic test and the sex-toy test. This raises the floor for the REMAX demo without closing the drift. The drift stays OPEN until Step 30d's preflight validator, provider-side moderation gate, and CI tests land.
**Doc-truthing this pass:** CANONICAL_RECAP §12 carries the Step 30d row (committed `d2b05c46`) that explicitly names this drift as one of its two closing conditions. No ARCHITECTURE prose change yet — Step 30d's design-shape edits (moderation gate in §3.3 and issuance-time preflight in §3.2.2) land surgically when the build lands, per DRIFTS §6 doctrine that a new roadmap step on its own is not a reason to grow the design docs.
**Closeout 2026-05-11 — RESOLVED on full closure (both Gap A on-topic scoping AND Gap B content safety addressed):**
**Closing tag (planned):** `step-30d-content-safety-complete` on merge of `step-30d` into `main`.
**Closing PRs, in order:** #14 (Deliverable A: issuance-time scope-prompt preflight, merged `3cbd489`) → #15 (Deliverable B: per-turn provider-agnostic content-safety moderation gate, fail-closed, default OpenAI, `KeywordModerationProvider` added in 30d-C for deterministic E2E refusal-path coverage, merged `c7d958e`) → #16 (Deliverable C: widget-surface E2E CI harness behind `workflow_dispatch` in v1 + AST contract test, merged `146f133`).
**Resolution:** Gap A closed by the preflight validator (`app/services/scope_prompt_preflight.py`) that refuses to mint a domain-scoped embed key unless the bound `domain_configs.system_prompt_additions` is non-empty; tenant-wide mints intentionally skip the preflight and surface a non-fatal warning, since they pre-date a domain selection. Gap B closed by the per-turn moderation gate in `app/policy/moderation.py` wired into `POST /api/v1/chat/widget`, fail-closed, sanitized SSE refusal (HTTP 200, same three-frame contract so the gate is not fingerprintable), categories never returned to the client. Step 30d Deliverable C adds the harness that exercises both gates against a real backend; in v1 it runs `workflow_dispatch`-only and the AST shape contract is pinned in the backend-free CI lane via `tests/api/test_widget_e2e_harness_shape.py`.
**Pattern E note:** The planned follow-up that adds `pull_request` alongside `workflow_dispatch` in `widget-e2e.yml` keeps the v1 trigger untouched per Pattern E (never delete — deactivate by replacement). The AST contract test's `pull_request absent in v1` assertion is documented in `ci/e2e/README.md` and will flip atomically with that follow-up commit.
**First clean harness run:** widget-e2e dispatch `25695322187` on `main` (2026-05-11, 1m21s, every assertion green end-to-end: alembic upgrades through `b0e003ffa07f`, uvicorn ready on `/health` attempt 3, four admin endpoints 200, happy-path SSE three-frame contract via stub LLM provider, refusal-path REFUSAL_MESSAGE frame via KeywordModerationProvider sentinel) is the observed-clean evidence that flipped CANONICAL_RECAP §12 Step 30d row 🔧 → ✅ in commit `21d7101`. Three pre-existing harness gaps surfaced between the step-30d merge (`4e7ffbc`) and the first clean run and were closed by Pattern E follow-up branches against `main` (never against `step-30d`): pgvector postgres image pin (PR #18 / `ff0e116`, closed run `25690719076` CREATE EXTENSION vector failure), readiness probe path `/healthz` → `/health` (PR #19 / `b517f29`, closed run `25693123650` 404 failure), hermetic stub LLM provider with default-False settings flag + construction-time WARNING (PR #20 / `ec8a72d`, closed run `25694850046` zero-providers failure). Each follow-up extended `tests/api/test_widget_e2e_harness_shape.py` (14 → 15 → 18 tests) to pin the cross-file invariant at PR-time in the backend-free CI lane. No new drifts opened — all three were pre-existing harness inaccuracies surfaced by the first real dispatch, not Step 30d regressions. Cross-refs: ARCHITECTURE §3.2.2 *Issuance* (Gap A surface — preflight validator), §3.3 step 6.5 (Gap B surface — moderation gate), §4.9 (rejected-alternative bullet on prompt-only content gating); CANONICAL_RECAP §12 Step 30d row carries the ✅ marker landed by this same arc.
**Pattern E follow-up landed:** PR #21 merged into `main` as squash commit `e02c282` (2026-05-11), adding `pull_request:` trigger alongside `workflow_dispatch:` in `.github/workflows/widget-e2e.yml` pinned to an 11-path widget-surface allowlist. Run `25697100429` on that PR is the first observed clean `pull_request`-triggered widget-e2e run in repo history (~1m21s, all assertions green) — the v2 trigger self-validated on its own PR once parse-valid. Implementation finding documented in commit history: GitHub Actions parser treats `paths` and `paths-ignore` as mutually exclusive for the same event (a v1 attempt with both was rejected at parse-time, 0s no-jobs failure on run `25696913391`, fixed in the same PR by dropping `paths-ignore` since `paths` already excludes non-matching PRs; sub-tree exclusions go via `!`-prefix inside `paths`). AST contract (`tests/api/test_widget_e2e_harness_shape.py`) grew 18 → 19 tests: the v1 negative `pull_request absent` assertion flipped to a v2 positive `both triggers present` assertion plus a new pin that `pull_request.paths` is the exact 11-entry widget-surface subset and `paths-ignore` is absent — matching the surgical AST-growth pattern of PRs #18/#19/#20.

### ~~D-confirmation-gate-not-enforced-2026-05-09~~

**Status:** RESOLVED on 2026-05-11 (build complete on `step-30c`, merged to `main`, closing tag cut, **and observed enforcing in production on the same day** — see Prod observed-clean note below).
**Closing tag:** `step-30c-action-classification-complete` on `main` commit `99c6eb5` (the doc-truthing commit where code and all three docs first agree that Step 30c is closed; the underlying code-complete commit is `b216300`, three commits earlier — the tag was deliberately re-cut forward onto the doc-truthing commit so the auditor can read the tag as "code + docs agree here"). Merge of `step-30c` into `main` on 2026-05-11.
**Raised by:** Architecture §3.3 step 8 ("Policy gate"), §4.9 (synchronous-only invocation rejected); Canonical Recap §4 (behavior contracts: "Luciel does not take consequential action without permission").
**Original design claim (kept for audit):** Tool invocations are classified into three tiers — **routine** (just do it, audited), **notify-and-proceed** (execute and surface visibly to the customer), and **approval-required** (return a confirmation request before executing). Approval is required only when an action is genuinely consequential — irreversible, high-blast-radius, off-pattern relative to the customer's established usage, or where Luciel itself is uncertain. Routine and reversible external-facing actions are not gated, because gating them would make Luciel feel timid and bureaucratic and would violate the senior-advisor voice in Recap §3.
**Resolution:** The three-tier gate is now enforced server-side at the tool broker. `app/policy/action_classification.py` ships a pluggable provider in the same shape as `app/policy/moderation.py` (Step 30d Deliverable B): `ActionTier` enum (ROUTINE / NOTIFY_AND_PROCEED / APPROVAL_REQUIRED), a Protocol-based `ActionClassifier`, a `StaticTierRegistryClassifier` that reads each tool's `declared_tier` from the class body, a `FailClosedActionClassifier` wrapper that converts `ToolTierUndeclared` *and* arbitrary exceptions into APPROVAL_REQUIRED (so an undeclared tier or a future memory-backed off-pattern detector that errors cannot silently escalate privilege), a `NullActionClassifier` for dev with a per-call WARNING, and an `ActionClassificationGate.from_settings` factory. Two new `Settings` knobs (`action_classifier='static'`, `action_classifier_fail_closed=True`) default to the production wiring; an unknown provider value raises `ConfigurationError` at boot. `LucielTool.declared_tier: ActionTier | None = None` on the base class is the new opt-in surface every concrete tool must override; an inherited `None` fails closed to APPROVAL_REQUIRED by design. The three shipped tools were tiered in the same commit: `save_memory` and `get_session_summary` ROUTINE, `escalate_to_human` NOTIFY_AND_PROCEED (the rationale for each tier is recorded in a class-body comment next to the declaration so a reviewer cannot miss it). `ToolBroker.execute_tool` now classifies before dispatch (the call-order invariant is pinned at AST level in `tests/api/test_action_classification.py`), stamps `tier` / `tier_reason` / `classifier` onto every `ToolResult.metadata` with four deliberately-distinct reason codes for audit clarity — `'declared_tier'` (normal classification), `'unknown_tool'` (the broker did not find the tool name in the registry; a stray name from the LLM), `'tier_undeclared'` (the registered tool's maintainer forgot to set `declared_tier` and the fail-closed wrapper caught the `ToolTierUndeclared` exception), and `'classifier_error'` (the classifier raised an arbitrary exception and the wrapper translated it); all three refusal codes still route to APPROVAL_REQUIRED but an auditor can tell the failure modes apart, and short-circuits APPROVAL_REQUIRED with a pending frame containing `pending=True`, `tool_name`, and `proposed_parameters` so the runtime layer can render a confirmation prompt. The historical `ToolBroker(registry)` signature is preserved per Pattern E by making the classifier parameter optional and lazy-importing `settings` for the default. Test coverage in `tests/api/test_action_classification.py` is 28 tests across AST contract + behavioural inline-fakes, mirroring the house style of `tests/api/test_content_safety_gate.py`; the suite runs in the existing backend-free CI lane and pins (a) the action-classification module's public surface, (b) the `ActionTier` enum has exactly three members, (c) `FailClosedActionClassifier.classify` catches both `ToolTierUndeclared` and `Exception`, (d) the broker constructor accepts an injectable classifier, (e) `self._classifier.classify` runs at a lower line number than `tool.execute`, (f) the APPROVAL_REQUIRED `if` branch contains a `Return` at a lower line number than `tool.execute`, (g) the three shipped tiers, (h) `LucielTool.declared_tier=None` default on the base class, and (i) end-to-end broker behaviour against a real `ToolRegistry` for all four code paths (ROUTINE executes, NOTIFY_AND_PROCEED executes, unknown-tool refuses, undeclared-tier refuses without executing). A partner-level live end-to-end harness at `tests/e2e/step_30c_live_e2e.py` (33 claims, backend-free, runs in under a second) exercises the shipped `ToolBroker` + `ToolRegistry` + shipped `LucielTool` subclasses against every English-language clause in CANONICAL_RECAP §12 row 30c — the first per-step live-e2e harness on the project, establishing a precedent documented in `tests/e2e/README.md` (one harness per closed step, a step is not "closed" until its harness is green on the closing branch). The harness caught the audit-signal collision (`tier_reason='unknown_tool'` reused for two operationally-different failure modes) before merge that the 28 unit tests missed; the surgical fix landed on the same branch as follow-up commit `becd92b`.
**Doc-truthing landed in the same commit:** `ARCHITECTURE.md` §3.3 step 8 flipped 📋 → ✅ with the implementation paragraph appended; §4.9 synchronous-only rejected-alternative bullet flipped to ✅ with a reference to the AST contract test that pins the call-order invariant; `CANONICAL_RECAP.md` §4 (the "On consequential" note) and §5 step 4 (the operating loop) both flipped their 30c references from 🔧 "lands in 30c" to ✅ "landed in 30c"; §12 row for Step 30c flipped 📋 → ✅ with the full closeout detail.
**Deliberate non-goals at v1 (carried forward, not regressions):**
  - Real off-pattern detection (an unusual category / amount / recipient / time bumping an invocation to APPROVAL_REQUIRED on top of the static declared tier). Named in Recap §4 and structurally supported by the classifier `Protocol` (a future `OffPatternActionClassifier` can be composed with the static one without touching the broker), but the detector itself needs the four-kinds memory architecture to be queryable, which is still a stub per DRIFTS.md `D-context-assembler-thin-2026-05-09`. Soft-dep; not re-opened here.
  - The customer-facing conversational confirmation UX. The broker returns a structured pending frame; rendering that as a chat-side approval prompt and accepting the customer's natural-language reply ("yes, send it") as approval is the Runtime layer's job and is scoped with Step 31's richer widget UX. The enforcement boundary is closed at the broker today; the conversational shape lands at Step 31.
  - The structured per-invocation audit row write keyed to the same `session_id` / `trace_id`. The broker emits `logger.info` lines with tier+reason+classifier for the interim operator signal; the database audit row is part of the broader widget-chat application-level audit work tracked in `D-widget-chat-no-application-level-audit-log-2026-05-10`. The tier metadata on `ToolResult.metadata` is the contract that downstream audit work will read.
  - Per-tenant tier overrides (e.g. a high-trust tenant downgrading certain notify-and-proceed tools to routine). Out of scope at v1; left to a future Step when there is a paying customer who needs it.
  - LLM-side instruction about the tiered contract. The gate is enforced server-side at the broker; the model does not need to know which tier a tool sits at to call it. A contract that the model has to remember is a contract that degrades silently when the model is swapped. Same rationale as the out-of-band moderation gate in ARCHITECTURE §4.9.
**Prod observed-clean:** Rolled to production on 2026-05-11. ECR image digest `sha256:f0bf303272fb0801eefc4cf0d20d2ddb624f2a5f60c8e845cbe422869739f863` built from `main` HEAD `84339a3` (closing tag target `99c6eb5`) and pushed under ECR tag `step-30c-84339a3`. New task-definition revisions registered against the cluster `luciel-cluster`: `luciel-backend:39` (succeeding rev 38; manifest renamed in repo from `td-backend-rev34.json` to `td-backend-rev39.json` in the same commit as this stanza) and `luciel-worker:19` (succeeding rev 18; manifest renamed from `td-worker-rev14.json` to `td-worker-rev19.json`). Both services accepted the new revisions and rolled forward without invoking the deploymentCircuitBreaker: `luciel-backend-service` showed `PRIMARY :39 running=1 desired=1 failed=0` immediately after the rollover with the old `:38` deployment moving to `DRAINING running=0` on the ALB connection-draining schedule; `luciel-worker-service` showed `PRIMARY :19 running=1 desired=1 failed=0` with the old `:18` deployment going `ACTIVE running=0` immediately. Backend `/ecs/luciel-backend` log group printed the canonical clean-startup sequence (`Started server process [1]` → `Waiting for application startup.` → `Application startup complete.` → `Uvicorn running on http://0.0.0.0:8000`) followed by a steady `GET /health → 200 OK` cadence from both ALB IPs (10.0.10.47 and 10.0.11.10) and zero `ERROR` / `Traceback` / `CRITICAL` hits in the filter window. Worker `/ecs/luciel-worker` log group printed the canonical clean-startup sequence (`Connected to sqs://localhost//` → `[tasks]   . app.worker.tasks.memory_extraction.extract_memory_from_turn` → `celery@ip-10-0-11-201.ca-central-1.compute.internal ready.`) at 22:57:23–22:57:24 UTC, with the old worker performing a clean `Warm shutdown` at 22:58:07 UTC — the correct boot-new-then-drain-old sequence. The runbook used for this rollout is `docs/runbooks/step-30c-action-classification-deploy.md` (committed as `401406d` ahead of the deploy and unchanged during execution). No new environment variables were required: the two new `Settings` knobs (`action_classifier='static'`, `action_classifier_fail_closed=True`) ship with production-correct defaults in `app/core/config.py`, so neither task-def revision needed any `environment` or `secrets` entries beyond the digest update. Real customer-facing traffic on the new revisions began with the first post-rollover `/health 200` returns; the next observable signal as actual tool invocations flow through the gate is `tier_reason='declared_tier'` lines on `ToolResult.metadata`-bearing log records in `/ecs/luciel-backend`. Side observation (does not block this closure but tracked separately): the worker container is still running as `uid=0` (Celery emitted its standard `SecurityWarning: running the worker with superuser privileges`) — a pre-existing condition unchanged by Step 30c, but logged here as the place we first noticed it on the record; see new drift `D-celery-worker-runs-as-root-2026-05-11` for the resolution path.
**Cross-refs:** ARCHITECTURE §3.3 step 8 (design statement, now ✅; also names the four `tier_reason` codes); ARCHITECTURE §4.9 (synchronous-only rejected-alternative bullet, now ✅; also names the live-e2e harness); CANONICAL_RECAP §4 ("On consequential" note, now ✅); CANONICAL_RECAP §5 step 4 (operating loop, now ✅); CANONICAL_RECAP §12 Step 30c row (now ✅; names all four `tier_reason` codes, the live-e2e harness, **and the prod observed-clean record**); `tests/api/test_action_classification.py` (28 unit tests); `tests/e2e/step_30c_live_e2e.py` (33-claim live e2e harness); `tests/e2e/README.md` (the per-step live-e2e harness precedent); `docs/runbooks/step-30c-action-classification-deploy.md` (the prod-rollout runbook used on 2026-05-11); soft-dep on `D-context-assembler-thin-2026-05-09` (off-pattern detector); downstream dep `D-widget-chat-no-application-level-audit-log-2026-05-10` (per-invocation audit row); side-observation drift `D-celery-worker-runs-as-root-2026-05-11` (logged during this rollout; pre-existing; tracked separately).

### ~~D-step-24-5c-impl-backlog-2026-05-11~~

**Status:** RESOLVED on 2026-05-11 (v1 implementation complete on `step-24-5c-*` sub-branches, all five merged to `main`, closing tag cut on the doc-truthing commit). The Step 24.5c roadmap row in CANONICAL_RECAP §12 flipped `📋 Planned` → `✅ Implemented` in this same commit.
**Closing tag:** `step-24-5c-cross-channel-identity-complete`, cut on this doc-truthing commit per the Step 30c `99c6eb5` precedent (so the tag reads "code + docs agree here").
**Implementation arc (as built):** Five sub-branch PRs merged to `main` on 2026-05-11, in order:
  - **PR #24 — sub-branch 1: models + migration.** `app/models/conversation.py` (UUID PK, scope-bearing FKs, `active`, `created_at`, `last_activity_at`); `app/models/identity_claim.py` (UUID PK, native Postgres enum `identity_claim_type` ∈ {EMAIL, PHONE, SSO_SUBJECT}, `user_id` FK to `users.id`, scope-bearing FKs, `issuing_adapter`, nullable `verified_at`, `active`); Alembic migration `3dbbc70d0105` (down_rev `a7c1f4e92b85`) installs both tables plus the load-bearing unique constraint `uq_identity_claims_type_value_scope` on `(claim_type, claim_value, tenant_id, domain_id)` that physically enforces the §4.9 cross-tenant-federation-rejected design. Contract tests: `tests/api/test_step24_5c_models_shape.py` (36 tests).
  - **PR #25 — sub-branch 2: `CrossSessionRetriever`.** `app/memory/cross_session_retriever.py` lands as a sibling of the per-session retriever. Signature: `CrossSessionRetriever(db).retrieve(*, conversation_id, tenant_id, domain_id, limit=20, exclude_session_id=None) -> list[CrossSessionPassage]`. `CrossSessionPassage` is a frozen dataclass with `content`, `role`, `source_session_id`, `source_channel`, `timestamp`, `message_id`. Four-layer scope discipline: input-validation fail-loud at the boundary (TypeError on non-UUID `conversation_id`, ValueError on blank `tenant_id`/`domain_id`), SQL WHERE clause, post-query defense-in-depth loop that re-asserts scope on each materialised row (logs an ERROR and drops the row if it ever fires — operationally impossible via the WHERE, but the defense is the four-layer §4.7 promise made concrete). Contract tests: `tests/api/test_step24_5c_cross_session_retriever_shape.py` (28 tests).
  - **PR #26 — sub-branch 3: `IdentityResolver`.** `app/identity/resolver.py` is the §3.3 step 4 hook. `normalise_claim_value(claim_type, raw)` canonicalises (EMAIL lowercase, PHONE E.164 shape, SSO_SUBJECT opaque); `IdentityResolution` frozen dataclass; `IdentityResolver(db).resolve(*, claim_type, claim_value, tenant_id, domain_id, issuing_adapter) -> IdentityResolution`. Existing-claim path looks up by the unique constraint and returns the bound `User` + most-recent-active `Conversation` under the scope; miss path mints `User` + `IdentityClaim` + `Conversation` in the same SQLAlchemy session (resolver itself does NOT commit — caller owns the transaction, AST-verified). Contract tests: `tests/api/test_step24_5c_identity_resolver_shape.py` (34 tests).
  - **PR #27 — sub-branch 4: adapter claims wiring.** `SessionRepository.create_session()` + `SessionService.create_session()` now accept optional `conversation_id` (legacy path stays untouched if the caller doesn't pass it). New `SessionService.create_session_with_identity(*, tenant_id, domain_id, agent_id, channel, claim_type, claim_value, issuing_adapter) -> SessionWithIdentity` is the ingress-adapter-facing surface that lazy-imports the resolver, runs it inside the same db handle, and creates a session bound to the resolution in one transaction. `SessionWithIdentity` frozen dataclass bundles the new session, the resolved `user_id` / `conversation_id` / `identity_claim_id`, and `is_new_user` / `is_new_conversation` booleans so callers can render the right onboarding UX. Widget + API routes are deliberately NOT touched in this sub-branch — that's deferred to Step 31's richer widget UX work. Contract tests: `tests/api/test_step24_5c_adapter_claims_shape.py` (14 tests).
  - **PR #28 — sub-branch 5: live e2e + this doc-truthing commit.** `tests/e2e/step_24_5c_live_e2e.py` is the partner-level live end-to-end harness (Step 30c precedent shape: runnable script, NOT pytest, exits 0 on all-claims-pass, exits 1 on any-claim-fail, exits 2 on env-not-set-up). Six claim groups exercise the v1 success criterion against the live shipped code: (1) widget adapter mints fresh identity via the §3.3 step 4 hook; (2) widget turn lands and `CrossSessionRetriever` surfaces it both with and without `exclude_session_id`; (3) programmatic API asserts the SAME email and joins the same `user_id` / `conversation_id` / `identity_claim_id`; (4) database state truthing — exactly ONE `identity_claims` row, ONE `conversations` row, TWO sessions, all joined by the same `conversation_id`; (5) programmatic API picks up the widget conversation via the retriever (the headline criterion), with a noise conversation under a different `domain_id` proving the scope filter is honoured; (6) same email under a different `domain_id` mints a separate `User` (the §4.9 cross-tenant-federation rejection enforced at the schema layer). POSTGRES REQUIRED — the harness refuses sqlite at the top because the schema uses a native Postgres enum and UUID columns. Backend-free harness-shape pin in `tests/api/test_step24_5c_live_e2e_shape.py` (10 AST + filesystem invariants — script existence, module-docstring required markers, all four runtime-surface imports, ClaimType + model imports, the non-Postgres refusal guard, the dual exit-code contract, both channel labels named in the success criterion, the `create_session_with_identity` call by name, the `exclude_session_id` kwarg). The shape test runs in the existing backend-free CI job so accidental deletion or signature drift fails CI loudly without needing a live DB.
**Doc-truthing landed in the same commit:** `CANONICAL_RECAP.md` §11 Q8 status flipped `📋 Planned (Step 24.5c)` → `✅ Implemented (Step 24.5c, 2026-05-11)` with the closeout stanza naming the three primitives, the §3.3 step 4 hook, and the live e2e proof; §12 Step 24.5c row flipped `📋 Planned` → `✅ Implemented` with the full five-PR closeout, the closing-tag commitment, and pointers to both the live e2e and the shape pin. `ARCHITECTURE.md` §3.2.11 lead-in flipped `📋 Design committed` → `✅ Implemented` with the five-PR breakdown and the live-e2e + shape-pin pointers; §3.3 step 5 cross-session-retriever marker flipped `📋` → `✅`; both §4.9 rejected-alternative bullets flipped `📋` → `✅` (the cross-tenant-federation bullet now names the unique-constraint mechanism that physically enforces the rejection, plus the live e2e CLAIM 6 that proves it).
**Deliberate non-goals at v1, carried forward as not-regressions:** (same list the open block carried) end-user-driven claim verification (Step 34a + Step 31); cross-tenant identity federation (Step 38 — physically rejected at the schema layer in v1); a per-`User` inbox view spanning all channels (Step 31 dashboards); the voice / SMS / email channel adapters themselves (Step 34a — the §3.2.11 primitives are framework-ready for them). Widget + programmatic-API ROUTE wiring (the request-time path that calls `create_session_with_identity` instead of `create_session`) is also deferred to Step 31's richer widget UX work; the §3.3 step 4 service surface is built and proven end-to-end against real Postgres, but the routes still take the legacy `create_session` path until Step 31 lands the embed-key→User claim assertion alongside its onboarding UX.
**Test totals:** 112 contract tests across sub-branches 1–4 (36 + 28 + 34 + 14) plus the 10-test harness-shape pin in sub-branch 5 = 122 backend-free tests pinning the Step 24.5c surface. The live e2e (six claim groups, ~30 individual claims) runs against a developer's Postgres dev DB on demand and on the doc-truthing commit — it is the integration proof that the four sub-branches compose into the v1 success criterion.
**Cross-refs:** ARCHITECTURE §3.2.11 (now ✅, names this drift in past tense); ARCHITECTURE §3.3 step 5 (now ✅); ARCHITECTURE §4.9 (both bullets now ✅; the cross-tenant-federation bullet names the unique-constraint mechanism); CANONICAL_RECAP §11 Q8 (now ✅); CANONICAL_RECAP §12 Step 24.5c row (now ✅; names the five PRs and both proofs); `tests/e2e/step_24_5c_live_e2e.py` (the v1 live-e2e proof, Postgres-only); `tests/e2e/README.md` (per-step live-e2e harness precedent — Step 24.5c is the second step on the project to ship one, after Step 30c); `tests/api/test_step24_5c_live_e2e_shape.py` (backend-free harness-shape pin); `tests/api/test_step24_5c_{models,cross_session_retriever,identity_resolver,adapter_claims}_shape.py` (sub-branch 1–4 contract tests); `D-channels-only-chat-implemented-2026-05-09` (Step 34a — the 24.5c primitives are framework-ready for voice/SMS/email when those adapters land); `D-context-assembler-thin-2026-05-09` (adjacent — cross-session retriever output will join per-session memory at context-assembly time when that consolidation work happens; not a 24.5c gating dep). Side-observation: no new drifts were opened during the 24.5c arc; the pre-existing test failures in `tests/api/test_step29y_cluster1_scope_hardening.py` around env-var dependence are UNRELATED and stay tracked in their own surface.

### Step 29.z — Repo-vs-Design audit (no findings)

**Status:** AUDIT COMPLETE on 2026-05-09; no fix-here drifts surfaced.
**Audit scope:** Treat `CANONICAL_RECAP.md`, `ARCHITECTURE.md`, and `DRIFTS.md` as ground truth; walk the repo top-down; for any divergence, classify as fix-here, new drift, or doc edit. Sandbox-only, no behavior changes, no AWS, no production touched.
**What was checked:** repo top-level layout; `app/` mapped to the six-component model (Persona, Runtime, Memory, Tool, Policy, Observability) plus the four cross-cutting concerns (scope isolation, audit chain, Pattern E, retention); 25 verification pillars file presence; alembic migration count and recency; tenant-vs-scope terminology footprint (113 files, ~2000 occurrences); root-level task-definition manifests; runtime stub depth.
**Result:** Zero material code-vs-doc misalignments that warrant a repo fix in this branch. Every gap that surfaced during the audit was already an open drift in Section 3 (`D-context-assembler-thin`, `D-celery-task-surface-thin`, `D-retention-purge-worker-missing`, `D-confirmation-gate-not-enforced`, `D-channels-only-chat-implemented`, `D-external-integrations-llm-only`) and is owned by an existing roadmap step (30c, 34, 34a). Cosmetic items deliberately not pursued: pillar filename `cross_tenant_*` vs doc concept `scope_leak` (renaming would touch the verification entry points without changing behavior); root-level `td-*.json` task-def manifests (intentionally tracked, referenced by deployment runbooks). The `tenant` term is preserved throughout the codebase as the legitimate top-of-hierarchy scope-level name per the resolution of `D-tenant-language-vs-scope-language-2026-05-09`.
**Why this entry exists:** A no-findings audit is itself a finding worth recording — it certifies that on this date, with these three docs as ground truth, the repo was aligned. Future drift hunts can anchor against this baseline. The `step-29z-repo-reconciliation` branch was cut and abandoned without commits because there was nothing to ship.
**Closing commit:** this commit.

---

## Section 6 — How drifts are added, closed, or reopened

**Adding a drift.** Append to Section 3 (open) or Section 4 (production-pending), with a fresh `D-<slug>-YYYY-MM-DD` id, a status, the section that raised it, the design claim, the repo or prod state, and a resolution path. Do not add to closed-drift section while the drift is still open.

**Closing a drift.** Move the entry verbatim to Section 5, wrap the heading in `~~strikethrough~~`, fill in the closing commit (and tag if relevant) and a one-line resolution. Do not delete the entry from Section 3 — move it. The audit chain depends on the body of each drift remaining readable after closure.

**Reopening a drift.** Open a new drift with a fresh id and a date suffix matching the day of reopening. Reference the closed predecessor in the body. The closed predecessor stays closed; reopening is a new story.

**Marker propagation.** As drifts close, the corresponding section in `ARCHITECTURE.md` or `CANONICAL_RECAP.md` gets the appropriate ✅ / 🔧 / 📋 / 🔬 marker added in surgical edits. The marker is the connective tissue between the design docs and this register; the doc edit and the drift closure are part of the same commit.

**What does NOT belong in the design docs.** The three canonical docs (`CANONICAL_RECAP.md`, `ARCHITECTURE.md`, `DRIFTS.md`) are stable references, not running notebooks. Build-time detail — framework choice, library selection, schema column lists, transport-protocol rationale, branding-knob inventories, sandbox demo plans — belongs in **commit messages** and **code-level docstrings**, not in the canonical docs. The canonical docs grow only when the architectural shape changes: a new cross-cutting property, a new component, a deletion of something that is no longer true, or a roadmap step landing such that its success criterion is provably met. A new roadmap step, on its own, is not a reason to grow the docs — the recap row already exists, and implementation detail is recoverable from `git log` and the code itself. If a build genuinely needs a multi-day shared scratchpad, it lives in `docs/in-flight/<slug>.md` during the build and is **deleted** (not archived) on merge. This principle exists because docs that grow with every roadmap step become unparsable; the discipline of keeping them stable is what makes them useful as references.

---

## Section 7 — Source-of-truth rule

If a chat summary, a session recap, a slide, an email, or any other artifact contradicts this document about the status of a drift, **this document wins**. Update the document; do not let the contradicting artifact stand.
